<h1>Projeto 01 - Portfólio - Estilização da Página index - Seção Contact</h1>



Na **segunda etapa da estilização da seção Contact**, o foco será o **formulário de contato**, responsável por permitir que o usuário envie mensagens de forma direta e prática.

Nesta etapa, iremos trabalhar a **estrutura visual e funcional do formulário**, definindo o layout dos campos, rótulos, áreas de texto e botões, além dos espaçamentos, tipografia e estados de interação. Uma estilização adequada do formulário garante **clareza**, **facilidade de uso** e **acessibilidade**, orientando o usuário durante o preenchimento e reduzindo a chance de erros.

Ao final dessa etapa, o formulário estará **integrado visualmente ao restante do site**, mantendo consistência com a identidade visual definida e proporcionando uma experiência de contato simples, intuitiva e profissional.

<br />

<h2>👣 Passo 01 - Estilizar a Seção Formulário</h2>



1. Abra o arquivo **style.css** e adicione os trechos de código abaixo:

<br />

### ✅ Seção Form

<div align="center"><img src="https://i.imgur.com/q94HG9x.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 500:** Selecionamos o elemento `form`, responsável pelo formulário de contato.

**Linha 501:** A propriedade `display: flex;` cria um layout flexível.

**Linha 502:** A propriedade `flex-direction: column;` organiza os campos do formulário em coluna.

**Linha 503:** A propriedade `gap` define o espaçamento vertical entre os campos.

<br />

### ✅ Seção Form - Label

<div align="center"><img src="https://i.imgur.com/Xoi5MC1.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 506:** Selecionamos os rótulos (`label`) do formulário.

**Linha 507:** A propriedade `font-size` define o tamanho do texto do rótulo.

**Linha 508:** A propriedade `font-weight: 600;` destaca os rótulos visualmente.

<br />

### ✅ Seção Form - Input e Text Area

<div align="center"><img src="https://i.imgur.com/n83ultL.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linhas 511 a 512:** Selecionamos os campos de entrada (`input` e `textarea`) do formulário.

**Linha 513:** A propriedade `width: 100%;` faz com que os campos ocupem toda a largura disponível.

**Linha 514:** A propriedade `padding` cria espaçamento interno confortável para digitação.

**Linha 515:** A propriedade `border` define uma borda inicial neutra.

**Linha 516:** A propriedade `border-radius` suaviza os cantos dos campos.

**Linha 517:** A propriedade `font-family: inherit;` mantém a fonte padrão do projeto.

**Linha 518:** A propriedade `font-size` define o tamanho do texto digitado.

**Linha 519:** A propriedade `transition` suaviza a mudança da cor da borda.

**Linhas 522 e 523:** Definimos o estado de foco dos campos do formulário.

**Linha 524:** A borda muda para a cor roxa principal, indicando que o campo está ativo.

**Linha 525:** A propriedade `outline: none;` remove o contorno padrão do navegador.

<br />

### ✅ Seção Form - Text Area e Span

<div align="center"><img src="https://i.imgur.com/UxZEHFi.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 528:** Selecionamos o campo `textarea`.

**Linha 529:** A propriedade `min-height` define uma altura mínima para o campo de texto.

**Linha 530:** A propriedade `resize: none;` impede que o usuário redimensione o campo manualmente.

**Linha 533:** Selecionamos o elemento `span` dentro do formulário.

**Linha 534:** A propriedade `margin-top` negativa aproxima a mensagem do campo relacionado.

**Linha 535:** A propriedade `font-size` define um tamanho menor para a mensagem.

**Linha 536:** A propriedade `color` aplica a cor de erro, utilizada para mensagens de validação.

<br />

<div align="left"><img src="https://i.imgur.com/63FhSvn.png" title="source: imgur.com" width="4%"/> <a href="https://www.w3schools.com/cssref/css3_pr_resize.php" target="_blank"><b>Documentação: Resize</b></a></div>

<br />

<h2>👣 Passo 02 - Visualizar a Página no Navegador</h2>



1. Abra a página index.html no Navegador, através do **Live Server** e clique no item **Contato** do menu:

<div align="center"><img src="https://i.imgur.com/vSGGOM0.png" title="source: imgur.com" /></div>

Após a aplicação das novas classes e propriedades na folha de estilos padrão do website, a **seção Contact** recebeu ajustes estruturais e visuais que organizam o conteúdo de contato, redes sociais e formulário, tornando a interação mais clara e acessível:

- O contêiner `.contact-container` passou a utilizar **CSS Grid** com duas colunas, permitindo a separação equilibrada entre **informações/redes sociais** e **formulário**, com espaçamento adequado entre as áreas.
- Os títulos e parágrafos da área `.contact-info` receberam **margens padronizadas**, melhorando a hierarquia visual e a leitura do conteúdo.
- O bloco de redes sociais (`.social-container`) utiliza **Flexbox**, centralizando os ícones e mantendo espaçamento uniforme entre eles.
- Cada `.social-icon` recebeu **dimensões fixas, padding e bordas arredondadas**, criando áreas de clique consistentes e visualmente destacadas.
- Os ícones em imagem foram estilizados com **filtro em tons neutros**, garantindo uniformidade visual, e recebem **efeito de hover com alteração de cor**, reforçando a interatividade.
- O formulário foi organizado em **layout vertical** com espaçamento consistente entre campos, facilitando o preenchimento.
- Os rótulos (`label`) receberam **destaque tipográfico**, auxiliando na identificação clara dos campos.
- Os campos de entrada e a área de texto receberam **bordas suaves, tipografia consistente e estados de foco personalizados**, oferecendo feedback visual ao usuário durante a interação.
- O campo de texto (`textarea`) possui **altura mínima definida** e redimensionamento desativado, mantendo o layout estável.
- As mensagens auxiliares ou de erro (`span`) utilizam **cor de estado** e tamanho reduzido, comunicando validações de forma clara e discreta.

Essas definições tornam a seção Contact mais **organizada, intuitiva e funcional**, melhorando a usabilidade tanto na visualização das redes sociais quanto no envio de mensagens pelo formulário, além de manter consistência visual com o restante do site.

Para visualizar os posicionamento dos elementos HTML no Navegador, ative a extensão **Pesticide**, clicando no botão <img src="https://i.imgur.com/3E84PHm.png" title="source: imgur.com" width="4%"/> em **Extensões**:

<div align="center"><img src="https://i.imgur.com/tA1srCH.png" title="source: imgur.com" /></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>