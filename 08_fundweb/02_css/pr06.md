<h1>Projeto 01 - Portfólio - Estilização da Página index - Seção About</h1>



Depois de estilizar a Seção Hero, vamos estilizar a **seção About**, responsável por **apresentar informações institucionais ou pessoais** sobre o site, produto ou profissional. Essa área geralmente contém um **texto descritivo**, destacando propósito, valores, trajetória ou objetivos, ajudando o usuário a compreender melhor o contexto da aplicação.

Uma boa estilização da seção About garante **leitura confortável**, **organização do conteúdo** e **hierarquia visual clara**, facilitando a assimilação das informações. Além disso, contribui para transmitir **credibilidade**, reforçar a identidade do projeto e criar uma conexão mais próxima com o usuário.

<br />

<h2>👣 Passo 01 - Estilizar a Seção About</h2>



1. Abra o arquivo **style.css** e adicione os trechos de código abaixo:

<br />

### ✅ Seção About (Container)

<div align="center"><img src="https://i.imgur.com/RYZuUQw.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 250:** Selecionamos a classe `.about-container`, responsável pela estrutura principal da seção *about*.

**Linha 251:** A propriedade `display: flex;` cria um layout flexível, permitindo alinhar a imagem e o conteúdo lado a lado.

**Linha 252:** A propriedade `align-items: center;` centraliza verticalmente os elementos dentro do contêiner.

**Linha 253:** A propriedade `gap` define o espaçamento entre os elementos internos, utilizando a variável `--spacing-2xl`.

**Linha 254:** A propriedade `margin` aplica espaçamento externo vertical e centraliza horizontalmente a seção na página.

**Linha 255:** A propriedade `border-radius` arredonda os cantos do contêiner, suavizando o visual.

**Linha 256:** A propriedade `background-color` define um fundo cinza claro, destacando a seção em relação ao restante da página.

<br />

### ✅ Painel Esquerdo (Imagem)

<div align="center"><img src="https://i.imgur.com/HuBLQhG.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 259:** Selecionamos a classe `.about-image`, responsável pelo contêiner da imagem do perfil.

**Linha 260:** A propriedade `flex: 0 0 17.5rem;` define uma largura fixa para a imagem, impedindo que ela cresça ou encolha.

**Linha 263:** Selecionamos a imagem dentro de `.about-image`.

**Linha 264:** A propriedade `width: 100%;` faz com que a imagem ocupe toda a largura do contêiner.

**Linha 265:** A propriedade `border` cria uma borda espessa com a cor roxa principal do projeto.

**Linha 266:** A propriedade `border-radius: var(--radius-full);` transforma a imagem em um formato circular.

**Linha 267:** A propriedade `box-shadow` adiciona uma sombra mais intensa, criando profundidade visual.

<br />

### ✅ Painel Direito (Texto)

<div align="center"><img src="https://i.imgur.com/Rv9lDqs.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linhas 270 a 272:** Selecionamos os títulos `<h2>` das seções *projects*, *about* e *contact*. Como essas seções compartilham a mesma estilização de título `<h2>`, os seletores foram agrupados em uma única regra, evitando repetição de código e facilitando a manutenção do estilo.

**Linha 273:** A propriedade `margin-bottom` cria espaçamento inferior em relação ao texto.

**Linha 274:** A propriedade `font-size` define um tamanho de fonte grande para o título da seção.

**Linha 275:** A propriedade `font-weight: 700;` reforça o destaque visual do título.

**Linha 276:** A propriedade `color` aplica um tom roxo mais escuro, alinhado à identidade visual do projeto.

**Linha 279:** Selecionamos os parágrafos dentro de `.about-content`.

**Linha 280:** A propriedade `margin-bottom` adiciona espaçamento entre os parágrafos.

**Linha 281:** A propriedade `text-align: justify;` alinha o texto dos dois lados, criando um bloco mais uniforme.

<br />

### ✅ Painel Inferior (Botões e Dados do GitHub)

<div align="center"><img src="https://i.imgur.com/TR2l12l.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 284:** Selecionamos a classe `.about-buttons-data`, responsável por agrupar botões e as informações numéricas do GitHub.

**Linha 285:** A propriedade `display: flex;` organiza os elementos em linha.

**Linha 286:** A propriedade `justify-content: space-between;` distribui os elementos nos extremos do contêiner.

**Linha 287:** A propriedade `align-items: center;` alinha verticalmente os itens.

**Linha 288:** A propriedade `gap` define o espaçamento entre os elementos.

**Linha 289:** A propriedade `margin-top` cria um afastamento em relação ao conteúdo anterior.

**Linha 292:** Selecionamos o botão dentro de `.about-buttons-data`.

**Linha 293:** A propriedade `margin-right: auto;` empurra os demais elementos para a direita, mantendo o botão alinhado à esquerda.

<br />

### ✅ Card - Dados do GitHub

<div align="center"><img src="https://i.imgur.com/xLOnZSJ.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 296:** Selecionamos a classe `.data-container`, responsável pelo agrupamento dos dados informativos.

**Linha 297:** A propriedade `display: flex;` organiza os dados em linha.

**Linha 298:** A propriedade `gap` define o espaçamento entre os itens.

**Linha 299:** A propriedade `margin-top: 0;` remove espaçamento superior adicional.

**Linha 302:** Selecionamos a classe `.data-item`, que representa cada bloco de informação.

**Linha 303:** A propriedade `display: flex;` cria um contêiner flexível.

**Linha 304:** A propriedade `flex-direction: column;` organiza número e rótulo em coluna.

**Linha 305:** A propriedade `align-items: center;` centraliza o conteúdo horizontalmente.

**Linha 308:** Selecionamos a classe `.data-number`.

**Linha 309:** A propriedade `font-size` define um tamanho de destaque para os números.

**Linha 310:** A propriedade `font-weight: 700;` reforça visualmente os valores.

**Linha 311:** A propriedade `text-align: center;` centraliza o texto.

**Linha 312:** A propriedade `color` aplica o roxo principal aos números.

**Linha 315:** Selecionamos a classe `.data-label`.

**Linha 316:** A propriedade `font-size` define o tamanho do texto descritivo.

**Linha 317:** A propriedade `text-align: center;` centraliza o rótulo.

**Linha 318:** A propriedade `color` aplica um tom de cinza, criando contraste suave com os números.

<br />

<h2>👣 Passo 02 - Visualizar a Página no Navegador</h2>



1. Abra a página index.html no Navegador, através do **Live Server** e clique no item **Sobre** do menu:

<div align="center"><img src="https://i.imgur.com/ap4aXzx.png" title="source: imgur.com" /></div>

Após a aplicação das propriedades no código acima, a **seção About** recebeu ajustes estruturais e visuais que organizam o conteúdo e reforçam a apresentação das informações pessoais ou institucionais:

- O contêiner `.about-container` passou a utilizar **Flexbox**, alinhando imagem e texto horizontalmente, com espaçamento adequado entre os elementos, além de margem automática para centralização e **fundo destacado** com bordas arredondadas.
- A área `.about-image` teve sua largura controlada, garantindo **proporção consistente** para a imagem de perfil.
- A imagem recebeu **borda espessa**, **formato circular** e **sombra**, criando um ponto de destaque visual e reforçando a identidade da seção.
- O título `<h2>` foi estilizado com **tamanho de fonte maior**, peso elevado e **cor de destaque**, estabelecendo claramente a hierarquia do conteúdo.
- Os parágrafos utilizam **espaçamento inferior** e **alinhamento justificado**, melhorando a leitura e a distribuição do texto.
- O bloco `.about-buttons-data` organiza botões e informações adicionais com **Flexbox**, distribuindo os elementos de forma equilibrada e mantendo alinhamento vertical consistente.
- O conjunto de dados (`.data-container`) apresenta informações complementares em **layout horizontal**, enquanto cada `.data-item` organiza seus elementos em coluna.
- Os valores numéricos receberam **destaque tipográfico e cor contrastante**, enquanto os rótulos utilizam tipografia neutra, facilitando a leitura e a compreensão dos dados.

Essas definições tornam a seção About mais **organizada, informativa e visualmente atrativa**, contribuindo para uma apresentação clara e profissional do conteúdo.

2. Para visualizar os posicionamento dos elementos HTML no Navegador, ative a extensão **Pesticide**, clicando no botão <img src="https://i.imgur.com/3E84PHm.png" title="source: imgur.com" width="4%"/> em **Extensões**:

<div align="center"><img src="https://i.imgur.com/HZdcjdS.png" title="source: imgur.com" /></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>