<h1>Projeto 01 - Portfólio - Estilização da Página index - Seção Header</h1>



Depois de posicionar os elementos principais na tela, vamos estilizar a **seção Header**, responsável pelo **cabeçalho da página**. Essa área normalmente abriga o **título ou logotipo do site** e o **menu de navegação**, funcionando como o principal ponto de orientação para o usuário.

Uma boa estilização do header garante **clareza visual**, **hierarquia de informações** e **acesso rápido às seções do site**, além de contribuir para a identidade visual da aplicação e para uma experiência de navegação mais intuitiva.

<br />

<h2>👣 Passo 01 - Estilizar a Seção Header (cabeçalho)</h2>



1. Abra o arquivo **style.css** e adicione os trechos de código abaixo:

<br />

### ✅ Seção Header

<div align="center"><img src="https://i.imgur.com/bUvXjL4.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 145:** Selecionamos a seção `<header>`, responsável pela estrutura principal do cabeçalho da página.

**Linha 146:** A propriedade `position: sticky;` faz com que a **Seção Header** permaneça visível no topo da página durante a rolagem, após atingir sua posição inicial.

**Linha 147:** A propriedade `top: 0;` define que a **Seção Header** ficará fixada no topo da janela do navegador quando o efeito *sticky* estiver ativo.

**Linha 148:** A propriedade `z-index: 1000;` garante que a **Seção Header** fique acima de outros elementos da página, evitando sobreposições indesejadas.

**Linha 149:** A propriedade `width: 100%;` faz com que a **Seção Header** ocupe toda a largura da tela.

**Linha 150:** A propriedade `height: 5rem;` define a altura fixa a **Seção Header**.

**Linha 151:** A propriedade `border-bottom` cria uma borda inferior, utilizando um espaçamento fino e a cor cinza clara, separando visualmente a **Seção Header** do conteúdo.

**Linha 152:** A propriedade `background-color` define a cor de fundo a **Seção Header** com um tom roxo mais escuro da paleta do projeto.

**Linha 153:** A propriedade `box-shadow` adiciona uma sombra sutil, criando profundidade e destacando a **Seção Header** do restante da página.

**Linha 156:** Selecionamos todos os links (`<a>`) dentro da **Seção Header**.

**Linha 157:** A propriedade `font-weight: 600;` deixa o texto dos links mais destacado.

**Linha 158:** A propriedade `color` define a cor padrão dos links como branca.

**Linha 159:** A propriedade `transition` aplica uma animação suave na mudança de cor ao interagir com os links.

**Linhas 162 a 164:** Definimos o estado *hover* dos links da **Seção Header**.

**Linha 163:** A cor do texto muda para um tom roxo mais claro, indicando interatividade.

<br />

### ✅ Título e Menu de Navegação

<div align="center"><img src="https://i.imgur.com/3ry08hk.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 166:** Selecionamos o elemento com o identificador (id) `#titulo`, geralmente utilizado para o nome ou logo do site.

**Linha 167:** A propriedade `font-size` define um tamanho de fonte maior, destacando o título da **Seção Header**.

**Linha 168:** A propriedade `font-weight: 700;` aplica um peso mais forte, reforçando a hierarquia visual.

**Linha 171:** Selecionamos a classe `.menu-content`, que organiza o conteúdo interno do menu.

**Linha 172:** A propriedade `display: flex;` cria um layout flexível para alinhar os elementos internos.

**Linha 173:** A propriedade `justify-content: space-between;` distribui os elementos nas extremidades, separando o título do menu de navegação.

**Linha 174:** A propriedade `align-items: center;` centraliza verticalmente os itens dentro do menu.

**Linha 175:** A propriedade `max-width` limita a largura do conteúdo, mantendo alinhamento com o restante da página.

**Linha 176:** A propriedade `height: 100%;` faz com que o conteúdo ocupe toda a altura do menu.

**Linha 177:** A propriedade `margin: 0 auto;` centraliza o conteúdo horizontalmente.

**Linha 178:** A propriedade `padding` adiciona espaçamento horizontal interno ao menu.

**Linha 181:** Selecionamos a classe `.menu-list`, responsável pela lista de navegação.

**Linha 182:** A propriedade `display: flex;` organiza os itens do menu horizontalmente.

**Linha 183:** A propriedade `gap` define o espaçamento entre os itens da lista.

**Linha 184:** A propriedade `list-style: none;` remove os marcadores padrão da lista.

**Linha 187:** Selecionamos os links dentro da lista do menu.

**Linha 188:** A propriedade `padding` aumenta a área clicável dos links, melhorando a usabilidade.

<br />

<h2>👣 Passo 02 - Visualizar a Página no Navegador</h2>



1. Abra a página index.html no Navegador, através do **Live Server**:

<div align="center"><img src="https://i.imgur.com/w6oASQL.png" title="source: imgur.com" /></div>

Após a aplicação das propriedades no código acima, o **cabeçalho da página** recebeu ajustes estruturais e visuais que reforçam a navegação e a identidade do layout:

- O `<header>` passou a ser **fixo no topo durante a rolagem** (`position: sticky`), garantindo acesso constante ao menu de navegação.
- Foi definida uma **altura fixa** e **largura total**, criando um cabeçalho estável e previsível em diferentes resoluções.
- A aplicação de **cor de fundo**, **borda inferior** e **sombra suave** destaca visualmente o cabeçalho em relação ao conteúdo da página.
- Os links do menu receberam **estilo tipográfico consistente**, cor clara e **efeito de hover**, melhorando a legibilidade e o feedback visual.
- O título principal foi destacado com **maior tamanho e peso de fonte**, reforçando a hierarquia visual.
- O container interno `.menu-content` utiliza **Flexbox** para alinhar e distribuir os elementos horizontalmente, mantendo-os centralizados dentro de uma largura máxima.
- A lista de navegação foi organizada com **Flexbox e espaçamento uniforme**, garantindo um menu limpo, alinhado e fácil de escanear visualmente.

Essas alterações tornam o cabeçalho mais **funcional, acessível e consistente**, contribuindo para uma experiência de navegação clara e profissional em todo o site.

2. Para visualizar os posicionamento dos elementos HTML no Navegador, ative a extensão **Pesticide**, clicando no botão <img src="https://i.imgur.com/3E84PHm.png" title="source: imgur.com" width="4%"/> em **Extensões**:

<div align="center"><img src="https://i.imgur.com/BEoEBrW.png" title="source: imgur.com" /></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>
