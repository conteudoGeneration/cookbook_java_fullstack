# Transformações (Transforms)



Até agora, você aprendeu a criar animações que **mudam cores e opacidades**. Esses efeitos já deixam uma interface mais viva, mas para criar **movimentos reais**, como **mover**, **girar**, **aumentar** ou **inclinar** um elemento, precisamos entender a propriedade **`transform`**.

A propriedade `transform` é o que dá movimento aos elementos. Ele é a “base física” das animações no CSS.

> 💡 Pense nele como uma ferramenta que permite mover um objeto em uma folha de papel — para o lado, para cima, girando, ou até parecendo sair do papel (no caso do 3D).
>

<br />

## **1. O que é a propriedade `transform`?**



A propriedade `transform` altera a **forma, a posição e o tamanho** de um elemento na tela. Ela não muda o layout da página — apenas o modo como o elemento é **exibido**.

Podemos usar `transform` para:

- Deslocar um elemento (mover);
- Girar (rotacionar);
- Aumentar ou diminuir o tamanho (escala);
- Inclinar (distorcer).

Essas ações acontecem em um **espaço 2D (duas dimensões)**, mas o CSS também permite criar a ilusão de **profundidade (3D)** com `perspective`.

<br />

> [!IMPORTANT]
>
> Este conteúdo focará em animações 2D.

<br />

## **2. Principais funções do `transform`**



| **Função**                       | **O que faz**                                            | **Exemplo visual**                          |
| -------------------------------- | -------------------------------------------------------- | ------------------------------------------- |
| **`translateX(x)translateY(y)`** | Move o elemento nos eixos horizontal (X) e vertical (Y). | Um quadrado deslizando pela tela.           |
| **`rotate(ângulo)`**             | Gira o elemento em torno de um ponto fixo.               | Um ícone girando.                           |
| **`scale(x, y)`**                | Aumenta ou diminui o tamanho.                            | Um botão que cresce ao passar o mouse.      |
| **`skew(x, y)`**                 | Inclina o elemento em determinado ângulo.                | Um cartão “torto” ou com efeito de balanço. |

<br />

## **3. Movendo um elemento - Horizontal**



O exemplo abaixo mostra como fazer um quadrado se mover da esquerda para a direita usando `translateX()`.

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Movimento com Translate</title>
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f3eefe;
    }

    .box {
      width: 80px;
      height: 80px;
      background-color: #6b36c8;
      border-radius: 12px;
      animation: mover 2s ease-in-out infinite alternate;
    }

    @keyframes mover {
      from {
        transform: translateX(0);
      }
      to {
        transform: translateX(7.5rem);
      }
    }
  </style>
</head>
<body>
  <div class="box"></div>
</body>
</html>
```

👉 O `translateX(7.5rem)` desloca o quadrado **120 pixels para a direita**. Com `alternate`, o movimento se repete em ambos os sentidos — vai e volta.

![](https://i.imgur.com/BmQ3HSU.gif)

<br />

## **4. Movendo um elemento - Vertical**



O exemplo abaixo mostra como fazer um quadrado se mover da base para o topo usando `translateY()`.

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Movimento com Translate</title>
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f3eefe;
    }

    .box {
      width: 80px;
      height: 80px;
      background-color: #6b36c8;
      border-radius: 12px;
      animation: mover 2s ease-in-out infinite alternate;
    }

    @keyframes mover {
      from {
        transform: translateY(0);
      }
      to {
        transform: translateY(-7.5rem);
      }
    }
  </style>
</head>
<body>
  <div class="box"></div>
</body>
</html>
```

👉 O `translateX(-7.5rem)` desloca o quadrado **120 pixels para cima**. Com `alternate`, o movimento se repete em ambos os sentidos — vai e volta.

![](https://i.imgur.com/63xCjtx.gif)

<br />

### **4.1. Por que usamos coordenadas negativas para mover um elemento para o topo?**



O motivo está na **forma como o sistema de coordenadas é definido nos navegadores** — e em praticamente todos os ambientes gráficos 2D —, que é diferente do que estamos acostumados no **plano cartesiano** da matemática.

No plano cartesiano tradicional, a origem (0, 0) fica no **centro** do plano (ou em outro ponto de referência), e:

- o **eixo Y positivo** aponta **para cima**;
- o **eixo Y negativo** aponta **para baixo**.

![](https://imgur.com/5BHyq0L.png)

Já no CSS, o **ponto de origem (0, 0)** fica no **canto superior esquerdo da tela**, como mostrado na imagem abaixo:

![](https://i.imgur.com/sUzKZTd.png)

A partir desse ponto:

- o eixo **X** cresce **da esquerda para a direita**;
- o eixo **Y** cresce **de cima para baixo**.

Em outras palavras:

- `Y positivo` → move o elemento **para baixo**
- `Y negativo` → move o elemento **para cima**

Essa inversão do eixo Y ocorre por um motivo prático: as telas são **renderizadas linha por linha, de cima para baixo**. Assim, o navegador inicia o desenho no topo da tela, no ponto (0, 0), e vai **incrementando o valor de Y** conforme avança no processo de renderização.

Essa convenção torna o desenho e o posicionamento de elementos mais eficientes, pois acompanha o fluxo natural de **leitura e exibição do conteúdo**, que também ocorre **de cima para baixo**.

<br />

## **5. Girando com `rotate`**



Aqui o elemento **gira em torno do próprio centro**, criando o efeito de rotação contínua.

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Rotação</title>
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f5f0ff;
    }

    .roda {
      width: 80px;
      height: 80px;
      background-color: #9a7bd1;
      animation: girar 2s linear infinite;
    }

    @keyframes girar {
      from {
        transform: rotate(0deg);
      }
      to {
        transform: rotate(360deg);
      }
    }
  </style>
</head>
<body>
  <div class="roda"></div>
</body>
</html>
```

👉 `rotate(360deg)` faz o elemento dar uma volta completa. A função `linear` garante uma rotação constante, sem acelerar ou desacelerar.

![](https://i.imgur.com/oWQtb00.gif)

<br />

### **5.1. A Unidade de medida `deg`**



A unidade **`deg`** (de *degrees*, ou **graus**) é usada no CSS para representar **ângulos**. Ela define **quanto algo deve girar ou inclinar** em torno de um ponto — por exemplo, ao usar transformações ou animações.

- **360deg** → uma volta completa
- **180deg** → meia volta
- **90deg** → um quarto de volta
- **90deg** → um quarto de volta no sentido contrário

<br />

## **6. Efeito de “crescimento” com `scale`**



O `scale()` altera o tamanho do elemento, criando um efeito de aumento e redução.

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Efeito de Escala</title>
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f8f5ff;
    }

    .bola {
      width: 80px;
      height: 80px;
      background-color: #6b36c8;
      border-radius: 50%;
      animation: pulsar 1.5s ease-in-out infinite alternate;
    }

    @keyframes pulsar {
      from {
        transform: scale(1);
      }
      to {
        transform: scale(1.3);
      }
    }
  </style>
</head>
<body>
  <div class="bola"></div>
</body>
</html>
```

👉 O elemento aumenta **30%** de tamanho e depois retorna. Esse é o mesmo princípio usado para criar efeitos de “pulso” em botões e ícones.

![](https://i.imgur.com/AiGrE46.gif)

<br />

## **7. Inclinação com `skew`**



A função `skew()` cria uma inclinação, como se o elemento estivesse sendo “esticado” para o lado.

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inclinação</title>
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f5f0ff;
    }

    .card {
      width: 150px;
      height: 100px;
      background-color: #9a7bd1;
      border-radius: 12px;
      animation: inclinar 2s ease-in-out infinite alternate;
    }

    @keyframes inclinar {
      from {
        transform: skew(0deg, 0deg);
      }
      to {
        transform: skew(20deg, 5deg);
      }
    }
  </style>
</head>
<body>
  <div class="card"></div>
</body>
</html>
```

👉 O card é inclinado nos eixos X e Y, criando uma sensação de leve balanço ou torção.

![](https://i.imgur.com/5yYziny.gif)

<br />

<div align="left"><img src="https://i.imgur.com/63FhSvn.png" title="source: imgur.com" width="4%"/> <a href="https://www.w3schools.com/css/css3_2dtransforms.asp" target="_blank"><b>Documentação: Transforms</b></a></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>