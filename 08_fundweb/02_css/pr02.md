<h1>Projeto 01 - Portfólio - Estilos Globais - Parte 01</h1>



Agora que já temos a nossa **Folha de Estilos** criada e inserida nos arquivos html, vamos iniciar a estilização definindo as **configurações globais** do site no arquivo **style.css**.

<br />

<h2>👣 Passo 01 - Resetar as Regras CSS do Navegador</h2>



Quando definimos as **configurações globais** de uma página, o primeiro passo é **resetar os estilos padrão do navegador**. Cada navegador aplica margens, espaçamentos e outros estilos próprios aos elementos HTML, o que pode gerar inconsistências no layout.

Ao realizar esse reset, garantimos que todos os elementos partam de uma **base neutra**, permitindo maior controle sobre a estilização e resultados mais previsíveis. Nesse momento, também é comum definir propriedades globais, como a **fonte padrão**, o **box model** e ajustes iniciais de comportamento visual, criando um ponto de partida consistente para todo o projeto.

<br />

> [!TIP]
>
> De acordo com as boas práticas, as propriedades CSS devem seguir uma ordem lógica para melhor legibilidade e manutenção. A ordem recomendada é:
>
> 1. **Posicionamento** (position, top, right, bottom, left, z-index)
> 2. **Display & Box Model** (display, flex/grid properties, width, height, margin, padding, border)
> 3. **Tipografia** (font, text, line-height, letter-spacing)
> 4. **Visual** (background, color, box-shadow, opacity)
> 5. **Transformações & Animações** (transform, transition, animation)
> 6. **Outros** (cursor, overflow, etc.)

<br />

1. Abra o arquivo **style.css** e adicione o trecho de código abaixo:

<div align="center"><img src="https://imgur.com/HrzqdLo.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 01:** Utilizamos o seletor universal (`*`), que seleciona todos os elementos HTML da página. As propriedades definidas nesse seletor são aplicadas globalmente, garantindo um comportamento visual consistente em todos os elementos.

**Linha 02:** A propriedade `margin: 0;` remove todas as margens padrão aplicadas pelos navegadores, evitando espaçamentos externos indesejados e permitindo um controle total do layout desde o início.

**Linha 03:** A propriedade `padding` define o espaçamento interno de um elemento, ou seja, a distância entre o conteúdo e sua borda. Ao definir `padding: 0;`, eliminamos todos os espaçamentos internos padrão.

**Linha 04:** A propriedade `box-sizing: border-box;` faz com que o `padding` e a `border` sejam incluídos no cálculo da largura e da altura total do elemento. Dessa forma, o tamanho final dos elementos se mantém previsível, facilitando a construção do layout.

**Linha 05:** A propriedade `font-family` define a fonte utilizada nos textos da página. Neste projeto, configuramos a fonte **Poppins**, importada do Google Fonts, como a fonte padrão para todos os elementos.

**Linha 06:** A propriedade `font-weight` controla a espessura dos caracteres do texto. O valor `400` representa o peso normal da fonte, sendo amplamente utilizado para textos de leitura padrão.

**Linha 07:** Por padrão, os links (`<a>`) possuem sublinhado. A propriedade `text-decoration: none;` remove esse sublinhado, permitindo que a aparência dos links seja personalizada conforme o design do projeto.

**Linha 08:** A propriedade `outline` cria um contorno externo ao redor do elemento, geralmente usado para indicar foco. Ao definir `outline: 0;`, esse contorno é removido, evitando que ele apareça visualmente na interface.

<br />

<div align="left"><img src="https://i.imgur.com/63FhSvn.png" title="source: imgur.com" width="4%"/> <a href="https://www.w3schools.com/css/css_outline.asp" target="_blank"><b>Documentação: Outline</b></a></div>

<br />

<h2>👣 Passo 02 - Criar as Variáveis</h2>



Variáveis é um recurso presente em praticamente todas as Linguagens de programação. No CSS elas foram criadas com o objetivo de simplificar o processo de alteração de cores, altura, largura, entre outras propriedades, que utilizam valores iguais de forma rápida, sem a necessidade de modificar os valores de várias propriedades manualmente.

Em nosso projeto, vamos criar algumas variáveis para armazenar as cores da paleta, as sombras, os espaçamentos, os tamanhos da fonte, as bordas arredondadas e a animação padrão. Desta forma, se no futuro desejarmos mudar qualquer um destes parâmetros, basta alterar as respectivas variáveis, que todo o site será modificado automaticamente.

Nas próximas imagens, você confere a Paleta de Cores e as Configurações da Fonte Poppins:

**Paleta Primária - Purple**

<div align="center">
    <img src="https://imgur.com/nfM6U0F.png" title="source: imgur.com"/>
</div>

A escolha da paleta **purple** como identidade principal do site portfólio foi baseada em sua capacidade de transmitir **criatividade, modernidade e sofisticação**, características fortemente associadas à área de tecnologia e desenvolvimento. 

Tons de roxo equilibram bem inovação e profissionalismo, criando um visual marcante sem comprometer a legibilidade ou a experiência do usuário. Além disso, essa paleta oferece excelente contraste quando combinada com cores neutras, facilitando a hierarquia visual, destacando elementos importantes e reforçando a identidade visual do portfólio de forma consistente e elegante.

<br />

**Paleta Secundária - Cores Neutras e Auxiliares**

<div align="center">
    <img src="https://imgur.com/zvl7sux.png" title="source: imgur.com"/>
</div>

A paleta **secundária**, composta por tons de cinza, preto suavizado e branco levemente off-white, foi escolhida para **equilibrar e valorizar** a paleta principal em roxo, sem competir visualmente com ela. 

O uso de cinzas permite criar áreas de respiro, estruturar o layout e garantir boa legibilidade dos textos, enquanto o tom `--black` oferece profundidade e contraste de forma mais confortável do que o preto puro (`#000000`). 

Da mesma forma, o `--white` foi preferido em vez do branco puro para reduzir o brilho excessivo e o cansaço visual. Essa abordagem resulta em uma interface mais agradável, moderna e acessível, mantendo o foco nos elementos principais. 

Já a cor de estado `--error` foi definida com um tom forte e perceptível, garantindo rápida identificação de mensagens de erro ou alerta sem comprometer a harmonia visual do site.

<br />

> [!TIP]
>
> A paleta de cores apresentada foi gerada com o auxílio do site **Coolors**, uma ferramenta on-line amplamente utilizada para criação e harmonização de cores. 
>
> Observe que, na parte superior, foram definidos os **nomes das variáveis**, facilitando a reutilização e a padronização das cores no CSS, enquanto, na parte inferior, estão os **valores em formato hexadecimal**, que representam exatamente cada tonalidade escolhida para o projeto.

<br />

**Fonte Poppins**

<div align="center">
    <img src="https://imgur.com/8nlTDFC.png" title="source: imgur.com"/>
</div>

A fonte **Poppins** foi escolhida por apresentar um visual **moderno, limpo e versátil**, características ideais para um site de portfólio. Sua geometria equilibrada garante excelente **legibilidade em diferentes tamanhos**, funcionando bem tanto em títulos quanto em textos corridos. Além disso, a família Poppins oferece uma ampla variedade de **pesos, estilos e variações**, permitindo criar hierarquia visual de forma consistente e profissional. 

Na imagem acima, é possível visualizar um *overview* da fonte, demonstrando suas variações de peso, estilo e tamanho. Para manter o padrão e facilitar a manutenção do projeto, os **tamanhos da fonte serão definidos por meio de variáveis CSS**, garantindo consistência visual e ajustes centralizados em todo o site.

<br />

Vamos definir as nossas variáveis dentro do arquivo **style.css**:

1. No arquivo **style.css** adicione o trecho de código abaixo:

<div align="center"><img src="https://imgur.com/yZti3Oi.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 11:** Utilizamos o seletor `:root`, que representa o elemento raiz do documento HTML. As variáveis CSS definidas nele ficam disponíveis globalmente, podendo ser reutilizadas em qualquer parte da aplicação.

**Linhas 13 a 22:** Definimos a paleta de cores baseada no tom roxo (*purple*), organizada por níveis de intensidade. Valores menores representam tons mais claros, enquanto valores maiores indicam tons mais escuros. Essa padronização facilita a manutenção visual e garante consistência no design.

**Linhas 25 a 28:** As cores neutras são utilizadas para textos, fundos e elementos de apoio. Elas ajudam a equilibrar o layout e a destacar as cores principais sem poluir visualmente a interface.

**Linha 31:** As cores de estado representam situações específicas da interface, como erros ou alertas. A variável `--error` é utilizada, por exemplo, para mensagens de erro, validações inválidas ou feedbacks negativos ao usuário.

**Linhas 34 a 36:** As variáveis de sombra definem diferentes níveis de profundidade visual. Elas são usadas para destacar elementos como cartões, botões e modais, criando uma hierarquia visual mais clara e moderna.

**Linhas 39 a 44:** Essas variáveis controlam os espaçamentos internos e externos dos elementos. O uso de uma escala padronizada garante alinhamento consistente e facilita ajustes globais no layout.

**Linhas 47 a 52:** Definimos uma escala tipográfica com tamanhos progressivos de fonte. Essa abordagem ajuda a manter a hierarquia visual entre títulos, subtítulos e textos comuns, além de facilitar ajustes de acessibilidade.

**Linhas 55 a 59:** As variáveis de *border-radius* controlam o arredondamento das bordas dos elementos. Valores menores criam cantos sutis, enquanto valores maiores produzem elementos mais arredondados, como botões ou avatares.

**Linha 62:** A variável `--transition-default` define uma transição padrão para animações CSS. Ela é utilizada para suavizar mudanças de estado, como *hover* ou *focus*, proporcionando uma experiência mais fluida ao usuário.

<br />

<h2>👣 Passo 03 - Definir as Configurações Básicas</h2>



Na sequência, vamos definir as **propriedades globais** dos elementos `html`, `body` e `main`, que servirão como **base estrutural e visual** das nossas páginas. Essas definições garantem um comportamento consistente do layout, controlando aspectos como dimensões, fluxo do conteúdo e organização geral, além de facilitar a aplicação dos estilos nas demais seções do site.

1. No arquivo **style.css** adicione o trecho de código abaixo:

<div align="center"><img src="https://imgur.com/5wS4PHH.png" title="source: imgur.com" /></div>

Vamos entender o código:

Segue a explicação do código CSS, mantendo o mesmo padrão de escrita e organização por linhas:

**Linha 65:** Selecionamos o elemento `html`, que representa a raiz da página. 

**Linha 66:** A propriedade `scroll-behavior: smooth;` define um comportamento de rolagem suave, fazendo com que a navegação entre âncoras ou seções da página ocorra de forma animada, em vez de instantânea.

**Linha 69:** Selecionamos o elemento `body`, responsável por envolver todo o conteúdo visível da página.

**Linha 70:** A propriedade `display: flex;` transforma o `body` em um contêiner flexível, permitindo organizar seus elementos filhos de forma mais controlada.

**Linha 71:** A propriedade `flex-direction: column;` define que os elementos filhos do `body` serão organizados em coluna, ou seja, um abaixo do outro, seguindo o fluxo vertical da página.

**Linha 72:** A propriedade `min-height: 100vh;` garante que o `body` tenha, no mínimo, a altura total da janela do navegador, mesmo que o conteúdo seja pequeno.

**Linha 73:** A propriedade `background-color` define a cor de fundo da página. Neste caso, utilizamos a variável CSS `--white`, garantindo consistência com a paleta de cores do projeto.

**Linha 74:** A propriedade `line-height: 1.6;` controla o espaçamento entre as linhas de texto, melhorando a legibilidade do conteúdo.

**Linha 75:** A propriedade `color` define a cor padrão do texto da página. Aqui, utilizamos a variável `--black`, mantendo o padrão visual estabelecido.

**Linha 78:** Selecionamos o elemento `main`, que representa o conteúdo principal da página.

**Linha 79:** A propriedade `flex: 1;` permite que o elemento `main` ocupe todo o espaço disponível dentro do contêiner flex (`body`). Isso é especialmente útil para manter o rodapé fixado ao final da página, mesmo quando o conteúdo principal é pequeno.

<br />

<div align="left"><img src="https://i.imgur.com/63FhSvn.png" title="source: imgur.com" width="4%"/> <a href="https://www.w3schools.com/cssref/pr_scroll-behavior.php" target="_blank"><b>Documentação: Scroll Behavior</b></a></div>

<br />

## 👣 Passo 04 - Visualizar a Página index no Navegador



1. Para visualizar a Página **index.html** no Navegador, na **Barra Explorer**, clique com o botão direito do mouse sobre a página **index.html** e no menu que será aberto, clique na opção **Open with Live Server**:

<div align="center"><img src="https://i.imgur.com/GEh806t.png" title="source: imgur.com" /></div>

<br />

> [!WARNING]
>
> A opção **Open with Live Server** estará disponível apenas se a **extensão Live Server** estiver instalada no **Visual Studio Code**.

<br />

2. A Página será aberta no Navegador WEB padrão da sua Máquina:

<div align="center"><img src="https://imgur.com/gQ9xEdD.png" title="source: imgur.com" /></div>

Observe que, após a aplicação das propriedades globais: 

- **As margens foram removidas**, garantindo um alinhamento mais preciso do layout

- A **fonte padrão foi redefinida**, assegurando consistência tipográfica em toda a página. 

- Os **marcadores das listas foram ocultados, devido à margem zerada**, permanecendo fora da área visível do layout. 

- O **espaçamento entre as linhas foi ajustado** para melhorar a legibilidade

- Os **links perderam o sublinhado padrão**, permitindo maior controle visual via CSS

- A combinação de **texto escuro** com **fundo claro em tons suaves** proporciona conforto visual e evita o contraste excessivo do preto e branco puro. 

Essas configurações criam uma base limpa, uniforme e previsível, facilitando a aplicação dos estilos personalizados ao longo de todo o layout.

<br />

> [!WARNING]
>
> Para que todos os elementos ficassem visíveis na imagem acima, o tamanho das imagens foram ajustados. Ao abrir o arquivo `index.html` no navegador, as imagens serão exibidas em seu tamanho original, ocupando toda a largura da página.

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>
