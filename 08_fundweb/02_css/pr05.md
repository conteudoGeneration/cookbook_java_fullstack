<h1>Projeto 01 - Portfólio - Estilização da Página index - Seção Hero</h1>



Depois de estilizar a **Seção Header**, vamos avançar para a **Seção Hero**, responsável por causar a **primeira impressão** ao usuário ao acessar a página. Essa área normalmente concentra o **conteúdo de maior destaque**, como o **título principal**, um **texto curto de apresentação**, chamadas para ação (*call to action*) e, em alguns casos, elementos visuais que reforçam a identidade do site.

Uma boa estilização da seção Hero é fundamental para estabelecer **hierarquia visual**, direcionar a atenção do usuário e comunicar rapidamente a proposta do site. Além disso, essa seção contribui diretamente para o **engajamento inicial**, guiando o visitante para as ações ou informações mais importantes da aplicação.

<br />

<h2>👣 Passo 01 - Estilizar a Seção Hero</h2>



1. Abra o arquivo **style.css** e adicione os trechos de código abaixo:

<br />

### ✅ Seção Hero (Container)

<div align="center"><img src="https://i.imgur.com/6HVX7W0.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 191:** Selecionamos a classe `.hero-container`, responsável pela estrutura principal da seção *hero* da página.

**Linha 192:** A propriedade `display: flex;` cria um layout flexível, permitindo alinhar o conteúdo textual e a imagem lado a lado.

**Linha 193:** A propriedade `align-items: center;` centraliza verticalmente os elementos dentro do contêiner.

**Linha 194:** A propriedade `gap` define o espaçamento entre os elementos internos, utilizando a variável `--spacing-2xl`.

**Linha 195:** A propriedade `min-height: calc(100vh - 5rem);` garante que a seção *hero* ocupe quase toda a altura da janela do navegador, descontando a altura do menu fixo.

<br />

### ✅ Painel Esquerdo (Texto)

<div align="center"><img src="https://i.imgur.com/6L1WZ2y.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 198:** Selecionamos a classe `.hero-content`, que representa a área de conteúdo textual da seção.

**Linha 199:** A propriedade `display: flex;` transforma o contêiner em flexível.

**Linha 200:** A propriedade `flex: 1;` permite que o conteúdo ocupe metade do espaço disponível no contêiner principal.

**Linha 201:** A propriedade `flex-direction: column;` organiza os elementos internos em coluna.

**Linha 202:** A propriedade `gap` define o espaçamento vertical entre os textos.

**Linha 205:** Selecionamos o elemento `<span>` dentro de `.hero-content`.

**Linha 206:** A propriedade `font-size` define um tamanho de fonte maior, usado como texto de destaque.

**Linha 207:** A propriedade `font-weight: 700;` aplica um peso forte, reforçando a ênfase visual.

**Linha 210:** Selecionamos o título `<h1>` dentro de `.hero-content`.

**Linha 211:** A propriedade `font-size` define um tamanho de fonte ainda maior, destacando o título principal da seção.

**Linha 212:** A propriedade `font-weight: 700;` reforça a hierarquia visual do título.

**Linhas 215 a 217:** Selecionamos os parágrafos das seções *hero*, *about* e *contact*. Como essas seções compartilham a mesma estilização de texto, os seletores foram agrupados em uma única regra, evitando repetição de código e facilitando a manutenção do estilo.

**Linha 218:** A propriedade `font-size` define um tamanho de texto confortável para leitura.

**Linha 219:** A propriedade `color` aplica um tom de cinza, suavizando o texto e melhorando a hierarquia visual.

<br />

### ✅ Painel Direito (Imagem)

<div align="center"><img src="https://i.imgur.com/P3wV4e1.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 222:** Selecionamos a classe `.hero-image`, responsável pelo contêiner da imagem.

**Linha 223:** A propriedade `display: flex;` cria um contêiner flexível.

**Linha 224:** A propriedade `flex: 1;` permite que a imagem ocupe a outra metade do espaço disponível.

**Linha 225:** A propriedade `justify-content: center;` centraliza horizontalmente a imagem.

**Linha 228:** Selecionamos a imagem (`<img>`) dentro de `.hero-image`.

**Linha 229:** A propriedade `width: 100%;` faz com que a imagem ocupe toda a largura disponível do contêiner.

**Linha 230:** A propriedade `max-width: 100%;` impede que a imagem ultrapasse o tamanho do contêiner.

**Linha 231:** A propriedade `border-radius` aplica cantos arredondados à imagem, utilizando o raio definido no projeto.

<br />

### ✅ Animação da Foto

<div align="center"><img src="https://i.imgur.com/e44ADPo.png" title="source: imgur.com" /></div>

Vamos entender o código:

Segue a explicação do código CSS, mantendo o padrão atual de escrita e a organização por linhas:

**Linha 235:** Utilizamos a regra `@keyframes float` para criar uma animação personalizada chamada `float`.

**Linhas 236 a 239:** No início (`0%`) e no final (`100%`) da animação, o elemento permanece em sua posição original, sem deslocamento vertical.

**Linhas 241 a 243:** No ponto intermediário da animação (`50%`), o elemento é deslocado levemente para cima utilizando `transform: translateY(-1.25rem);`, criando o efeito de flutuação.

**Linha 246:** Selecionamos a classe `.float-animation`, que será aplicada aos elementos que devem receber a animação.

**Linha 247:** A propriedade `animation` associa a animação `float` ao elemento, com duração de 3 segundos, transição suave (`ease-in-out`) e repetição contínua (`infinite`), resultando em um movimento contínuo e suave.

<br />

<h2>👣 Passo 02 - Visualizar a Página no Navegador</h2>



1. Abra a página index.html no Navegador, através do **Live Server**:

<div align="center"><img src="https://i.imgur.com/T7qTC1j.png" title="source: imgur.com" /></div>

Após a aplicação das propriedades no código acima, a **seção Hero** recebeu ajustes estruturais e visuais que reforçam o destaque inicial da página e a organização do conteúdo:

- O contêiner `.hero-container` passou a utilizar **Flexbox**, alinhando os elementos horizontalmente e centralizando-os verticalmente, com espaçamento amplo entre conteúdo e imagem.
- Foi definida uma **altura mínima proporcional à viewport** (`min-height: calc(100vh - 5rem)`), garantindo que a seção Hero ocupe a maior parte da tela inicial, sem sobrepor o header.
- O bloco `.hero-content` organiza os textos em **coluna**, com espaçamentos consistentes, facilitando a leitura e reforçando a hierarquia das informações.
- Elementos de destaque, como o texto em `<span>` e o `<h1>`, receberam **tamanhos e pesos de fonte maiores**, direcionando a atenção do usuário para a mensagem principal.
- Os parágrafos utilizam **tipografia secundária e cor neutra**, criando contraste suave e melhorando o conforto visual.
- A área `.hero-image` também utiliza **Flexbox** para centralizar a imagem, mantendo equilíbrio visual entre texto e elemento gráfico.
- A imagem foi configurada como **fluida e responsiva**, ocupando toda a largura disponível e respeitando bordas arredondadas para um acabamento mais moderno. As bordas arredondadas estarão visíveis quando você substituir a imagem temporária pela sua foto personalizada. Além disso, foi adicionada uma animação flutuante, para destacar a foto.

Essas definições tornam a seção Hero mais **impactante, legível e responsiva**, fortalecendo a comunicação inicial do site e proporcionando uma experiência visual clara e bem estruturada.

2. Para visualizar os posicionamento dos elementos HTML no Navegador, ative a extensão **Pesticide**, clicando no botão <img src="https://i.imgur.com/3E84PHm.png" title="source: imgur.com" width="4%"/> em **Extensões**:

<div align="center"><img src="https://i.imgur.com/yc7k4Z4.png" title="source: imgur.com" /></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>
