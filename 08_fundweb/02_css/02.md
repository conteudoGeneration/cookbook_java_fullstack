<h1>Seletores</h1>



Os **seletores CSS** são essenciais para aplicar estilos aos elementos da sua página. Eles funcionam como uma “ponte” que conecta as regras de estilo aos elementos HTML, indicando exatamente quais devem ser afetados.

<br />

<h2>1. Seletores básicos</h2>



<h3>1.1. Seletor do elemento (tag)</h3>



Imagine que você tem um parágrafo (`<p>`) em sua página e quer que o texto dele fique azul. Para isso, usamos o seletor que corresponde à tag do elemento:

<img src="https://i.imgur.com/O5NjoiA.png" title="source: imgur.com" width="5%"/>**Código HTML - Exemplo:**

```html
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link rel="stylesheet" href="./assets/css/style.css">
    
    <title>Seletores CSS - Tag</title>
</head>
<body>
    <p>
  		Texto do parágrafo
	</p>
</body>
</html>
```

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="5%"/>**CSS - Exemplo:**

```css
p {
	color: blue;
}
```

<img src="https://i.imgur.com/3HPZ0By.png" title="source: imgur.com" width="5%"/>**Resultado:**

<div align="center"><img src="https://i.imgur.com/ROhOYJL.png" title="source: imgur.com" /></div>

O código CSS acima, vai aplicar a cor azul a **todos** os parágrafos (`<p>`) da página.

Comovemos na imagem acima, o navegador vai exibir o texto do parágrafo em azul. Se você adicionar outros parágrafos eles também serão exibidos na cor azul.

Cada tag HTML possui o seu próprio seletor: `div`, `span`, `h1`, `img` e outros. Quando você usa um seletor de elemento, todos os elementos daquele tipo são estilizados da mesma forma.

<br />

<h3>1.2. Seletores de Id e Classe</h3>



Além das tags, os elementos HTML podem ter atributos especiais para identificação: o `id` e a `class`. Eles são muito usados para aplicar estilos específicos.

- **ID**: identifica um elemento único na página. No CSS, o seletor de ID começa com `#`.
- **Classe**: pode ser compartilhada por vários elementos. No CSS, a classe é representada com um ponto (`.`).

<br />

<img src="https://i.imgur.com/O5NjoiA.png" title="source: imgur.com" width="5%"/>**Código HTML - Exemplo:**

```html
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link rel="stylesheet" href="./assets/css/style.css">
    
    <title>Seletores CSS - ID e Classe</title>
</head>
<body>
    <h1 id="titulo">
  		Texto do título
	</h1>

    <p class="texto">
      	Texto do parágrafo
    </p>
</body>
</html>
```

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="5%"/>**CSS - Exemplo:**

```css
#titulo {
	color: red;
}

.texto {
	color: green;
}
```

<img src="https://i.imgur.com/3HPZ0By.png" title="source: imgur.com" width="5%"/>**Resultado:**

<div align="center"><img src="https://i.imgur.com/WNiDCXf.png" title="source: imgur.com" /></div>

Como vemos no exemplo acima, o título foi exibido na cor vermelha e todos os elementos com a classe `texto` foram exibidos va cor verde.

👉**Lembre-se que:**

- Um `id` deve ser único na página (não use o mesmo ID para vários elementos).
- Uma mesma `class` pode ser usada em vários elementos.
- Um elemento pode ter várias classes ao mesmo tempo, mas apenas um ID.

<br />

<h2>2. Seletor Universal</h2>



O seletor universal `*` seleciona **todos** os elementos da página.

Ele é muito útil para definir configurações básicas que “zeram” os estilos padrão do navegador, para garantir que seu CSS comece do zero e seja consistente em todos os navegadores.

<br />

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="5%"/>**CSS - Exemplo:**

```css
* {
    margin: 0;
    padding: 0;
	box-sizing: border-box;
}
```

Neste código, todas as margens e espaçamentos são zerados, e o `box-sizing: border-box` garante que o tamanho dos elementos seja calculado de forma intuitiva, considerando bordas e preenchimentos.

Essa configuração é conhecida como **Reset do Navegador** e serve para garantir que nenhuma configuração padrão de margem e espaçamentos interfira na estilização da página, ou seja, tudo começará do zero.

<br />

<h2>3. Seletor Raiz</h2>



O seletor `:root` representa o **elemento raiz do documento HTML**, que normalmente é a tag `<html>`.

Ele é muito utilizado para definir **variáveis CSS globais**, que podem ser reutilizadas em toda a aplicação.

As variáveis definidas dentro dele ficam disponíveis para todos os elementos da página, facilitando a padronização de cores, espaçamentos, fontes e outros valores.

<br />

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="5%"/>**CSS - Exemplo:**

```css
:root {
    --cor-primaria: #6b36c8;
    --cor-secundaria: #f4eaff;
    --espacamento-padrao: 16px;
}
```

Essas variáveis podem ser utilizadas em qualquer parte do CSS usando a função `var()`:

```css
body {
    background-color: var(--cor-secundaria);
    color: var(--cor-primaria);
}
```

<br />

**Vantagens do uso do seletor `:root`**

- Centraliza configurações importantes do projeto
- Facilita mudanças de tema e identidade visual
- Evita repetição de valores no código
- Torna o CSS mais organizado e escalável

O uso do `:root` é uma **boa prática moderna** e muito comum em projetos profissionais.

<br />

<h2>4. Agrupamento de seletores CSS</h2>



Quando vários elementos precisam ter o mesmo estilo, você pode agrupá-los em uma única regra CSS, separando os seletores por vírgula.

<br />

<img src="https://i.imgur.com/O5NjoiA.png" title="source: imgur.com" width="5%"/>**Código HTML - Exemplo:**

```html
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link rel="stylesheet" href="./assets/css/style.css">
    
    <title>Seletores CSS - Agrupamento</title>
</head>
<body>
    <h1>
  		Texto do título
	</h1>
	<h2>
  		Texto do subtítulo
	</h2>
    <p>
      	Texto do parágrafo
    </p>
</body>
</html>
```

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="5%"/>**CSS - Exemplo:**

```css
h1, h2, p {
  	text-align: center;
	color: green;
}
```

<img src="https://i.imgur.com/3HPZ0By.png" title="source: imgur.com" width="5%"/>**Resultado:**

<div align="center"><img src="https://i.imgur.com/tfb6TdT.png" title="source: imgur.com" /></div>

No exemplo acima, os títulos (`h1` e `h2`) e os parágrafos (`p`) terão o texto centralizado e na cor verde.

<br />

<h2>5. Seletores Combinados</h2>



Os **seletores combinados** permitem selecionar elementos com base na **relação que eles possuem entre si** dentro da estrutura HTML. Eles são muito úteis para estilizar elementos conforme o contexto em que aparecem.

<br />

<h3>5.1. Seletores Descendentes</h3>



Os **seletores descendentes** permitem aplicar estilos a elementos com base na **estrutura hierárquica do HTML**, considerando a relação entre um elemento **pai** e seus **descendentes**.

Esse tipo de seletor é amplamente utilizado em projetos reais, pois possibilita **escopar estilos por seção**, tornando o CSS mais específico e organizado, sem a necessidade de criar diversas classes adicionais no HTML.

O navegador interpreta o seletor **da esquerda para a direita**, aplicando as regras apenas aos elementos que estejam **dentro do contexto definido**, respeitando a hierarquia indicada no seletor.

<br />

<img src="https://i.imgur.com/O5NjoiA.png" title="source: imgur.com" width="5%"/>**Código HTML - Exemplo:**

```html
<div class="hero-content">
    <h1>Título Principal</h1>
    <p>Texto de apresentação</p>
</div>
```

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="5%"/>**CSS - Exemplo:**

```css
.hero-content h1 {
    font-size: 2rem;
    color: purple;
}
```

Nesse exemplo, apenas o `<h1>` que estiver **dentro de um elemento com a classe `hero-content`** receberá o estilo. Outros `<h1>` fora desse contexto não serão afetados.

<br />

Também é possível criar seletores ainda mais específicos, combinando vários níveis:

```css
.about-buttons-data .botao {
    margin-right: auto;
}
```

Nesse caso, o estilo será aplicado somente aos elementos com a classe `.botao` que estejam **dentro de um elemento com a classe `.about-buttons-data`**.

<br />

**Quando usar seletores descendentes?**

- Para estilizar elementos apenas dentro de uma seção específica
- Para evitar conflitos entre estilos semelhantes
- Para manter o HTML mais limpo, com menos classes

Esse tipo de seletor melhora a organização do CSS e torna o código mais legível e previsível.

<br />

<h3>5.2. Seletores de filho direto</h3>



Os **seletores de filho direto** permitem aplicar estilos **apenas aos elementos que são filhos imediatos** de um determinado elemento pai, ignorando níveis mais profundos da hierarquia.

Esse tipo de seletor é útil quando se deseja **maior controle e precisão**, garantindo que o estilo seja aplicado somente à estrutura esperada, evitando efeitos colaterais em elementos aninhados.

O navegador aplica o estilo somente quando a relação entre os elementos corresponde **exatamente** ao seletor definido, utilizando o operador `>` para indicar o vínculo direto entre pai e filho.

<br />

<img src="https://i.imgur.com/O5NjoiA.png" title="source: imgur.com" width="5%"/>**Código HTML - Exemplo:**

```html
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <link rel="stylesheet" href="./assets/css/style.css" />
    
    <title>Seletores Descendente e Filho Direto</title>
</head>
<body>
	<section class="container">
		<h2>Título da Seção</h2>
		<p>Parágrafo filho direto da section.</p>

			<div>
				<p>Parágrafo dentro da div (não é filho direto da section).</p>
			</div>
	</section>
</body>
</html>

```

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="5%"/>**CSS - Exemplo:**

```css
.container > p {
    color: purple;
    font-weight: bold;
}
```

<img src="https://i.imgur.com/3HPZ0By.png" title="source: imgur.com" width="5%"/>**Resultado:**

<div align="center"><img src="https://i.imgur.com/p54U959.png" title="source: imgur.com" /></div>

Neste exemplo, o seletor `.container > p` aplica o estilo **somente** ao `<p>` que é **filho direto** do elemento com a classe `container`.

O parágrafo que está **dentro da `<div>`** não recebe o estilo, pois ele não é um filho direto da `<section>`, e sim um **descendente em outro nível**.

Esse tipo de seletor é ideal quando você precisa garantir que o estilo seja aplicado apenas a uma estrutura específica do HTML, evitando efeitos indesejados em elementos aninhados.

<br />

<h2>6. Seletores de Atributos</h2>



O seletor de atributo permite aplicar estilos a elementos que possuem um atributo específico, ou que tenham um atributo com um valor determinado.

Por exemplo, para estilizar todos os campos de formulário do tipo texto, podemos usar:

<br />

<img src="https://i.imgur.com/O5NjoiA.png" title="source: imgur.com" width="5%"/>**Código HTML - Exemplo:**

```html
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <link rel="stylesheet" href="./assets/css/style.css" />
    
    <title>Seletor de Atributo</title>
</head>
<body>
    <form>
        <input type="text" placeholder="Texto aqui" />
        <input type="password" placeholder="Senha" />
        <input type="email" placeholder="Email" />
    </form>
</body>
</html>
```

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="5%"/>**CSS - Exemplo:**

```css
input[type="text"] {
  border: 1px solid #ccc;
  padding: 5px;
}
```

<img src="https://i.imgur.com/3HPZ0By.png" title="source: imgur.com" width="5%"/>**Resultado:**

<div align="center"><img src="https://i.imgur.com/WDzVysY.png" title="source: imgur.com" /></div>

Nesse caso, o seletor seleciona todos os elementos `<input>` cujo atributo `type` seja exatamente `"text"` e aplica uma borda sólida cinza com 1px de espessura, além do espaçamento interno de 5px.

<br />

<h2>7. Pseudo-classes</h2>



As **pseudo-classes** permitem aplicar estilos a elementos HTML com base em **estados específicos** ou **condições temporárias**, como a interação do usuário ou a posição do elemento dentro da estrutura do documento.

Elas são amplamente utilizadas para melhorar a **experiência do usuário (UX)**, tornando a interface mais interativa, intuitiva e responsiva a ações como passar o mouse, clicar ou navegar por formulários.

Alguns exemplos comuns de pseudo-classes são:

- `:hover` → quando o usuário passa o mouse sobre um elemento
- `:active` → quando o elemento está sendo clicado
- `:focus` → quando o elemento recebe foco (teclado ou mouse)
- `:first-child` → quando o elemento é o primeiro filho de seu contêiner

<br />

<img src="https://i.imgur.com/O5NjoiA.png" title="source: imgur.com" width="5%"/>**Código HTML - Exemplo:**

```html
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <link rel="stylesheet" href="./assets/css/style.css" />
    
    <title>Pseudo-classes Básicas</title>
</head>
<body>
    <a href="#">Passe o mouse aqui</a>
</body>
</html>
```

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="5%"/>**CSS - Exemplo:**

```css
a:hover {
  color: red;
  text-decoration: underline;
}
```

<img src="https://i.imgur.com/3HPZ0By.png" title="source: imgur.com" width="5%"/>**Resultado:**

<div align="center"><img src="https://i.imgur.com/ld9Pm5H.png" title="source: imgur.com" /></div>

Nesse exemplo, a pseudo-classe `:hover` aplica estilos ao link **somente quando o usuário passa o mouse sobre ele**, alterando a cor do texto e adicionando um sublinhado. Esse tipo de feedback visual é essencial para indicar que o elemento é interativo.

As pseudo-classes são um recurso poderoso do CSS e serão exploradas com mais profundidade em um **capítulo dedicado exclusivamente a esse tema**, onde veremos seus principais tipos, casos de uso reais e boas práticas de aplicação.

<br />

<h2>8. Pseudo-elementos</h2>



Os **pseudo-elementos** permitem criar **partes virtuais** dentro, antes ou depois do conteúdo real de um elemento HTML, sem a necessidade de alterar o código HTML original. Eles são muito utilizados para adicionar **detalhes visuais**, **ícones**, **efeitos decorativos** ou pequenos conteúdos auxiliares apenas com CSS.

Diferente das pseudo-classes, que atuam sobre **estados**, os pseudo-elementos atuam sobre **partes específicas** de um elemento.

Os pseudo-elementos mais utilizados são:

- **`::before`** → insere conteúdo **antes** do conteúdo real do elemento
- **`::after`** → insere conteúdo **depois** do conteúdo real do elemento

<br />

> [!WARNING]
>
> Para que funcionem corretamente, os pseudo-elementos **precisam da propriedade `content`**, mesmo que ela esteja vazia.

<br />

<img src="https://i.imgur.com/O5NjoiA.png" title="source: imgur.com" width="5%"/>**Código HTML - Exemplo:**

```html
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <link rel="stylesheet" href="./assets/css/style.css" />
    
    <title>Pseudo-elementos</title>
</head>
<body>
    <p>Este parágrafo tem estrelas antes e depois.</p>
    <p>Veja como fica o efeito.</p>
</body>
</html>
```

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="5%"/>**CSS - Exemplo:**

```css
p::before {
  content: "★ ";
  color: gold;
}

p::after {
  content: " ★";
  color: gold;
}
```

<img src="https://i.imgur.com/3HPZ0By.png" title="source: imgur.com" width="5%"/>**Resultado:**

<div align="center"><img src="https://i.imgur.com/zJC5lcm.png" title="source: imgur.com" /></div>

Nesse exemplo, todas as tags `<p>` recebem uma **estrela dourada antes e depois do texto**, criada exclusivamente com CSS. Nenhum caractere foi adicionado diretamente ao HTML, o que mantém o código **limpo, semântico e fácil de manter**.

Os pseudo-elementos são amplamente utilizados em projetos reais para criar **efeitos visuais**, **separadores**, **ícones decorativos** e **detalhes de interface**, e também serão explorados com mais profundidade em um **capítulo dedicado**, abordando boas práticas, limitações e casos de uso comuns.

<br />

## 9. Hierarquia e Herança no CSS



O CSS funciona com base em dois conceitos fundamentais: **hierarquia (prioridade das regras)** e **herança**. Entender como esses mecanismos operam é essencial para prever qual estilo será aplicado a um elemento e para evitar conflitos no código.

Vamos entender **como o navegador decide qual regra CSS prevalece** e **como certos estilos são herdados pelos elementos filhos**.

<br />

### 9.1. Hierarquia das Regras CSS (Prioridade)



Quando várias regras CSS afetam o mesmo elemento, o navegador utiliza a **cascata** para definir qual delas será aplicada. Essa decisão não depende de um único fator, mas da combinação de vários critérios.

<br />

#### Ordem de prioridade das regras

| Prioridade | Origem / Regra                        | Descrição                                                    |
| ---------- | ------------------------------------- | ------------------------------------------------------------ |
| 1 (menor)  | **Estilos do navegador (User Agent)** | Estilos padrão aplicados automaticamente pelo navegador      |
| 2          | **CSS externo**                       | Arquivos `.css` vinculados com `<link>`                      |
| 3          | **CSS interno**                       | Estilos definidos dentro da tag `<style>`                    |
| 4          | **CSS inline**                        | Estilos aplicados diretamente no atributo `style` do elemento |
| 5          | **Especificidade do seletor**         | Seletores mais específicos têm prioridade sobre os genéricos |
| 6          | **Ordem da cascata**                  | A última regra válida sobrescreve as anteriores              |
| 7 (maior)  | **Regras com `!important`**           | Forçam prioridade máxima, ignorando a cascata e a especificidade |

<br />

Na prática, a **cascata do CSS** avalia três fatores **em conjunto**, nunca isoladamente:

1. **Origem da regra** (externo, interno ou inline)
2. **Especificidade do seletor**
3. **Ordem em que a regra aparece no código**

Ou seja, uma regra pode sobrescrever outra mesmo estando em um arquivo externo, desde que seja mais específica ou declarada depois.

<br />

### 9.2. Especificidade dos Seletores CSS



A **especificidade** define o “peso” de um seletor. Quanto maior esse peso, maior a chance da regra ser aplicada.

| Prioridade | Tipo de seletor           | Exemplo                 | Descrição                                  |
| ---------- | ------------------------- | ----------------------- | ------------------------------------------ |
| 1 (menor)  | **Seletor universal**     | `*`                     | Aplica estilo a todos os elementos         |
| 2          | **Seletor de tipo (tag)** | `p`, `div`, `h1`        | Seleciona elementos pelo nome da tag       |
| 3          | **Pseudo-elementos**      | `::before`, `::after`   | Estilizam partes específicas do elemento   |
| 4          | **Classes**               | `.card`, `.btn`         | Selecionam elementos por classe            |
| 5          | **Atributos**             | `input[type="text"]`    | Selecionam elementos com base em atributos |
| 6          | **Pseudo-classes**        | `:hover`, `:focus`      | Estilos baseados em estado ou posição      |
| 7          | **Seletores combinados**  | `.menu .item`           | Aumentam o peso ao combinar seletores      |
| 8          | **ID**                    | `#header`               | Seleciona um elemento único                |
| 9          | **Estilo inline**         | `style="color: red"`    | Estilo aplicado diretamente no HTML        |
| 10 (maior) | **`!important`**          | `color: red !important` | Força prioridade máxima                    |

**Observações importantes:**

- **Classes, pseudo-classes e seletores de atributos possuem o mesmo peso**
- **Pseudo-elementos têm o mesmo peso que seletores de tipo**
- A **especificidade é cumulativa**
- Em caso de empate, **vence a última regra declarada**

<br />

### 9.3. Herança no CSS



A **herança** permite que certos estilos definidos em um elemento pai sejam automaticamente aplicados aos seus elementos filhos.

#### Propriedades que normalmente são herdadas

- `color`
- `font-family`
- `font-size`
- `line-height`
- `text-align`

#### Propriedades que **não são herdadas por padrão**

- `margin`
- `padding`
- `border`
- `width`
- `height`
- `background`

<br />

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="5%"/>**CSS - Exemplo:**

```css
body {
  font-family: Arial, sans-serif;
  color: #333;
  padding: 2px;
}
```

Neste exemplo, as propriedades **`font-family`** e **`color`** serão **herdadas por todos os elementos filhos** do `<body>`, garantindo consistência tipográfica e de cor em toda a página.

Já a propriedade **`padding` não é herdada**. Isso significa que os containers internos (como `div`, `section` ou `article`) **não receberão automaticamente esse espaçamento**, pois propriedades de layout e espaçamento precisam ser aplicadas diretamente a cada elemento quando necessário.

<br />

<h2>10. O Modificador !important</h2>



O **`!important`** é um modificador do CSS que força uma regra a ter **prioridade máxima**, sobrescrevendo outras regras, independentemente da especificidade ou da ordem em que aparecem no arquivo.

Embora seja um recurso válido da linguagem, o seu uso **excessivo ou inadequado** pode tornar o código difícil de manter, depurar e evoluir. Por isso, deve ser utilizado com **cautela e apenas quando realmente necessário**.

Quando uma propriedade recebe `!important`, ela **ignora a hierarquia normal do CSS**, incluindo:

- Ordem de carregamento
- Especificidade de seletores
- Regras herdadas

<br />

<img src="https://i.imgur.com/O5NjoiA.png" title="source: imgur.com" width="5%"/>**Código HTML - Exemplo:**

```html
<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <link rel="stylesheet" href="style.css" />
  <title>Uso do !important</title>
</head>
<body>
  <p>Mensagem.</p>
  <p class="alerta">Atenção: mensagem importante.</p>
</body>
</html>
```

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="5%"/>**CSS - Exemplo:**

```css
p {
  color: green;
}

.alerta {
  color: red !important;
}
```

<img src="https://i.imgur.com/3HPZ0By.png" title="source: imgur.com" width="5%"/>**Resultado:**

<div align="center"><img src="https://i.imgur.com/Sya6LOK.png" title="source: imgur.com" /></div>



O texto do segundo parágrafo será exibido na cor **vermelha**, mesmo existindo outra regra para todas as tags `<p>`.

<br />

<div align="left"><img src="https://i.imgur.com/63FhSvn.png" title="source: imgur.com" width="5%"/> <a href="https://www.w3schools.com/css/css_selectors.asp" target="_blank"><b>Documentação do CSS - Seletores</b></a></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>