<h1>Unidades de Medida CSS</h1>



Ao estilizar uma página web, definir tamanhos corretamente é fundamental para garantir uma boa experiência visual. No CSS, utilizamos **unidades de medida** para controlar o tamanho de fontes, margens, espaçamentos, larguras e alturas dos elementos.

Compreender as diferenças entre essas unidades e saber quando utilizar cada uma é essencial para criar layouts que funcionem bem em diferentes dispositivos e tamanhos de tela, especialmente em projetos responsivos.

Neste conteúdo, vamos conhecer as principais unidades de medida do CSS — desde as mais tradicionais até as mais modernas — e entender como elas influenciam na adaptação do design a diferentes contextos e dispositivos.

A unidade de medida mais tradicional e amplamente utilizada no CSS é o **pixel (`px`)**. Durante muitos anos, o `px` foi a escolha padrão para definir tamanhos de fontes, espaçamentos e outros elementos visuais, pois oferece precisão e previsibilidade.

No entanto, com o avanço dos dispositivos — como smartphones, tablets e telas com diferentes resoluções e densidades de pixels — tornou-se necessário adotar unidades mais **flexíveis**, capazes de se adaptar melhor a variados tamanhos de tela.

Para atender a essa necessidade de **responsividade**, surgiu a unidade **`em`**, que funciona de forma relativa ao tamanho da fonte do elemento pai. Isso permite que os elementos sejam dimensionados de forma proporcional dentro da hierarquia do documento. Com a evolução do CSS, outras unidades modernas foram introduzidas, facilitando a criação de layouts mais fluidos, adaptáveis e acessíveis.

> **Web design responsivo** é a técnica de desenvolver páginas que se ajustam automaticamente a diferentes tamanhos de tela e dispositivos, garantindo boa usabilidade e aparência consistente. Isso é alcançado principalmente pelo uso inteligente de CSS, especialmente com unidades relativas e *media queries*.

Agora, vamos conhecer as principais unidades de medida utilizadas atualmente no CSS:

- **`px` (pixel)**: unidade fixa que representa um ponto na tela. É útil quando se precisa de precisão, mas não se adapta bem a diferentes tamanhos de tela.
- **`em`**: unidade relativa ao tamanho da fonte do elemento pai. É indicada para criar escalas proporcionais dentro de componentes.
- **`rem`** (*root em*): unidade relativa ao tamanho da fonte do elemento raiz (`<html>`). Ajuda a manter consistência em todo o layout.
- **Percentual (`%`)**: representa uma porcentagem em relação ao elemento pai. É muito utilizada para larguras, alturas e layouts flexíveis.
- **`vh` (viewport height\)**: corresponde a uma porcentagem da altura da área visível do navegador.
- **`vw` (viewport width)**: corresponde a uma porcentagem da largura da área visível do navegador.
- **`vmin`**: utiliza como referência o menor valor entre a largura e a altura do viewport.
- **`vmax`**: utiliza como referência o maior valor entre a largura e a altura do viewport.

Essas unidades permitem que o layout se adapte melhor a diferentes tamanhos de tela, facilitando a criação de interfaces responsivas, flexíveis e mais acessíveis para o usuário final.

<br />

<h2>1. Unidade px</h2>



A unidade **`px`** (*pixel*) é a mais básica e tradicionalmente utilizada no CSS para definir tamanhos. Diferentemente das unidades relativas, como `em` e `rem`, o `px` **não depende do tamanho da fonte do elemento pai** e representa uma medida fixa no contexto do layout.

Apesar de ser tratada como uma unidade fixa no CSS, é importante compreender que o **pixel não é uma unidade física absoluta**. Seu tamanho pode variar conforme a densidade de pixels do dispositivo, normalmente medida em **PPI** (*pixels per inch*). Em telas com maior densidade, um pixel ocupa menos espaço físico; em telas com menor densidade, ocupa mais.

<div align="center">   <img src="https://i.imgur.com/YzIinxq.png" title="source: imgur.com" width="50%"/> </div>

Cada pixel exibido na tela é composto por três **subpixels** das cores primárias da luz:

- **Vermelho (R – Red)**
- **Verde (G – Green)**
- **Azul (B – Blue)**

Esses subpixels variam sua intensidade luminosa para formar a cor final percebida pelo olho humano. Esse sistema é conhecido como **modelo de cores RGB**.

No modelo RGB, cada canal de cor pode assumir valores entre **0 e 255**:

- **0** representa ausência de luz naquele canal.
- **255** representa intensidade máxima de luz.

Exemplos comuns:

- `rgb(255, 0, 0)` → vermelho puro
- `rgb(0, 255, 0)` → verde puro
- `rgb(0, 0, 255)` → azul puro
- `rgb(255, 255, 255)` → branco (todas as cores no máximo)
- `rgb(0, 0, 0)` → preto (ausência de luz)

A combinação desses valores permite que as telas exibam **milhões de cores diferentes**.

<br />

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="4%"/>**CSS - Exemplo:**

```css
p{
	font-size: 16px;
}
```

No exemplo acima, a propriedade **`font-size`** define o tamanho da fonte dos parágrafos como **16 pixels**, valor que geralmente corresponde ao tamanho padrão de fonte adotado pela maioria dos navegadores.

Embora o `px` seja simples e previsível, ele **não é a melhor escolha para layouts responsivos**, pois não se ajusta automaticamente às preferências do usuário ou ao tamanho da tela. Por isso, em projetos modernos, seu uso costuma ser combinado com unidades relativas, como `em`, `rem` e unidades de viewport.

<br />

<h2>2. Unidade em</h2>



A unidade **`em`** é uma unidade **relativa ao tamanho da fonte do elemento pai** onde ela é aplicada. Isso significa que seu valor depende do contexto em que o elemento está inserido, tornando o design mais flexível e escalável.

Considere o seguinte exemplo em HTML:

<img src="https://i.imgur.com/O5NjoiA.png" title="source: imgur.com" width="4%"/>**HTML - Exemplo:**

```html
<main>
  Texto do elemento main
  <p>Texto do elemento parágrafo</p>
</main>
```

A estrutura acima cria o seguinte resultado visual:

<div align="center"><img src="https://i.imgur.com/4y83flV.png" title="source: imgur.com"/></div>

Nesse caso, o parágrafo (`<p>`) é um elemento filho do `<main>`. Por padrão, a maioria dos navegadores define o tamanho base da fonte como **16px**.

Agora, vamos alterar o tamanho da fonte do elemento `<main>` e utilizar a unidade `em` no parágrafo:

<br />

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="4%"/>**CSS - Exemplo:**

```css
main{
  font-size: 24px;
}

main p{
  font-size: 1em;
}
```

<img src="https://i.imgur.com/3HPZ0By.png" title="source: imgur.com" width="5%"/>**Resultado:**

<div align="center"><img src="https://i.imgur.com/5GTVBWl.png" title="source: imgur.com"/></div>

No exemplo acima, o tamanho da fonte do `<main>` é definido como **24px**. Como o parágrafo utiliza `font-size: 1em`, ele assume exatamente o mesmo tamanho da fonte do elemento pai, ficando também com **24px**.

Se o valor fosse alterado para `font-size: 2em`, o parágrafo teria o dobro do tamanho do elemento pai, resultando em **48px**.

A principal característica da unidade `em` é essa **capacidade de escalar proporcionalmente com base no elemento pai**, o que facilita a criação de layouts mais flexíveis e adaptáveis.

Por ser uma unidade baseada em hierarquia, o uso excessivo de `em` em elementos aninhados pode gerar **efeitos acumulativos**, tornando os tamanhos difíceis de prever em estruturas muito profundas. Por esse motivo, o `em` é muito útil em componentes isolados, enquanto unidades como `rem` ajudam a manter maior consistência global.

<br />

<h2>3. Unidade rem</h2>



A unidade **`rem`** (*Root EM*) é uma medida **relativa** no CSS utilizada para definir tamanhos de fontes, espaçamentos e outras dimensões. Diferentemente do **`px`**, que é uma unidade fixa, o **`rem`** é sempre calculado com base no tamanho da fonte do **elemento raiz do documento**, ou seja, a tag `<html>`.

<br />

<h3>3.1. Como funciona o rem?</h3>



- **1rem** equivale ao valor definido em `font-size` no elemento `<html>`.
- Por padrão, a maioria dos navegadores define o `font-size` do `<html>` como **16px**.
- Ao alterar o tamanho da fonte no `<html>`, todos os elementos que utilizam `rem` são redimensionados automaticamente de forma proporcional.

<br />

**Exemplos práticos:**

Com o `font-size` do `<html>` em **16px**:

- **1rem** = 16px
- **2rem** = 32px

Se definirmos `font-size: 20px;` no `<html>`:

- **1rem** = 20px
- **2rem** = 40px

<br />

<img src="https://i.imgur.com/O5NjoiA.png" title="source: imgur.com" width="4%"/>**HTML - Exemplo:**

```html
<main>
  Texto do elemento main
  <p>Texto do elemento parágrafo</p>
</main>
```

A estrutura acima cria o seguinte resultado visual:

<div align="center"><img src="https://i.imgur.com/4y83flV.png" title="source: imgur.com"/></div>

Por padrão, o parágrafo (`<p>`) herda o tamanho base definido no `<html>`, que geralmente é **16px**. Agora, vamos observar o comportamento do `rem` no CSS:

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="4%"/>**CSS - Exemplo:**

```css
main{
  font-size: 24px;
}

main p{
  font-size: 1rem;
}
```

<img src="https://i.imgur.com/3HPZ0By.png" title="source: imgur.com" width="5%"/>**Resultado:**

<div align="center"><img src="https://i.imgur.com/QfjvMWF.png" title="source: imgur.com"/></div>

Nesse caso, mesmo que o `<main>` possua `font-size: 24px`, o parágrafo (`<p>`) continuará com **16px**, pois o valor de `1rem` é sempre baseado no tamanho da fonte do elemento `<html>`, e não no elemento pai.

Se o valor fosse alterado para `font-size: 2rem`, o parágrafo passaria a ter **32px** (2 × 16px). Esse comportamento garante **consistência visual** em todo o documento.

<br />

<h3>3.2. Benefícios do uso de REM</h3>



1. **Escalabilidade e acessibilidade:** se o usuário alterar o tamanho da fonte no navegador, todo o layout se ajusta proporcionalmente.
2. **Consistência visual:** todos os elementos utilizam a mesma base de cálculo, evitando efeitos acumulativos comuns com `em`.
3. **Facilidade de manutenção:** alterar apenas o `font-size` do `<html>` pode redimensionar todo o site.

Além de fontes, o `rem` pode ser utilizado em margens, espaçamentos, larguras e outras propriedades, contribuindo para um design mais flexível e adaptável.

> Por esses motivos, o uso de **`rem`** é altamente recomendado em projetos modernos que priorizam **responsividade, acessibilidade e manutenção simplificada**.

<br />

## 📊 Quadro Comparativo — `px` × `em` × `rem`

| Unidade | Tipo de medida | Base de cálculo                           | Quando usar                                                  |
| ------- | -------------- | ----------------------------------------- | ------------------------------------------------------------ |
| **px**  | Fixa           | Pixel da tela                             | Quando é necessária **precisão absoluta**, como em bordas, ícones pequenos ou ajustes visuais específicos. |
| **em**  | Relativa       | Tamanho da fonte do **elemento pai**      | Quando o tamanho deve **crescer ou diminuir proporcionalmente** dentro de um contexto específico. |
| **rem** | Relativa       | Tamanho da fonte do elemento **`<html>`** | Quando se busca **consistência, acessibilidade e facilidade de manutenção** em todo o layout. |

<br />

### 🧠 Resumo prático

- **Use `px`** para detalhes visuais que não devem variar.
- **Use `em`** quando o tamanho precisa se adaptar ao contexto do elemento pai.
- **Use `rem`** como padrão para fontes e espaçamentos em layouts responsivos.

> Em projetos modernos, o uso de **`rem`** tende a ser a melhor escolha para garantir escalabilidade e acessibilidade, enquanto `px` e `em` podem ser utilizados de forma complementar.

<br />

Após compreender `px`, `em` e `rem`, o próximo passo natural é entender **unidades percentuais e de viewport ( `vw` e `vh`)**, que permitem criar layouts verdadeiramente fluidos e adaptáveis ao tamanho da tela.

<br />

<h2>4. Unidade Percentual</h2>



A unidade **percentual** (`%`) no CSS é uma medida **relativa**, cujo valor é calculado com base nas dimensões de outro elemento — geralmente o **elemento pai**. Ela é amplamente utilizada para criar layouts flexíveis e responsivos, pois permite que os elementos se ajustem proporcionalmente ao espaço disponível.

<br />

<h3>4.1. Como funciona a unidade percentual?</h3>



Ao utilizar porcentagem para definir **largura** (`width`) ou **altura** (`height`), o valor será calculado em relação ao tamanho do elemento pai.

<br />

<img src="https://i.imgur.com/O5NjoiA.png" title="source: imgur.com" width="4%"/>**HTML - Exemplo:**

```html
<main>
  <section>
    Seção
  </section>
</main>
```

A estrutura acima gera o seguinte resultado visual:

<div align="center"><img src="https://i.imgur.com/KwjrEJJ.png" title="source: imgur.com"/> </div>

Agora, vamos definir a largura do `<main>` como **1000px** e a largura do `<section>` como **80%**. Nesse cenário, o `<section>` ocupará **800px**, pois 80% de 1000px equivale a esse valor.

<br />

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="4%"/>**CSS - Exemplo:**

```css
main {
  width: 1000px;
}

section {
  width: 80%;
}
```

<img src="https://i.imgur.com/3HPZ0By.png" title="source: imgur.com" width="5%"/>**Resultado:**

<div align="center"><img src="https://i.imgur.com/GeAqUWI.png" title="source: imgur.com"/> </div>

Nesse exemplo, a `<section>` ocupa **80% da largura do `<main>`**, ajustando-se automaticamente caso o tamanho do elemento pai seja alterado.

O mesmo conceito se aplica à **altura** (`height`). No entanto, é importante destacar que, para que a altura em porcentagem funcione corretamente, o elemento pai **precisa ter uma altura definida explicitamente**. Caso contrário, o navegador não terá uma referência para o cálculo.

<br />

> ### 💡 Pontos importantes
>
> - `%` **não é relativo à tela**, e sim ao **elemento pai**
> - Se o tamanho do elemento pai mudar, o elemento filho **se ajusta automaticamente**
> - Para `height: %`, o elemento pai **precisa ter altura definida**
> - O navegador **sempre olha primeiro para o tamanho do elemento pai**
> - Em seguida, aplica a porcentagem definida no elemento filho

<br />

<h3>4.2. Benefícios do uso de porcentagens</h3>



- **Responsividade:** Facilita a adaptação do layout a diferentes tamanhos de tela sem a necessidade de valores fixos.
- **Escalabilidade:** Mantém proporções consistentes entre os elementos, mesmo quando o layout é redimensionado.
- **Flexibilidade:** Permite criar estruturas fluídas que se ajustam ao espaço disponível.

A unidade **percentual** é fundamental em layouts modernos, pois contribui diretamente para a criação de interfaces **flexíveis, adaptáveis e responsivas**, funcionando muito bem em conjunto com unidades como `rem`, `vw` e `vh`.

<br />

<h2>5. Unidade vh e vw</h2>



No **web design responsivo**, é comum utilizar medidas percentuais (`%`) para redimensionar elementos conforme o tamanho de seus elementos pais. No entanto, em algumas situações, é necessário que o dimensionamento seja **relativo diretamente à tela do usuário**, e não à estrutura do layout.

Para atender a esse cenário, o CSS oferece as unidades **`vh`** (*viewport height*) e **`vw`** (*viewport width*), que utilizam como referência o tamanho da **viewport**.

> **Viewport** é a área visível da página web no momento da visualização — ou seja, a região efetivamente exibida na tela do dispositivo, considerando o tamanho da janela do navegador.

<br />

<h3>5.1. Como funcionam essas unidades?</h3>



- **`1vh`** → equivale a **1% da altura total** da viewport.
- **`1vw`** → equivale a **1% da largura total** da viewport.

Essas unidades permitem criar layouts que se ajustam automaticamente ao tamanho da tela, independentemente da resolução, proporção ou dispositivo utilizado.

<br />

<h3>5.2. Unidade vh - Altura da Viewport</h3>



<div align="center">
    <img src="https://i.imgur.com/IMHAoik.png" title="source: imgur.com" width="80%"/>
</div>

No exemplo acima, o valor `100vh` representa **100% da altura da viewport**, fazendo com que o elemento ocupe toda a altura visível da tela.

<br />

<h3>5.3. Unidade vw - Largura da Viewport</h3>



<div align="center">
    <img src="https://i.imgur.com/6sy0JKw.png" title="source: imgur.com" width="80%"/>
</div>

De forma semelhante, `100vw` representa **100% da largura da viewport**, ocupando toda a extensão horizontal visível da tela.

<br />

<h3>5.4. Cálculo prático</h3>



- Se a **altura** da viewport for **900px**, então `1vh` corresponde a **9px**.
- Se a **largura** da viewport for **750px**, então `1vw` corresponde a **7,5px**.

Essas unidades são especialmente úteis para:

- Criar seções de **tela cheia** (*full screen*).
- Desenvolver **banners** e **sliders** responsivos.
- Ajustar elementos que precisam se alinhar exatamente ao tamanho da tela.

<br />

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="4%"/>**CSS - Exemplo:**

```css
.slide {  
    height: 100vh; 
}
```

O código acima cria um elemento que ocupa exatamente **toda a altura da viewport**, independentemente do tamanho ou da resolução do dispositivo.

<br />

> [!TIP]
>
> As unidades `vh` e `vw` podem ser combinadas com outras unidades, como `rem` e `%`, para obter maior flexibilidade no layout. Por exemplo, é comum definir **larguras em `vw`** e **espaçamentos em `rem`**, garantindo adaptação à tela sem comprometer a legibilidade.

<br />

> [!IMPORTANT]
>
> Em dispositivos móveis, a viewport pode variar com a exibição da barra de endereço do navegador, o que pode causar pequenas diferenças no comportamento de `vh`.

<br />

<h2>6. Unidades vmin e vmax</h2>



As unidades **`vmin`** e **`vmax`** são variações das unidades **`vh`** (*viewport height*) e **`vw`** (*viewport width*), com um diferencial importante: seus valores são calculados com base **na menor ou na maior dimensão da viewport**, respectivamente.

> **Viewport** é a área visível da página web no momento da visualização, seja no navegador ou em modo tela cheia.

<br />

<h3>6.1. Unidade vmin</h3>



A unidade **`vmin`** representa **1% da menor dimensão da viewport**, considerando largura ou altura.

<div align="center">
    <img src="https://i.imgur.com/Gfh5fUh.png" title="source: imgur.com" width="80%"/>
</div>
<br />

**Exemplos de cálculo:**

- Largura **1200px** e altura **800px** → `100vmin` = **800px**
- Largura **900px** e altura **600px** → `100vmin` = **600px**

<br />

> [!TIP]
>
> Utilizar `vmin` é ideal quando o elemento **não deve ultrapassar a menor dimensão da tela**, garantindo melhor adaptação em dispositivos menores ou em diferentes orientações.

<br />

<h3>6.2. Unidade vmax - Largura da Viewport</h3>



A unidade **`vmax`** representa **1% da maior dimensão da viewport**, considerando largura ou altura.

<div align="center">
    <img src="https://i.imgur.com/zotd4Zq.png" title="source: imgur.com" width="80%"/>
</div>
**Exemplos de cálculo:**

- Largura **1200px** e altura **800px** → `100vmax` = **1200px**
- Largura **900px** e altura **600px** → `100vmax` = **900px**

<br />

> [!TIP]
>
> O uso de `vmax` é especialmente útil para **banners, imagens de fundo e áreas de destaque**, que precisam acompanhar a maior dimensão da tela.

<br />

<h3>6.3. Cálculo prático</h3>



Se a viewport tiver **1100px** de largura e **700px** de altura:

- `1vmin` = **7px** (1% de 700px)
- `1vmax` = **11px** (1% de 1100px)

Se a viewport tiver **800px** de largura e **1080px** de altura:

- `1vmin` = **8px**
- `1vmax` = **10,8px**

<br />

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="4%"/>**CSS - Exemplo:**

```css
.card {
    width: 50vmin;  /* Proporcional à menor dimensão da tela */
    height: 50vmax; /* Proporcional à maior dimensão da tela */
}

```

O exemplo acima cria um componente que ajusta sua largura com base na **menor dimensão** da viewport e sua altura com base na **maior dimensão**, garantindo um comportamento responsivo dinâmico.

<br />

<h2>6. Ângulo</h2>



No CSS, além de unidades usadas para definir **tamanhos e espaçamentos**, também existem unidades voltadas para **direção e rotação**. A unidade **`deg`** (*degree / grau*) é utilizada para representar **ângulos**, sendo fundamental em efeitos visuais como rotações, gradientes e transformações.

Ela não define tamanho, mas sim **orientação**, controlando a direção em que um efeito visual será aplicado.

A unidade **`deg`** representa um **grau de ângulo**, seguindo o mesmo conceito usado na geometria:

<div align="center">
    <img src="https://i.imgur.com/cDe9Vvd.png" title="source: imgur.com" width="80%"/>
</div>

- **0deg (0º)** → ponto inicial do ângulo
- **90deg (90º)** → um quarto de volta
- **180deg (180º)** → meia volta
- **270deg (270º)** → três quartos de volta
- **360deg (360º)** → volta completa

No CSS, os ângulos começam a ser medidos a partir de um ponto de referência específico, que pode variar conforme a propriedade utilizada.

Observe que o sentido do ângulo também é importante:

- **Valores positivos** → rotação no sentido horário
- **Valores negativos** → rotação no sentido anti-horário

A unidade **`deg`** é mais comum nas seguintes propriedades CSS:

- `transform: rotate()`
- `linear-gradient()`
- `conic-gradient()`
- `skew()`

Ela é essencial sempre que precisamos **girar elementos** ou **definir a direção de um efeito visual**.

A rotação é feita com a função `rotate()`, aplicada pela propriedade `transform`.

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="4%"/>**CSS - Exemplo:**

```css
.box {
  transform: rotate(45deg);
}
```

Nesse exemplo, o elemento é girado **45 graus no sentido horário** a partir do seu centro.

<img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="4%"/>**CSS - Exemplo:**

```css
.box {
  transform: rotate(-30deg);
}
```

Nesse exemplo, o elemento é girado **30 graus no sentido anti-horário** a partir do seu centro.

Use a unidade **`deg`** quando precisar:

- Girar elementos visuais
- Controlar a direção de gradientes
- Criar efeitos visuais dinâmicos
- Trabalhar com animações e transições baseadas em rotação

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>
