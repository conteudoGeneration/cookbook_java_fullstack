<h1>Projeto 01 - Portfólio - Estilização da Página index - Seção Projects</h1>



Depois de estilizar a Seção About, vamos estilizar a **seção Projects**, responsável por **apresentar os principais projetos ou trabalhos desenvolvidos**. Essa área funciona como um **portfólio visual**, permitindo que o usuário conheça exemplos práticos, tecnologias utilizadas e o estilo das soluções apresentadas.

A estilização da seção Projects será realizada em **duas etapas**:

1. **Seção Projects (container e cards)**: nesta primeira etapa, o foco será na estruturação e no estilo do contêiner principal e dos **cards de projetos**, definindo layout, espaçamentos, tipografia e organização visual do conteúdo.
2. **Personalização do Carrossel (Swiper)**: em um segundo momento, será feita a customização do **carrossel gerado pela biblioteca Swiper**, ajustando navegação, indicadores e comportamentos visuais para integrar o componente ao layout do site.

Nesta etapa, o foco estará exclusivamente na **Seção Projects**, garantindo **organização, hierarquia visual e boa legibilidade** dos cards. Uma estilização bem definida facilita a navegação, valoriza os projetos apresentados e contribui para uma experiência visual clara, profissional e consistente.

<br />

<h2>👣 Passo 01 - Estilizar a Seção Projects</h2>



1. Abra o arquivo **style.css** e adicione os trechos de código abaixo:

<br />

### ✅ Seção Projects (Container)

<div align="center"><img src="https://i.imgur.com/OGNTqjr.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 321:** Selecionamos a classe `.projects-container`, responsável pela estrutura geral da seção de projetos.

**Linha 322:** A propriedade `scroll-margin-top` define um espaçamento superior ao navegar até essa seção por âncoras, evitando sobreposição com o menu fixo.

**Linha 323:** A propriedade `margin` aplica espaçamento externo vertical e centraliza a seção horizontalmente.

**Linha 324:** A propriedade `text-align: center;` centraliza o texto dentro do contêiner.

<br />

### ✅ Carrossel de Projetos (Container)

<div align="center"><img src="https://i.imgur.com/XM43ujM.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 327:** Selecionamos a classe `.projects-swiper`, contêiner principal do carrossel de projetos.

**Linha 328:** A propriedade `position: relative;` permite o posicionamento correto de elementos internos, como botões de navegação.

**Linha 329:** A propriedade `width: 100%;` faz o carrossel ocupar toda a largura disponível.

**Linha 330:** A propriedade `padding` adiciona espaçamento interno, garantindo respiro visual entre os cards e os controles.

**Linha 331:** A propriedade `overflow: hidden;` impede que conteúdos excedentes fiquem visíveis fora do carrossel.

>### Propriedade Overflow
>
>A propriedade `overflow` controla o que acontece quando o conteúdo de um elemento ultrapassa o espaço definido por sua largura e altura. Ela é muito utilizada para lidar com textos longos, imagens grandes ou qualquer conteúdo que exceda os limites do contêiner.
>
><img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="4%"/>**CSS - Exemplo:**
>
>```css
>.container {
>    overflow: visible;
>}
>```
>
>**Opções comuns:**
>
>- **`visible`**: Valor padrão da propriedade. O conteúdo que ultrapassa o tamanho do elemento continua visível fora de seus limites, sem qualquer corte. 
>- **`hidden`**: Oculta todo o conteúdo que ultrapassar o tamanho do elemento. O excesso simplesmente não é exibido. 
>- **`scroll`**: Exibe barras de rolagem horizontal e vertical, independentemente de o conteúdo ultrapassar ou não o tamanho do elemento.
>- **`auto`**: Adiciona barras de rolagem apenas quando o conteúdo realmente ultrapassa o tamanho do elemento. É a opção mais usada na prática.
>
><br />
>
>#### Controle independente dos eixos
>
>Também é possível controlar o comportamento do conteúdo separadamente nos eixos:
>
>- **`overflow-x`**: controla o excesso no eixo horizontal
>- **`overflow-y`**: controla o excesso no eixo vertical
>
><img src="https://i.imgur.com/DSBasbH.png" title="source: imgur.com" width="4%"/>**CSS - Exemplo:**
>
>```css
>.container {
>    overflow-x: hidden;
>    overflow-y: auto;
>}
>```
>
>Nesse caso, o conteúdo não ultrapassa horizontalmente, mas pode ser rolado verticalmente se necessário.
>
>A propriedade `overflow` é essencial para manter o layout organizado, controlar vazamentos de conteúdo e melhorar a experiência do usuário em interfaces responsivas.

<br />

<div align="left"><img src="https://i.imgur.com/63FhSvn.png" title="source: imgur.com" width="4%"/> <a href="https://www.w3schools.com/css/css_overflow.asp" target="_blank"><b>Documentação: Overflow</b></a></div>

<br />

### ✅ Card do Projeto

<div align="center"><img src="https://i.imgur.com/vXSq0it.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 334:** Selecionamos a classe `.project-card`, responsável pelo card individual de cada projeto.

**Linha 335:** A propriedade `display: flex;` cria um layout flexível para organizar o conteúdo interno.

**Linha 336:** A propriedade `flex-direction: column;` organiza os elementos do card em coluna.

**Linhas 337 e 338:** As propriedades `width` e `height` garantem que o card ocupe todo o espaço disponível no slide.

**Linha 339:** A propriedade `border` cria uma borda sutil ao redor do card.

**Linha 340:** A propriedade `border-radius` arredonda os cantos do card.

**Linha 341:** A propriedade `background-color` define o fundo branco do card.

**Linha 342:** A propriedade `box-shadow` adiciona uma sombra leve, criando profundidade.

**Linha 343:** A propriedade `overflow: hidden;` impede que conteúdos internos ultrapassem os limites do card.

**Linha 344:** A propriedade `transition` aplica uma animação suave às mudanças visuais.

**Linha 347:** Definimos o estado *hover* do card.

**Linha 348:** A sombra é intensificada, reforçando o feedback visual de interação.

<br />

### ✅ Card  do Projeto - Imagem do Projeto

<div align="center"><img src="https://i.imgur.com/6VgBZhb.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 351:** Selecionamos a classe `.project-image`, contêiner da imagem do projeto.

**Linha 352:** A propriedade `display: flex;` cria um layout flexível para organizar o conteúdo interno.

**Linhas 353 e 354:** As propriedades *align-items* e *justify-content* centralizam a imagem horizontalmente e verticalmente.

**Linhas 355 e 356:** As propriedades `width` e `height` definem um espaço fixo para a área da imagem.

**Linha 357:** A propriedade `background` aplica um degradê suave, destacando visualmente a área da imagem.

**Linha 360:** Selecionamos a imagem dentro de `.project-image`.

**Linhas 361 e 362:** As propriedades `width` e `height` controlam as dimensões da imagem em relação ao contêiner, garantindo que ela se ajuste proporcionalmente ao espaço disponível.

**Linhas 363 e 364:** As propriedades `max-width` e `max-height` limitam o tamanho máximo da imagem, evitando que ela ultrapasse a área definida e preservando a harmonia visual do layout.

**Linha 365:** A propriedade `object-fit: contain;` garante que a imagem seja exibida sem cortes ou distorções.

**Linha 366:** A propriedade `transition` prepara a imagem para animações suaves.

<br />

### ✅ Card  do Projeto - Dados do Projeto

<div align="center"><img src="https://i.imgur.com/1Xcx8b7.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 369:** Selecionamos a classe `.project-content`, responsável pelo conteúdo textual do card.

**Linha 370:** A propriedade `display: flex;` cria um layout flexível.

**Linha 371:** A propriedade `flex: 1;` permite que o conteúdo ocupe o espaço restante do card.

**Linha 372:** A propriedade `flex-direction: column;` organiza título, texto e tags em coluna.

**Linha 373:** A propriedade `padding` adiciona espaçamento interno ao conteúdo.

**Linha 374:** A propriedade `text-align: left;` alinha o texto à esquerda para melhor leitura.

**Linha 377:** Selecionamos o título `<h3>` do projeto.

**Linha 378:** A propriedade `margin-bottom` cria espaçamento inferior.

**Linha 379:** A propriedade `font-size` define um tamanho adequado para o título.

**Linha 380:** A propriedade `font-weight: 700;` reforça a hierarquia visual.

**Linha 383:** Selecionamos o parágrafo descritivo do projeto.

**Linha 384:** A propriedade `flex: 1;` permite que o texto ocupe o espaço disponível.

**Linha 385:** A propriedade `margin-bottom` separa o texto dos elementos abaixo.

**Linha 386:** A propriedade `font-size` ajusta levemente o tamanho do texto.

**Linha 387:** A propriedade `color` aplica um tom de cinza para suavizar o texto.

<br />

### ✅ Card  do Projeto - Tags

<div align="center"><img src="https://i.imgur.com/8HhFDiO.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 390:** Selecionamos a classe `.project-tags`, responsável pelo agrupamento das tags do projeto.

**Linha 391:** A propriedade `display: flex;` organiza as tags em linha.

**Linha 392:** A propriedade `flex-wrap: wrap;` permite que as tags quebrem linha quando necessário.

**Linha 393:** A propriedade `gap` define o espaçamento entre as tags.

**Linha 396:** Selecionamos a classe `.tag`, que representa cada tag individual.

**Linha 397:** A propriedade `padding` cria uma área interna confortável.

**Linha 398:** A propriedade `border-radius` arredonda os cantos da tag.

**Linha 399:** A propriedade `background-color` define o fundo da tag com um roxo claro.

**Linhas 400 a 402:** As propriedades `font-size`, `font-weight` e `color` definem tamanho, peso e cor do texto.

<br />

### ✅ Card  do Projeto - Botões

<div align="center"><img src="https://i.imgur.com/lntImZr.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 405:** Selecionamos a classe `.project-buttons`, responsável pelos botões do card.

**Linha 406:** A propriedade `display: flex;` organiza os botões em linha.

**Linha 407:** A propriedade `justify-content: center;` centraliza os botões horizontalmente.

**Linha 408:** A propriedade `align-items: center;` centraliza os botões verticalmente.

**Linha 409:** A propriedade `gap` define o espaçamento entre os botões.

**Linha 410:** A propriedade `margin-top` cria espaçamento superior em relação ao conteúdo.

<br />

<h2>👣 Passo 02 - Visualizar a Página no Navegador</h2>



1. Abra a página index.html no Navegador, através do **Live Server** e clique no item **Projetos** do menu:

<div align="center"><img src="https://i.imgur.com/6oiXKJ1.png" title="source: imgur.com" /></div>

Após a aplicação das propriedades no código acima, a **seção Projects** recebeu ajustes estruturais e visuais que organizam a apresentação dos projetos e facilitam a navegação entre os itens:

- O contêiner `.projects-container` passou a contar com **margem superior controlada na rolagem** (`scroll-margin-top`) e **centralização horizontal**, garantindo alinhamento correto ao navegar por âncoras e melhor espaçamento entre seções.
- A área `.projects-swiper` define o **espaço estrutural do carrossel**, com preenchimento interno adequado e **overflow oculto**, assegurando que os slides sejam exibidos corretamente dentro da área visível.
- Cada `.project-card` foi estruturado como um **card flexível em coluna**, com bordas, cantos arredondados, fundo claro e **sombra sutil**, criando um padrão visual consistente para os projetos.
- O efeito de **hover** nos cards reforça a interatividade, destacando visualmente o projeto ao passar o cursor.
- A região `.project-image` centraliza a imagem do projeto e aplica um **fundo em degradê suave**, valorizando o elemento visual sem competir com o conteúdo.
- As imagens dos projetos foram configuradas para serem **responsivas e proporcionais**, utilizando `object-fit: contain` e transições suaves para possíveis efeitos de interação.
- O bloco `.project-content` organiza título, descrição e tags em **layout vertical**, com alinhamento à esquerda, garantindo boa legibilidade e hierarquia das informações.
- As **tags** utilizam cores suaves, tipografia destacada e espaçamento consistente, facilitando a identificação das tecnologias ou categorias de cada projeto.
- A área `.project-buttons` centraliza os botões de ação, mantendo **espaçamento uniforme** e reforçando a usabilidade.

Essas definições tornam a seção Projects mais **organizada, escaneável e visualmente coerente**, preparando o layout para a navegação em carrossel e para a personalização avançada que será realizada na próxima etapa com a biblioteca Swiper.

2. Para visualizar os posicionamento dos elementos HTML no Navegador, ative a extensão **Pesticide**, clicando no botão <img src="https://i.imgur.com/3E84PHm.png" title="source: imgur.com" width="4%"/> em **Extensões**:

<div align="center"><img src="https://i.imgur.com/lxgKchS.png" title="source: imgur.com" /></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>