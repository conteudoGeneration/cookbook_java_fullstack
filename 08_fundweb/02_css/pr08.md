<h1>Projeto 01 - Portfólio - Estilização da Página index - Seção Projects</h1>



Na **etapa 2** da estilização da **Seção Projects**, o foco será a **personalização do carrossel de projetos**, que será construído a **Biblioteca Swiper**, no conteúdo sobre **JavaScript para WEB**. Nessa fase, a estrutura básica da seção já está definida, e o objetivo passa a ser **integrar o comportamento do carrossel ao visual do site**.

A personalização do Swiper envolve ajustes visuais e funcionais, como **espaçamento entre os cards**, **controles de navegação**, **indicadores de página** e **comportamentos de interação**, garantindo que o carrossel seja intuitivo e coerente com a identidade visual do projeto.

Essa etapa é fundamental para melhorar a **experiência de navegação**, permitindo que o usuário explore os projetos de forma fluida e organizada. Um carrossel bem customizado valoriza o conteúdo apresentado, mantém a consistência do layout e reforça o aspecto profissional da seção Projects.

<br />

<h2>👣 Passo 01 - Inserir o arquivo CSS do Swiper na página index</h2>



De acordo com a documentação oficial da **biblioteca Swiper**, é necessário adicionar o **arquivo CSS padrão do Swiper** na página `index`, pois ele contém os **estilos base essenciais** para o funcionamento correto do carrossel. Esses estilos definem a **estrutura dos slides**, o **comportamento de rolagem**, as **animações** e o posicionamento adequado dos elementos internos, como o contêiner de slides (*wrapper*) e os próprios *slides*.

Sem esse arquivo, o Swiper pode até ser inicializado via JavaScript, porém os slides não serão exibidos ou organizados corretamente, comprometendo o layout e a usabilidade da página.

O arquivo CSS do Swiper deve ser inserido **antes do CSS principal da aplicação**, permitindo que seus estilos padrões sejam **sobrescritos de forma controlada**, possibilitando a personalização visual do carrossel e garantindo consistência com o restante do layout do site.

1. Abra a página **index.html**
2. Na seção `<head>`, **antes da folha de estilo padrão da página**, insira o código abaixo:

```html
<!-- SWIPER CSS -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css"> 
```

3. O resultado você confere na imagem abaixo:

<div align="center"><img src="https://i.imgur.com/OYzZziz.png" title="source: imgur.com" /></div>

<br />

> [!WARNING]
>
> Não é necessário adicionar o **arquivo CSS padrão da biblioteca Swiper** na página **`success.html`**, pois essa página **não utiliza componentes de carrossel**. Como o Swiper não será inicializado nem exibido nesse contexto, a inclusão do arquivo seria desnecessária e não traria benefícios ao layout ou à funcionalidade da página.
>
> Manter apenas os arquivos estritamente necessários contribui para um **carregamento mais eficiente**, melhor organização do projeto e **boa prática de performance**.

<br />

<div align="left"><img src="https://i.imgur.com/ZUDElcI.png" title="source: imgur.com" width="4%"/> <a href="https://swiperjs.com/get-started#use-swiper-from-cdn" target="_blank"><b>Documentação do Swiper JS - Carrossel</b></a></div>

<br />

<h2>👣 Passo 02 - Estilizar o Carrossel</h2>



Todas as **propriedades CSS sobrescritas** nesta etapa foram definidas com base na **documentação oficial da biblioteca Swiper**, garantindo compatibilidade com a estrutura interna do carrossel e respeito às boas práticas recomendadas pelos mantenedores da ferramenta.

A utilização dessas referências oficiais assegura que as personalizações sejam feitas de forma **segura, consistente e sustentável**, evitando comportamentos inesperados e facilitando futuras manutenções ou atualizações da biblioteca. O link para a documentação completa encontra-se ao final desta seção.

1. Abra o arquivo **style.css** e adicione os trechos de código abaixo:

<br />

### ✅ Containers do Swiper

<div align="center"><img src="https://i.imgur.com/f6tPklg.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 413:** Selecionamos a classe `.swiper-wrapper`.

**Linha 414:** A propriedade `align-items: stretch;` garante que todos os slides tenham a mesma altura.

**Linha 417:** Selecionamos a classe `.swiper-slide`.

**Linha 418:** A propriedade `display: flex;` permite que o card interno se adapte corretamente.

**Linha 419:** A propriedade `height: auto;` ajusta a altura do slide ao conteúdo.

<br />

### ✅ Botões de Navegação do Swiper

<div align="center"><img src="https://i.imgur.com/DRfbGIV.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linhas 423 e 424:** Selecionamos os botões de navegação do Swiper.

**Linhas 425 e 426:** As propriedades `width` e `height` definem o tamanho dos botões.

**Linhas 429 e 430:** Selecionamos os ícones internos dos botões.

**Linha 431:** A propriedade `font-size` define o tamanho do ícone.

**Linha 432:** A propriedade `color` aplica a cor roxa principal.

**Linhas 435 e 436:** Definimos o estado *hover* dos botões de navegação.

**Linha 437:** A cor do ícone é alterada para um tom mais escuro, reforçando a interatividade.

<br />

> [!IMPORTANT]
>
> Os **ícones dos botões de navegação do Swiper não são imagens**, mas sim **caracteres tipográficos** (os símbolos de **maior `>`** e **menor `<`**), inseridos via pseudo-elementos (`::after`).
>
> Por esse motivo, eles respondem diretamente a **propriedades de tipografia**, como:
>
> - `font-size`, que controla o tamanho do símbolo
> - `color`, que define a cor do botão
> - efeitos de `hover`, aplicados da mesma forma que em textos
>
> Essa abordagem permite **personalização simples e consistente**, facilitando a adaptação dos botões à identidade visual do projeto sem a necessidade de imagens ou SVGs adicionais.

<br />

### ✅ Paginação do Swiper

<div align="center"><img src="https://i.imgur.com/twwucIa.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 441:** Selecionamos os indicadores de paginação.

**Linhas 442 e 443:** As propriedades `width` e `height` definem o tamanho dos indicadores.

**Linha 444:** A propriedade `background-color` aplica um tom neutro.

**Linha 445:** A propriedade `opacity: 1;` mantém os indicadores totalmente visíveis.

**Linha 448:** Selecionamos o indicador ativo da paginação.

**Linha 449:** A propriedade `width` amplia o indicador ativo, destacando-o visualmente.

**Linha 450:** A propriedade `border-radius` suaviza os cantos.

**Linha 451:** A propriedade `background-color` aplica o roxo principal, indicando o slide ativo.

<br />

<div align="left"><img src="https://i.imgur.com/ZUDElcI.png" title="source: imgur.com" width="4%"/> <a href="https://swiperjs.com/swiper-api" target="_blank"><b>Documentação do Swiper JS - Personalizando o Carrossel</b></a></div>

<br />

<h2>👣 Passo 03 - Visualizar a Página no Navegador</h2>



1. Abra a página index.html no Navegador, através do **Live Server** e clique no item **Projetos** do menu:

<div align="center"><img src="https://i.imgur.com/eHU1xyY.png" title="source: imgur.com" /></div>

Após a aplicação das propriedades no código acima, a **personalização do Swiper** recebeu ajustes visuais e estruturais que aprimoram o comportamento dos slides e os elementos de navegação do carrossel:

- O `.swiper-wrapper` passou a alinhar os slides com **altura uniforme** (`align-items: stretch`), garantindo consistência visual entre os cards, mesmo com conteúdos de tamanhos diferentes.
- Cada `.swiper-slide` foi configurado como um **contêiner flexível**, permitindo que o card interno ocupe toda a altura disponível e mantenha alinhamento adequado.
- Os botões de navegação (`.swiper-button-next` e `.swiper-button-prev`) tiveram suas **dimensões padronizadas**, facilitando a interação e melhorando a área de clique.
- Os ícones dos botões receberam **tamanho de fonte definido** e **cor personalizada**, integrando os controles do carrossel à identidade visual do projeto.
- O efeito de **hover nos botões** fornece feedback visual claro ao usuário, reforçando a interatividade.
- Os indicadores de paginação (`.swiper-pagination-bullet`) foram estilizados com **tamanho uniforme**, cor neutra e opacidade total, tornando-os visíveis e discretos.
- O indicador ativo recebeu **destaque visual**, com maior largura, cantos levemente arredondados e cor de contraste, facilitando a identificação do slide atual.

Essas customizações garantem um carrossel mais **coeso, intuitivo e alinhado ao design do site**, complementando a estilização da seção Projects e elevando a qualidade da experiência de navegação.

2. Para visualizar os posicionamento dos elementos HTML no Navegador, ative a extensão **Pesticide**, clicando no botão <img src="https://i.imgur.com/3E84PHm.png" title="source: imgur.com" width="4%"/> em **Extensões**:

<div align="center"><img src="https://i.imgur.com/PRsU85o.png" title="source: imgur.com" /></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>