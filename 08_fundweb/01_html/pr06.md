<h1>Projeto 01 - Portfólio - Criar a Página index - Seção About</h1>



Agora que concluímos a **estrutura da Seção Hero**, vamos focar na construção da **Seção About**.

<div align="center"><img src="https://i.imgur.com/bkc4v3j.png" title="source: imgur.com" /></div>

A **Seção About** tem como objetivo **apresentar informações sobre o autor do site**, como um breve resumo profissional, experiências, habilidades ou interesses. Essa seção ajuda o visitante a conhecer melhor quem está por trás do projeto, fortalecendo a conexão e a credibilidade da página.

Observe no wireframe abaixo como a Seção About é organizada, destacando os elementos que serão utilizados para apresentar o conteúdo de forma clara e objetiva.

<div align="center"><img src="https://i.imgur.com/MSC05JZ.png" title="source: imgur.com" /></div>

A **Seção About** será composta pelos seguintes elementos estruturais:

- Uma tag semântica `<figure>` (retângulo vermelho tracejado), responsável por exibir a **foto de perfil do GitHub**, reforçando a identificação visual do autor.
- Uma tag semântica `<article>` (retângulo verde tracejado), que conterá o **texto de apresentação**, além de links para o **GitHub** e para o **Currículo**. Esses links serão estilizados posteriormente com CSS para se comportarem como botões.
  Ainda dentro dessa estrutura, serão exibidos **dois cards informativos** com a quantidade de **Seguidores** e de **Repositórios Públicos** do GitHub.
- Os **links e os dados do GitHub** serão agrupados por uma tag `<div>` (retângulo cinza tracejado), facilitando a organização do conteúdo.
- Os **links** ficarão envolvidos por uma tag `<div>` específica (retângulo rosa tracejado), permitindo uma estilização independente.
- Os **cards com os dados do GitHub** serão agrupados por uma tag `<div>` (retângulo laranja tracejado).
- Cada **card individual** será construído dentro de uma tag `<div>` própria (retângulo azul tracejado), garantindo flexibilidade no layout.

Para construir a estrutura básica da **Seção About**, utilizaremos as **Tags Estruturais do HTML**, a tag **`<img>`** para a imagem de perfil, além das tags de **texto** e **links**.

<br />

<h2>👣 Passo 01 - Criar a Estrutura da Seção About na Página index.html</h2>



Vamos atualizar o código do arquivo **index.html**, inserindo **o conteúdo da Seção About**, **dentro da tag `<section id="about">`**, que foi criada no interior da tag `<main>`.

Com isso, a estrutura completa dessa seção ficará definida, restando apenas a aplicação dos estilos com CSS nos próximos passos.

<div align="center"><img src="https://i.imgur.com/li4KYlB.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linhas 73 a 113:** A tag `<section>` foi atualizada com as classes **section-container** e **about-container**, definidas por meio do atributo `class`. Essas classes serão utilizadas posteriormente na estilização da seção About com CSS.

**Linhas 75 a 77:** Utilizamos a tag semântica `<figure>` com a classe `about-image`, para agrupar e representar a imagem do autor.

**Linha 76:** Inserimos a imagem com a tag `<img>`, definindo o caminho do arquivo SVG no atributo `src` e uma descrição no atributo `alt`, essencial para acessibilidade.

> [!IMPORTANT]
>
> Não se preocupe quanto à **foto do perfil**, pois ela **não será definida manualmente**.
> Essa imagem temporária será **substituída de forma dinâmica, via código**, pela **sua foto de perfil do GitHub**, garantindo que o conteúdo esteja sempre atualizado sem a necessidade de alterações no HTML.

**Linhas 79 a 111:** Criamos um contêiner de conteúdo com a tag `<article>` e a classe `about-content`, que agrupa todas as informações textuais da seção.

**Linha 80:** Inserimos um título de seção através da tag `<h2>`, indicando que o conteúdo a seguir se refere à apresentação pessoal.

**Linhas 81 a 92:** Inserimos dois parágrafos de texto com a tag `<p>`. O texto *Lorem ipsum* é utilizado apenas como conteúdo temporário durante o desenvolvimento e deve ser substituído por um texto real.

<br />

> **O que é o Lorem ipsum?**
>
> A expressão **Lorem ipsum** em design gráfico e editoração é um  texto padrão em latim utilizado na produção gráfica para preencher os  espaços de texto em publicações para testar e ajustar aspectos visuais  antes de utilizar conteúdo real.
>
> Para gerar o Lorem ipsum automaticamente no Visual Studio Code, basta digitar a palavra **lorem**, que será aberto um menu com a opção para criar um parágrafo inteiro com o texto padrão do Lorem ipsum.

<br />

> [!TIP]
>
> O texto **Lorem Ipsum** utilizado nesta seção serve apenas como **conteúdo temporário** para fins de estrutura e layout.
>
> Posteriormente, ele deve ser **substituído pela sua descrição profissional**, semelhante ao resumo apresentado no **LinkedIn** e no **Perfil do GitHub**, destacando quem é você, a sua área de atuação, principais habilidades e tecnologias dominadas.
>
> 💡 **Dica:** você pode utilizar ferramentas de **Inteligência Artificial** para ajudar na construção desse texto. Basta informar sua profissão, nível de experiência e tecnologias que domina. A IA pode gerar um texto inicial bem estruturado, que você pode ajustar para refletir sua identidade e trajetória profissional.

<br />

**Linhas 94 a 110:** Criamos um contêiner com a classe `about-buttons-data`, responsável por organizar os botões e os dados numéricos da seção.

**Linhas 95 a 98:** Inserimos um contêiner com a classe `buttons-container`, utilizado para agrupar os botões de ação.

**Linha 96:** Criamos um link estilizado como botão com a tag `<a>` e a classe `botao`, direcionando o usuário para o GitHub em uma nova aba por meio do atributo `target="_blank"`.

**Linha 97:** Criamos um segundo link estilizado como botão com a classe `botao-outline`, destinado ao acesso ao currículo do autor.

> [!TIP]
>
> Para disponibilizar o **Currículo** no site, utilize o currículo que foi construído na **Seção sobre Currículos**, gere um **arquivo em PDF** e publique-o como um **link público no Google Drive**. Esse link será utilizado no botão de acesso ao currículo dentro da página.
>
> ### Passo a passo:
>
> 1. **Exporte o currículo em PDF**
>    - Utilize a opção **“Salvar/Exportar como PDF”** do Google Docs.
> 2. **Faça o upload no Google Drive**
>    - Acesse o seu Google Drive e envie o arquivo PDF do currículo.
> 3. **Configure o acesso público**
>    - Clique com o botão direito no arquivo → **“Compartilhar”**
>    - Altere a permissão para **“Qualquer pessoa com o link”**
>    - Defina como **Visualizador**.
> 4. **Copie o link público**
>    - Após liberar o acesso, copie o link gerado pelo Google Drive.
> 5. **Utilize o link no site**
>    - Insira esse link no link de **Currículo** da seção About, permitindo que qualquer pessoa visualize ou baixe o PDF.
>
> Dessa forma, seu currículo ficará **sempre acessível**, organizado e fácil de atualizar, bastando substituir o arquivo no Google Drive sempre que houver mudanças.

**Linhas 100 a 109:** Criamos um contêiner com a classe `data-container`, responsável por agrupar os cards com os dados do GitHub.

**Linhas 101 a 104:** Criamos o primeiro item de dado através da tag `<div>` com a classe `data-item`.

**Linha 102:** Inserimos um valor numérico com a tag `<span>` e a classe `data-number`, representando a quantidade de seguidores.

**Linha 103:** Inserimos um rótulo descritivo com a tag `<span>` e a classe `data-label`, explicando o significado do número exibido.

**Linhas 105 a 108:** Criamos o segundo item de dado através da tag `<div>` com a classe `data-item`.

**Linha 106:** Inserimos outro valor numérico, representando a quantidade de repositórios.

**Linha 107:** Inserimos o rótulo correspondente ao valor apresentado.

> [!IMPORTANT]
>
> Não se preocupe com o **link do GitHub** nem com os números de **repositórios públicos** e **seguidores**, pois essas informações **não serão definidas manualmente**.
>
> Esses dados temporários serão **substituídos dinamicamente via código**, a partir das informações do **seu perfil do GitHub**. Dessa forma, o conteúdo permanecerá **sempre atualizado**, sem a necessidade de realizar alterações diretas no arquivo **HTML** sempre que houver mudanças no perfil.

<br />

<h2>👣 Passo 02 - Visualizar a Página index no Navegador</h2>



1. Volte para o **navegador** e observe que a **estrutura da Seção About** já estará visível no final da sua **página HTML**:

<div align="center"><img src="https://i.imgur.com/a6fvphU.png" title="source: imgur.com" /></div>

Nesse momento, os elementos da **Seção About** aparecerão na página **sem nenhum tipo de estilização**, exibindo apenas a estrutura definida em HTML.

<br />

> [!WARNING]
>
> Para que todos os elementos ficassem visíveis na imagem acima, o tamanho da imagem foi ajustado. Ao abrir o arquivo `index.html` no navegador, a imagem da Seção About será exibida em seu tamanho original, ocupando toda a largura da página.

<br />

Quando a **Seção About** for estilizada com **CSS**, ela passará a ter a aparência ilustrada na imagem abaixo:

<div align="center"><img src="https://i.imgur.com/Y4bUoYf.png" title="source: imgur.com" /></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>