<h1>Projeto 01 - Portfólio - Criar a Página success</h1>

<br />

Agora que concluímos a construção da página **index.html**, vamos focar na construção da página **success.html**.

A **página Success** tem como objetivo **confirmar que o envio do formulário de contato foi realizado com sucesso**, informando ao visitante que a mensagem foi recebida corretamente. Essa página oferece um **retorno claro e imediato**, transmitindo segurança e confiança no processo de comunicação.

Além de confirmar o envio, a página Success melhora a **experiência do usuário**, evitando dúvidas sobre o status da mensagem e reforçando a credibilidade do site, ao demonstrar que o formulário funciona corretamente e que o contato foi registrado com êxito.

<div align="center"><img src="https://i.imgur.com/pprS1rX.png" title="source: imgur.com" /></div>

Como ilustrado na imagem anterior, a construção da página **success** será **simples e objetiva**, reutilizando a **mesma estrutura básica da página index**. Essa padronização garante consistência visual, melhor experiência do usuário e facilita a manutenção do código.

<div align="center"><img src="https://i.imgur.com/Y5g9MgA.png" title="source: imgur.com" /></div>

Na imagem acima, é possível observar os detalhes das **3 seções que compõem a página success**:

- **Header**: mantém o título ou logo do site e o menu de navegação, permitindo que o usuário continue navegando normalmente após o envio do formulário.
- **Main**: exibe a mensagem de **confirmação de sucesso**, informando de forma clara que o e-mail foi enviado corretamente.
- **Footer**: apresenta as informações finais do site, como copyright ou links complementares, mantendo o padrão visual das demais páginas.

Essa abordagem reforça a **coerência do layout**, evita que o usuário se sinta “fora do site” após o envio do formulário e transmite mais **profissionalismo e confiabilidade**.

<br />

<h2>👣 Passo 01 - Criar a Página success</h2>



Vamos criar o arquivo **success.html**, dentro da nossa pasta **portfolio**:

1. Na **Barra Explorer**, selecione a pasta **portfolio** e clique no botão **New File** (Novo Arquivo), indicado na imagem abaixo:  

<div align="center"><img src="https://i.imgur.com/lb077DZ.png" title="source: imgur.com" /></div>

2. O nome do arquivo será **success.html**, como mostra a figura abaixo. Após digitar o nome do arquivo, pressione a tecla **enter** do seu teclado para concluir. 

<div align="center"><img src="https://i.imgur.com/Fq2RCKs.png" title="source: imgur.com" /></div>

<br />

<h2>👣 Passo 02 - Criar a Estrutura Básica da Página success</h2>



Vamos escrever o código da página **success.html** por partes, começando pela estrutura básica, que será a mesma da página **index.html**:

```html
<!DOCTYPE html>
<html lang="pt-br">

<head>

    <meta charset="UTF-8">

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <meta name="author" content="Generation Brasil">
    <meta name="description"
        content="Portfólio da Generation Brasil, desenvolvedor Full Stack especializado em criar experiências digitais incríveis. Conheça meus projetos e entre em contato.">
    <meta name="robots" content="index, follow">

    <link rel="icon" type="image/svg+xml" href="./assets/img/favicon.svg">

    <title>Portfólio Generation Brasil</title>

</head>

<body>

    <!-- Cabeçalho da Página (header) -->
    <header class="menu-container">
        
    </header>

    <!-- Seção Principal da Página (main) -->
    <main>
        
    </main>

    <!-- Rodapé da Página (footer) -->
    <footer>
        
    </footer>

</body>

</html>
```

 <br />

<h2>👣 Passo 03 - Criar a Seção Header da Página success</h2>



Vamos escrever o código da **Seção Header** da página **success.html**, reutilizando a **mesma estrutura da página index.html**, garantindo **padronização visual** e **consistência na navegação** do site.

A principal diferença estará nos **links do menu**, que deverão ser ajustados para apontar corretamente para a página **index.html** e para as respectivas seções, permitindo que o usuário retorne facilmente à página principal após o envio bem-sucedido do formulário.

<div align="center"><img src="https://i.imgur.com/FxAcWOK.png" title="source: imgur.com" /></div>

Vamos entender as **alterações realizadas no código da Seção Header** em relação à página **index.html**:

Embora a estrutura do cabeçalho seja a mesma, alguns **ajustes nos links** são necessários para garantir a navegação correta a partir da página **success.html**:

- **Linha 26:** O **título ou logo** do site passa a apontar para a página **index.html**, permitindo que o usuário retorne facilmente à página principal.
- **Linhas 30 a 33:** Os **links do menu de navegação** deixam de utilizar apenas âncoras internas (como `#about`, `#projects`, entre outras.) e passam a apontar para a **index.html** seguida do identificador da seção (por exemplo, `index.html#about`). Essa adaptação garante que, mesmo estando em outra página, o usuário consiga acessar diretamente qualquer seção do site sem perda de contexto.

Dessa forma, mantemos a **consistência visual**, reaproveitamos o mesmo código de estrutura e asseguramos uma **experiência de navegação fluida e intuitiva** entre as páginas do projeto.

 <br />

<h2>👣 Passo 04 - Criar a Seção Main da Página success</h2>



Vamos escrever o código da **Seção Main** da página **success.html**, utilizando uma **estrutura simples e objetiva**, focada em informar o usuário sobre o **envio bem-sucedido do formulário**.

Diferente da página **index.html**, a Seção Main da página **success** terá como objetivo principal **confirmar a ação realizada**, exibindo uma mensagem clara de sucesso, orientando o usuário sobre os próximos passos e oferecendo uma **opção de retorno à página inicial**.

Observe no wireframe abaixo que a **Seção Main** será composta apenas pela **Seção Success**, responsável por exibir, de forma clara e objetiva, a mensagem de confirmação do envio do e-mail.

<div align="center"><img src="https://i.imgur.com/F9ZxYjD.png" title="source: imgur.com" /></div>

Na imagem acima, é apresentado o wireframe da **Seção Main** da página **success**. Essa seção será composta pelos seguintes elementos:

- Uma tag semântica `<section>`, responsável por definir a **Seção Success** (representada pelo retângulo tracejado vermelho).
- Uma tag `<div>` que envolverá todos os elementos da mensagem de sucesso (retângulo tracejado verde), atuando como um **container** para facilitar a organização e o posicionamento dos elementos com CSS.
- Uma tag semântica `<figure>` (retângulo tracejado azul), responsável por exibir o **ícone de sucesso**.
- Uma tag `<h1>` com a **mensagem principal de sucesso**.
- Uma tag `<h2>` com a **mensagem de agradecimento pelo envio da mensagem**.
- Uma tag de link `<a>`, que fornecerá a **opção de retorno para a página index**, e que posteriormente será estilizada com CSS para se comportar como um **botão**.

<div align="center"><img src="https://i.imgur.com/3xPthvR.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linhas 43 a 59:** Criamos a **Seção Success** com a tag `<section>` e a classe `success-container`, responsável por envolver toda a área de mensagem de sucesso.

**Linhas 44 a 58:** Inserimos um contêiner semântico com a tag `<article>` e a classe `success-content`, que agrupa todos os elementos da mensagem exibida ao usuário.

**Linhas 46 a 51:** Utilizamos a tag semântica `<figure>` com a classe `success-icon`, responsável por agrupar a imagem ilustrativa da mensagem de sucesso.

**Linhas 47 a 50:** Inserimos a imagem com a tag `<img>`, definindo o caminho do arquivo no atributo `src`, o significado da ilustração no atributo `alt`, e aplicamos a classe `float-animation`, para adicionar um efeito de flutuação na imagem.

**Linha 53:** Inserimos um título principal com a tag `<h1>`, informando de forma clara que o e-mail foi enviado com sucesso.

**Linha 54:** Inserimos um subtítulo com a tag `<h2>`, complementando a mensagem principal. A tag `<br>` é utilizada para forçar a quebra de linha e melhorar a legibilidade do texto.

**Linha 56:** Inserimos um link estilizado como botão por meio da tag `<a>` e da classe `botao`. O atributo `href="index.html"` direciona o usuário de volta para a página inicial do site.

 <br />

<h2>👣 Passo 05 - Criar a Seção Footer da Página success</h2>



Vamos escrever o código da **Seção Footer** da página **success.html**, reutilizando a **mesma estrutura da página index.html**, garantindo **padronização visual** e **consistência na navegação** do site.

<div align="center"><img src="https://i.imgur.com/Y069Qxv.png" title="source: imgur.com" /></div>

 <br />

<h2>👣 Passo 06 - Visualizar a Página success no Navegador</h2>



1. Para visualizar a Página **success.html** no Navegador, na **Barra Explorer**, clique com o botão direito do mouse sobre a página success.html e no menu que será aberto, clique na opção **Open with Live Server**:

<div align="center"><img src="https://i.imgur.com/7dk0lIJ.png" title="source: imgur.com" /></div>

2. A Página **success** será aberta no Navegador WEB padrão da sua Máquina:

<div align="center"><img src="https://i.imgur.com/EUD5Gaw.png" title="source: imgur.com" />

Nesse momento, os elementos da **Página success** aparecerão **sem nenhum tipo de estilização**, exibindo apenas a estrutura definida em HTML.

<br />

> [!WARNING]
>
> Para que todos os elementos ficassem visíveis na imagem acima, o tamanho da imagem foi ajustado. Ao abrir o arquivo `index.html` no navegador, a imagem da Seção Success será exibida em seu tamanho original, ocupando toda a largura da página.

<br />

Quando a **Página success** for estilizada com **CSS**, ela passará a ter a aparência ilustrada na imagem abaixo:

<div align="center"><img src="https://i.imgur.com/DkZ7pB8.png" title="source: imgur.com" />

<br />

<div align="left"><img src="https://i.imgur.com/JACNZiR.png" title="source: imgur.com" width="5%"/> <a href="https://github.com/rafaelq80/portfolio_fundweb_2026/tree/01_html" target="_blank"><b>Código fonte do projeto - HTML</b></a></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>