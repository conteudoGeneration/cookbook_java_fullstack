<h1>Projeto 01 - Portfólio - Criar a Página index - Seção Projects</h1>



Agora que concluímos a **estrutura da Seção About**, vamos focar na construção da **Seção Projects**.

<div align="center"><img src="https://i.imgur.com/JCN7McZ.png" title="source: imgur.com" /></div>

A **Seção Projects** tem como objetivo **apresentar os projetos desenvolvidos pelo autor**, destacando suas principais **habilidades técnicas**, **experiências práticas** e **áreas de atuação**. Essa seção permite que o visitante visualize, de forma objetiva, o que já foi construído, fortalecendo a **credibilidade profissional** e demonstrando a aplicação prática dos conhecimentos.

Observe no wireframe abaixo como a **Seção Projects** é organizada, evidenciando a estrutura de **cards dentro de um  carrossel**, que será utilizada para exibir cada projeto de maneira clara, organizada e visualmente atraente.

<div align="center"><img src="https://i.imgur.com/wqpEgV1.png" title="source: imgur.com" /></div>

A **Seção Projects** será estruturada para apresentar os projetos de forma organizada, visual e dinâmica, utilizando um **carrossel de cards**. A seguir, estão descritos os elementos que compõem essa seção e a função de cada um:

<br />

<h3>1.1. Estrutura da Seção Projects</h3>



- Uma tag **`<h2>`**, responsável por exibir o **título da seção**.
- Uma tag **`<div>`** (retângulo vermelho tracejado), que funcionará como **container principal do carrossel**, reunindo todos os elementos necessários para a navegação.
- Dentro desse container, haverá **três `<div>` adicionais** (retângulos cinza tracejados), utilizadas pela **biblioteca Swiper JS** para posicionar:
  - Botão de navegação **anterior**
  - Botão de navegação **próximo**
  - Indicadores de navegação (**dots**)
- Uma tag **`<div>`** (retângulo azul tracejado), que conterá o **carrossel propriamente dito**.
- Uma tag **`<div>`** (retângulo verde tracejado), responsável por envolver os **slides do carrossel**, ou seja, os **cards dos projetos**.
- Uma tag semântica **`<article>`**, utilizada para construir o **card de um projeto**. Apenas um card será criado manualmente, pois os demais serão gerados **dinamicamente** pelo JavaScript em conjunto com a biblioteca Swiper JS.

<br />

> [!IMPORTANT]
>
> Os **elementos de navegação do carrossel** (botões de próximo e anterior, bem como os indicadores de navegação) serão **gerados automaticamente pela biblioteca Swiper JS** e **customizados posteriormente com CSS**, permitindo total controle visual sem comprometer a funcionalidade.
>
> No HTML, iremos apenas indicar onde estes elementos ficarão na estrutura da página.

<br />

<h3>1.2. Estrutura interna do Card de Projeto</h3>



<div align="center"><img src="https://i.imgur.com/WZnjGSX.png" title="source: imgur.com" /></div>

Dentro de cada **`<article>`**, teremos:

- Uma tag semântica **`<figure>`** (retângulo marrom tracejado), responsável por exibir o **logo da principal linguagem ou tecnologia utilizada no projeto**.
- Uma tag **`<div>`** (retângulo roxo tracejado), que conterá as **informações do projeto**.

Dentro dessa `<div>` de informações, teremos:

- Uma tag **`<h3>`** com o **título do projeto**.
- Uma tag **`<p>`** com a **descrição do projeto**.
- Uma tag **`<div>`** (retângulo laranja tracejado), que conterá as **palavras-chave do projeto** (tags do GitHub), construídas dinamicamente com a tag **`<span>`**.
- Uma tag **`<div>`** (retângulo roxo tracejado), que conterá os **links do projeto**, os quais serão posteriormente estilizados com CSS para se comportarem como **botões**.

Para construir a estrutura básica da **Seção Projects**, utilizaremos as **tags estruturais do HTML**, a tag **`<img>`** para exibição das imagens e as tags de **texto** e **links**, garantindo uma base semântica, organizada e fácil de manter.

<br />

> [!IMPORTANT]
>
> Os **dados dos projetos não serão definidos manualmente no HTML**. Todas as informações — como título, descrição, tecnologias e links — serão **obtidas diretamente do GitHub**, garantindo que os projetos exibidos estejam sempre atualizados.
>
> A partir desses dados, os **cards de projetos e todo o seu conteúdo serão construídos dinamicamente via JavaScript**, eliminando a necessidade de manutenção manual sempre que um novo projeto for adicionado ou atualizado.
>
> Inicialmente, iremos construir apenas um card, que servirá de base para gerar os demais, através de código JavaScript.


<br />

<h2>👣 Passo 01 - Criar a Estrutura da Seção Projects na Página index.html</h2>



Vamos atualizar o código do arquivo **index.html**, inserindo **o conteúdo da Seção Projects**, **dentro da tag `<section id="projects">`**, que foi criada no interior da tag `<main>`.

Com isso, a estrutura completa dessa seção ficará definida, restando apenas a aplicação dos estilos com CSS nos próximos passos.

<div align="center"><img src="https://i.imgur.com/6aqNI10.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linhas 117 a 160:** A tag `<section>` foi atualizada com as classes `section-container` e `projects-container` para estilização.

**Linha 118:** Inserimos um título de seção através da tag `<h2>`, indicando que o conteúdo a seguir apresenta os projetos desenvolvidos.

**Linhas 121 a 159:** Criamos o contêiner principal do Swiper com a tag `<div>` e as classes `swiper` e `projects-swiper`. Esse elemento será controlado pela biblioteca **Swiper** para criar o carrossel de projetos.

**Linhas 122 a 151:** Inserimos o contêiner `<div class="swiper-wrapper">`, obrigatório no Swiper, responsável por agrupar todos os slides.

**Linhas 123 a 150:** Criamos um slide individual com a tag `<div class="swiper-slide">`, que representa um item do carrossel.

**Linhas 125 a 149:** Inserimos um `<article>` com a classe `project-card`, representando um card individual de projeto.

**Linhas 126 a 129:** Utilizamos a tag semântica `<figure>` com a classe `project-image` para agrupar a imagem do projeto.

**Linhas 127 a 128:** Inserimos a imagem com a tag `<img>`, definindo o ícone do GitHub no atributo `src` e uma descrição acessível no atributo `alt`.

**Linhas 131 a 147:** Criamos um contêiner com a classe `project-content`, responsável por agrupar as informações textuais do projeto.

**Linha 132:** Inserimos um título de menor hierarquia com a tag `<h3>`, indicando o nome do projeto.

**Linha 133:** Inserimos um parágrafo com a tag `<p>`, descrevendo brevemente o projeto apresentado.

**Linhas 135 a 137:** Criamos um contêiner com a classe `project-tags`, utilizado para exibir as tecnologias ou categorias do projeto.

**Linha 136:** Inserimos uma tag com a classe `tag`, representando uma tecnologia associada ao projeto — neste caso, GitHub.

**Linhas 139 a 146:** Criamos um contêiner com a classe `project-buttons`, responsável por agrupar os botões de ação do projeto.

**Linhas 140 a 142:** Inserimos um link estilizado como botão com as classes `botao` e `botao-sm`, que direciona para o repositório do projeto no GitHub, abrindo em uma nova aba com `target="_blank"`. A classe `botao-sm` tem o objetivo de ajustar o tamanho do botão para ser utilizado no card de projetos.

**Linhas 143 a 145:** Inserimos um segundo link estilizado como botão com as classes `botao-outline` e `botao-sm`, destinado ao acesso ao deploy do projeto. A classe `botao-sm` tem o objetivo de ajustar o tamanho do botão para ser utilizado no card de projetos.

**Linha 154:** Inserimos o botão de navegação “próximo” do Swiper com a classe `swiper-button-next`.

**Linha 155:** Inserimos o botão de navegação “anterior” do Swiper com a classe `swiper-button-prev`.

**Linha 158:** Inserimos o contêiner de paginação com a classe `swiper-pagination`, responsável por exibir os indicadores de navegação do carrossel.

<br />

<h2>👣 Passo 02 - Visualizar a Página index no Navegador</h2>



1. Volte para o **navegador** e observe que a **estrutura da Seção Projects** já estará visível no final da sua **página HTML**:

<div align="center"><img src="https://i.imgur.com/jtng6D3.png" title="source: imgur.com" /></div>

Nesse momento, os elementos da **Seção Projects** aparecerão na página **sem nenhum tipo de estilização**, exibindo apenas a estrutura definida em HTML.

<br />

> [!WARNING]
>
> Para que todos os elementos ficassem visíveis na imagem acima, o tamanho da imagem foi ajustado. Ao abrir o arquivo `index.html` no navegador, a imagem da Seção Projects será exibida em seu tamanho original, ocupando toda a largura da página.

<br />

Quando a **Seção Projects** for estilizada com **CSS**, ela passará a ter a aparência ilustrada na imagem abaixo, com apenas um card, ocupando toda a área disponível:

<div align="center"><img src="https://i.imgur.com/EI40f5r.png" title="source: imgur.com" /></div>

Após a implementação do **Carrossel de Projetos** utilizando a **Biblioteca Swiper JS**, a **Seção Projects** terá um comportamento dinâmico e interativo, permitindo a navegação entre os projetos por meio de botões e indicadores visuais. O resultado final será semelhante ao apresentado na imagem abaixo:

<div align="center"><img src="https://i.imgur.com/4kSvjz0.png" title="source: imgur.com" /></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>