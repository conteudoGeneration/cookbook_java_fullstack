<h1>Projeto 01 - Portfólio - Criar a Página index - Seção Main</h1>



Agora que já concluímos a **estrutura da Seção Header**, vamos focar na construção da **Seção Main** da página. Observe a imagem abaixo:

<div align="center"><img src="https://i.imgur.com/PgKPo4P.png" title="source: imgur.com" /></div>

A **Seção Main** da nossa página será composta pelas seguintes áreas:

- Uma tag semântica `<section>` chamada **Hero**, que será a **primeira seção** da `<main>`, responsável por dar as boas-vindas ao usuário.
- Uma tag semântica `<section>` chamada **About**, que será a **segunda seção** da `<main>`, destinada a apresentar informações sobre o autor da página.
- Uma tag semântica `<section>` chamada **Projects**, que será a **terceira seção** da `<main>`, contendo um carrossel de cards para apresentar os projetos desenvolvidos pelo autor.
- Uma tag semântica `<section>` chamada **Contact**, que será a **quarta seção** da `<main>`, reunindo os links para as redes sociais do autor e um formulário de contato para envio de mensagens para o e-mail do autor.

Para construir a **estrutura básica da Seção Main**, utilizaremos as **tags estruturais e semânticas do HTML**, que ajudam a organizar o conteúdo, melhorar a acessibilidade e tornar o código mais claro e fácil de manter.

<br />

<h2>👣 Passo 01 - Inserir a Estrutura da Seção Main da Página index.html</h2>



Primeiro vamos construir a estrutura básica das seções da **Seção Main** e depois vamos adicionar os conteúdos internos. Vamos atualizar o código do arquivo **index.html**, inserindo o trecho de código abaixo dentro da tag `<main>`, que foi criada dentro da tag `<body>`:

<div align="center"><img src="https://i.imgur.com/OKGan4F.png" title="source: imgur.com" /></div>

**Linhas 47 a 49:** foi utilizada a tag `<section>` para identificar a **Seção Hero**, que funciona como o container responsável por exibir o conteúdo de **boas-vindas** ao usuário.

**Linhas 52 a 54:** foi utilizada a tag `<section>` para identificar a **Seção About**, que será o container onde será inserido o conteúdo com informações sobre o **autor da página**.

**Linhas 57 a 59:** foi utilizada a tag `<section>` para identificar a **Seção Projects**, que funcionará como o container destinado à **lista de projetos** desenvolvidos pelo autor.

**Linhas 62 a 64:** foi utilizada a tag `<section>` para identificar a **Seção Contact**, que será o container responsável por agrupar os **links para as redes sociais** e o **formulário de contato**.

Agora que a **estrutura básica da Seção Main** está pronta, vamos começar a organizar cada uma das **seções internas**.

Na imagem abaixo, é possível visualizar o **wireframe completo da Seção Main**, mostrando em detalhes a estrutura das **quatro seções internas** e como cada área do conteúdo será posicionada dentro da página:

<div align="center"><img src="https://i.imgur.com/53mALwe.png" title="source: imgur.com" /></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>
