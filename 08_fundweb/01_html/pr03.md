<h1>Projeto 01 - Portfólio - Criar a Página index - Seção Header</h1>



Agora que já temos a **estrutura básica da página index**, vamos iniciar a construção de cada uma das **três seções principais** da página, começando pela **Seção Header**.

<div align="center"><img src="https://i.imgur.com/pprS1rX.png" title="source: imgur.com" /></div>

A **Seção Header** tem como objetivo **apresentar a identidade principal do site** e **facilitar a navegação do usuário**, reunindo o **título ou logotipo** da página e o **menu de navegação**. Essa seção é fundamental para orientar o visitante desde o primeiro contato, deixando claro onde ele está e como pode acessar as demais áreas do site.

Por meio do Header, o usuário pode **navegar rapidamente entre as seções**, utilizando o menu para acessar conteúdos como *Hero*, *About*, *Projects* e *Contact*, garantindo uma experiência de navegação intuitiva e organizada.

Observe no wireframe abaixo como a **Seção Header** é estruturada, destacando a disposição do **título ou logo** e das **opções do menu de navegação**, pensadas para oferecer clareza, usabilidade e consistência visual em todo o site.

<div align="center"><img src="https://i.imgur.com/mxLa3o2.png" title="source: imgur.com" /></div>

Na imagem acima, é apresentado o **wireframe da estrutura do cabeçalho** da página index, ou seja, o conteúdo da **Seção Header**. Essa seção será composta pelos seguintes elementos:

- Uma `<div>` responsável por envolver todos os elementos do cabeçalho (retângulo azul tracejado). Essa `<div>` funcionará como um **container**, facilitando o posicionamento e a organização dos elementos quando aplicarmos o CSS.
- Um link `<a>` chamado **titulo** (retângulo verde tracejado), que exibirá o título da página (**Portfólio**) e apontará para o arquivo `index.html`.
- Um elemento `<nav>` (retângulo vermelho tracejado), que conterá o **menu de navegação** do site.
- As opções do menu serão criadas por meio de uma **lista não ordenada** (`<ul>`), onde cada item da lista será envolvido por uma tag de link (`<a>`). Esses links apontarão para as diferentes seções que serão criadas dentro do elemento `<main>`, permitindo a navegação dentro da página.

Para construir essa estrutura inicial da **Seção Header**, utilizaremos as **tags estruturais do HTML**, que ajudam a organizar o conteúdo de forma semântica, tornando o código mais legível, acessível e alinhado às boas práticas do desenvolvimento web.

<br />

<h2>👣 Passo 01 - Criar a Estrutura do Cabeçalho Página index.html</h2>



Vamos iniciar a construção da **estrutura da Seção Header**, neste primeiro momento **sem adicionar os conteúdos internos** (como textos e links).

Para isso, vamos atualizar o código do arquivo **index.html**, inserindo o trecho de código abaixo **na tag `<header>`**, que já foi criada no interior da tag `<body>`.

<div align="center"><img src="https://i.imgur.com/mP8MfTO.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linhas 22 a 41:** a tag `<header>` define o **cabeçalho da página**, sendo responsável por agrupar os elementos introdutórios e de navegação, como o **logo ou título principal** e o **menu de opções**. Essa estrutura reforça a **semântica do HTML**, melhora a acessibilidade e facilita a organização visual do layout.

**Linhas 24 a 39:** foi adicionada uma tag `<div>` para **agrupar todo o conteúdo do cabeçalho**. Essa `<div>` atua como um **container interno** da seção `<header>`, centralizando e organizando os elementos do menu. A classe **`menu-content`**, definida no atributo **`class`**, será utilizada posteriormente no CSS para **controlar alinhamento, espaçamentos e posicionamento** dos itens do cabeçalho.

> ### **Atributos `class` e `style`**
>
> Existem duas formas principais de aplicar **estilização CSS** a um elemento HTML:
>
> - **`class`**: atributo utilizado para **identificar e agrupar elementos HTML**, permitindo aplicar estilos e comportamentos de forma **reutilizável** em vários elementos ao mesmo tempo.
> - **`style`**: atributo utilizado para aplicar **estilos diretamente no elemento HTML**, de forma pontual, por meio de regras CSS escritas inline.
>
> Como iremos criar um **arquivo CSS externo** para centralizar todos os estilos do site, utilizaremos o atributo **`class`** para aplicar as classes aos elementos, seguindo as boas práticas de organização e manutenção do código.

**Linha 27:** foi adicionada uma tag de link `<a>` responsável por exibir o **título do cabeçalho** da página.

**Linhas 30 a 37:** foi adicionada a tag `<nav>`, que representa semanticamente a área de **navegação** do site e contém o menu do cabeçalho.

**Linhas 31 a 36:** dentro da tag `<nav>`, foi criada uma **lista não ordenada** utilizando as tags `<ul>` e `<li>`. A tag `<ul>` recebeu a classe **menu-list**, que será utilizada posteriormente na **estilização** e no **posicionamento** dos itens do menu.

- Cada item da lista foi envolvido por uma tag de link (`<a>`), formando as **opções do menu do cabeçalho**.
- Os links já estão configurados para apontar para as seções que serão criadas dentro do elemento `<main>` da página, facilitando a navegação interna do site.

**Exemplo - Link para um elemento da própria página:**

 `<a href="#about">`

Nesse caso, **#about** é o **identificador (`id`)** da **Seção Sobre**. Ao clicar nesse link, a página será **rolada automaticamente** até que a seção com o `id="about"` seja exibida.

> **Por que utilizamos listas não ordenadas para criar os itens do menu?**
>
> No desenvolvimento web, a **lista não ordenada** (`<ul>`) é a forma mais comum e recomendada para construir menus de navegação, especialmente o **menu principal** de um site. Isso ocorre porque listas representam naturalmente um conjunto de itens relacionados e organizados hierarquicamente, permitindo inclusive a criação de **submenus** (listas dentro de listas).
>
> Além disso, o **HTML5** introduziu o elemento `<nav>`, que possui um significado **semântico** específico para áreas de navegação. Quando combinamos `<nav>` com `<ul>` e `<li>`, o código se torna mais claro, organizado e fácil de entender, tanto para navegadores quanto para outras pessoas desenvolvedoras.
>
> Embora seja possível usar uma `<div>` para agrupar links e estilizar o menu com CSS, essa prática não é a mais indicada. O uso inadequado de `<div>` pode comprometer a semântica do código e dificultar a manutenção futura.
>
> Por esse motivo, as **boas práticas de desenvolvimento web** recomendam utilizar a tag `<ul>` para criar as opções de menu, salvo em situações específicas que justifiquem uma abordagem diferente.

<br />

<h2>👣 Passo 02 - Visualizar a Página index no Navegador</h2>



1. Volte para o Navegador e observe que a estrutura do cabeçalho foi adicionada na Página index:

<div align="center"><img src="https://i.imgur.com/dhPg7cw.png" title="source: imgur.com" /></div>

Por enquanto, temos apenas os **links criados**, pois estamos construindo somente a **estrutura** da página.
Quando essa seção for **estilizada com CSS**, o cabeçalho da página **index** passará a ter a aparência ilustrada na imagem abaixo.

<div align="center"><img src="https://i.imgur.com/W42MyoD.png" title="source: imgur.com" /></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>
