<h1>Projeto 01 - Portfólio - Criar a Página index - Seção Contact</h1>



Agora que já temos a estrutura **Seção Projects** finalizada, vamos nos concentrar na estrutura da **Seção Contact**. 

<div align="center"><img src="https://i.imgur.com/b02uJm0.png" title="source: imgur.com" /></div>

A **Seção Contact** tem como objetivo **facilitar o contato entre o visitante e o autor do site**, reunindo em um único espaço os **links para as redes sociais** e um **formulário de contato**. Essa seção é fundamental para incentivar conexões profissionais, oportunidades de networking e possíveis propostas de trabalho.

Por meio dessa seção, o visitante pode escolher a forma de contato mais adequada, seja acessando diretamente as redes sociais ou enviando uma mensagem de forma rápida e objetiva pelo formulário.

Observe no wireframe abaixo como a **Seção Contact** é organizada, evidenciando a disposição dos **ícones das redes sociais** e do **formulário**, pensados para oferecer uma experiência clara, acessível e intuitiva ao usuário.

<div align="center"><img src="https://i.imgur.com/IzKqcUV.png" title="source: imgur.com" /></div>

A **Seção Contact** será composta pelos seguintes elementos estruturais:

- Uma tag semântica **`<article>`**, chamada **redes_sociais** (retângulo vermelho tracejado), que conterá o **texto introdutório da seção** e os **links para as redes sociais** do autor.
- Uma tag **`<div>`** (retângulo azul tracejado), responsável por **agrupar os links das redes sociais**, facilitando a organização e a estilização dos ícones via CSS.
- Uma tag **`<form>`** (retângulo verde tracejado), que conterá o **formulário de contato**, permitindo que o visitante envie uma mensagem diretamente para o e-mail do autor do site.

Para construir a estrutura básica da **Seção Contact**, utilizaremos as **Tags Estruturais do HTML**, em conjunto com as **tags específicas de formulário**, garantindo uma organização semântica adequada e preparando o layout para a estilização com CSS.

<br />

<h2>👣 Passo 01 - Criar a Estrutura da Seção Contact na Página index.html</h2>



Vamos atualizar o código do arquivo **index.html**, inserindo **o conteúdo da Seção Contact**, **dentro da tag `<section id="contact">`**, que foi criada no interior da tag `<main>`.

Com isso, a estrutura completa dessa seção ficará definida, restando apenas a aplicação dos estilos com CSS nos próximos passos.

<div align="center"><img src="https://i.imgur.com/E3BDtQk.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linhas 163 a 204:** A tag `<section>` foi atualizada com as classes `section-container` e `contact-container` para estilização.

<br />

<h3>Redes Sociais</h3>



**Linhas 166 a 181:** Criamos um contêiner semântico com a tag `<article>` e a classe `contact-info`, responsável por agrupar as informações de contato e as redes sociais.

**Linha 167:** Inserimos um título de seção através da tag `<h2>`, indicando ao usuário que este é o espaço de contato.

**Linha 168:** Inserimos um parágrafo de texto com a tag `<p>`, convidando o usuário a entrar em contato de forma clara e objetiva.

**Linhas 170 a 180:** Criamos um contêiner com a classe `social-container`, responsável por agrupar os ícones das redes sociais.

**Linhas 171 a 173:** Inserimos um link com a tag `<a>`, estilizado como ícone social através da classe `social-icon`.
O atributo `aria-label` é utilizado para melhorar a acessibilidade, descrevendo o destino do link para leitores de tela. 

**Linha 172:** Inserimos o ícone com o logo do LinkedIn com a tag `<img>`, definindo o ícone no atributo `src` e uma descrição acessível no atributo `alt`.

**Linhas 174 a 176:** Repetimos a estrutura para o link do GitHub, mantendo o mesmo padrão de acessibilidade e organização.

**Linhas 177 a 179:** Repetimos novamente a estrutura para o link do Instagram.

> [!TIP]
>
> Para adicionar links para **outras redes sociais**, basta **inserir um novo ícone e o respectivo link**, seguindo o **mesmo padrão utilizado nas demais Redes Sociais**.
> Dessa forma, a estrutura permanece organizada, consistente e fácil de manter, permitindo expandir a seção sempre que necessário sem alterações complexas no código.

<br />

<h3>Formulário de Contato</h3>



**Linhas 184 a 203:** Criamos o formulário com a tag `<form>` e o `id="formulario"`, que pode ser utilizado posteriormente para manipulação e validação via JavaScript.

**Linha 186:** Inserimos um rótulo com a tag `<label>` associado ao campo nome por meio do atributo `for`.

**Linha 187:** Inserimos um campo de entrada de texto com a tag `<input>`, onde o usuário poderá digitar seu nome completo.

**Linha 188:** Inserimos uma tag `<span>` destinada à exibição de mensagens de erro relacionadas a validação do campo nome.

**Linha 190:** Inserimos o rótulo do campo e-mail.

**Linha 191:** Inserimos o campo de entrada para o e-mail do usuário.

**Linha 192:** Inserimos um `<span>` para exibição de mensagens de erro de validação do campo e-mail.

**Linha 194:** Inserimos o rótulo do campo assunto.

**Linha 195:** Inserimos o campo de entrada para o assunto da mensagem.

**Linha 196:** Inserimos um `<span>` para exibição de mensagens de erro de validação do campo assunto.

**Linha 198:** Inserimos o rótulo do campo mensagem.

**Linha 199:** Inserimos um campo de texto multilinha com a tag `<textarea>`, permitindo que o usuário escreva mensagens mais longas.

**Linha 200:** Inserimos um `<span>` para exibição de mensagens de erro de validação do campo mensagem.

**Linha 202:** Inserimos um botão com a tag `<button>` e a classe `botao`, responsável por enviar o formulário quando clicado.

<br />

<h2>👣 Passo 02 - Visualizar a Página index no Navegador</h2>



1. Volte para o **navegador** e observe que a **estrutura da Seção Contact** já estará visível no final da sua **página HTML**:

<div align="center"><img src="https://i.imgur.com/lX9JWcT.png" title="source: imgur.com" /></div>

Nesse momento, os elementos da **Seção Contact** aparecerão na página **sem nenhum tipo de estilização**, exibindo apenas a estrutura definida em HTML.

Quando a **Seção Contact** for estilizada com **CSS**, ela passará a ter a aparência ilustrada na imagem abaixo:

<div align="center"><img src="https://i.imgur.com/1kKWgtR.png" title="source: imgur.com" /></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>