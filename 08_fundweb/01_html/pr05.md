<h1>Projeto 01 - Portfólio - Criar a Página index - Seção Hero</h1>



Agora que concluímos a **estrutura da Seção Main**, vamos focar na construção de suas **subseções**, começando pela **Seção Hero**.

<div align="center"><img src="https://i.imgur.com/ObQ0sO8.png" title="source: imgur.com" /></div>

A **Seção Hero** é a **primeira área visível de uma página web** e tem como objetivo receber o usuário com uma mensagem de boas-vindas ou apresentar o conteúdo principal do site de forma destacada. Ela é essencial para causar uma **primeira impressão impactante** e direcionar a navegação do usuário para outras partes do site.

Observe o wireframe da Seção Hero abaixo:

<div align="center"><img src="https://i.imgur.com/LMdxAS1.png" title="source: imgur.com" /></div>

A **Seção Hero**, inserida dentro da Seção Main, será composta por:

- Uma tag semântica `<article>` (retângulo verde tracejado), que conterá o **texto de boas-vindas** e links para as seções **Contact** e **About**, que posteriormente serão estilizados com CSS para se comportarem como botões.
- Uma tag semântica `<figure>` (retângulo vermelho tracejado), que exibirá a **imagem principal** da Seção Hero.

Para construir a estrutura da Seção Hero, utilizaremos as **tags estruturais do HTML**, garantindo que o conteúdo fique semântico, organizado e pronto para receber estilos visuais. Além disso, utilizaremos tags de link, imagem e texto.

<br />

<h2>👣 Passo 02 - Criar a Estrutura da Seção Hero na Página index.html</h2>



Vamos atualizar o código do arquivo **index.html**, inserindo **o conteúdo da Seção Hero**, **dentro da tag `<section id="hero">`**, que foi criada no interior da tag `<main>`.

Com isso, a estrutura completa dessa seção ficará definida, restando apenas a aplicação dos estilos com CSS nos próximos passos.

<div align="center"><img src="https://i.imgur.com/olcwfIR.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linhas 47 a 70:** A tag `<section>` foi atualizada com as classes **section-container** e **hero-container**, definidas por meio do atributo `class`. Essas classes serão utilizadas posteriormente na estilização da seção Hero com CSS.

**Linhas 50 a 61:** Criamos um contêiner semântico com a tag `<article>` e a classe `hero-content`, responsável por agrupar todo o conteúdo textual da seção Hero.

**Linha 52:** Inserimos o texto de boas vindas através da tag `<span>`. Além do texto, inserimos um ícone visual (👋), tornando a apresentação mais amigável.

> [!TIP]
>
> Personalize a **Seção Hero** substituindo o título **Generation Brasil** pelo **seu nome**, deixando a página com a sua identidade.

**Linha 53:** Inserimos o Título Principal da página e da seção por meio da tag `<h1>`, destacando o cargo ou área de atuação — neste caso, *Desenvolvedor Full Stack*.

> **A importância da Hierarquia de Títulos (h1 a h6)**
>
> A hierarquia dos títulos **h1 a h6** organiza o conteúdo da página de forma lógica e consistente, facilitando a **leitura**, a **acessibilidade** (especialmente para leitores de tela) e o **SEO**, pois ajuda navegadores e mecanismos de busca a compreenderem a estrutura e a relevância de cada seção.
>
> No HTML, essa hierarquia funciona de maneira muito semelhante aos **estilos de títulos dos editores de texto**, como o Word:
>
> - **h1** → equivale ao **Título 1**: representa o título principal da página.
> - **h2** → equivale ao **Título 2**: define as seções principais do conteúdo.
> - **h3** → equivale ao **Título 3**: indica subseções dentro de uma seção.
> - **h4 a h6** → correspondem a níveis cada vez mais específicos, usados para detalhar o conteúdo.
>
> Assim como nos editores de texto, essa organização cria uma **estrutura clara e hierárquica**, melhora a **navegação** pela página e torna o conteúdo mais compreensível tanto para pessoas quanto para ferramentas automáticas.

**Linha 54:** Inserimos um parágrafo de texto com a tag `<p>`, utilizado para apresentar uma breve descrição profissional de forma objetiva.

**Linhas 56 a 59:** Criamos um contêiner com a tag `<div>` e a classe `buttons-container`, que agrupa os botões de ação da seção.

**Linha 57:** Inserimos um link estilizado como botão através da tag `<a>` com a classe `botao`. O atributo `href="#contact"` direciona o usuário para a seção de contato da página.

**Linha 58:** Inserimos um segundo link estilizado como botão, utilizando a classe `botao-outline`. Esse botão direciona o usuário para a seção “Sobre”, indicada pelo `href="#about"`.

**Linhas 64 a 68:** Utilizamos a tag semântica `<figure>` com a classe `hero-image`, destinada a agrupar e contextualizar a imagem da seção Hero.

**Linha 65:** Inserimos a imagem com a tag `<img>`, utilizando o atributo `src` para definir o caminho da foto principal da seção Hero.

**Linha 66:** Definimos o atributo `alt`, essencial para acessibilidade, descrevendo o conteúdo da imagem para leitores de tela.

**Linha 67:** Aplicamos a classe `float-animation`, que será utilizada para adicionar uma animação visual via CSS, de flutuação.

> [!TIP]
>
> Para a imagem da **Seção Hero**, você pode utilizar uma foto de sua preferência ou optar por gerar uma **ilustração no estilo Studio Ghibli** a partir de uma foto real.
>
> Utilizando uma ferramenta de **geração de imagens com IA**, basta enviar a sua foto e solicitar a **transformação para o estilo Ghibli**, descrevendo um **ambiente de trabalho em Home Office**, com computador, mesa organizada, plantas e uma atmosfera acolhedora e criativa. Veja o exemplo abaixo:
>
> | <img src="https://i.imgur.com/CA66Fw7.png" title="source: imgur.com"/> | <img src="https://i.imgur.com/aHMaaeT.png" title="source: imgur.com" /> |
> | :----------------------------------------------------------: | :----------------------------------------------------------: |
> |         *Imagem Gerada por Inteligência Artificial*          |         *Imagem Gerada por Inteligência Artificial*          |
>
> Esse tipo de imagem ajuda a transmitir personalidade, profissionalismo e proximidade logo no primeiro contato com a página.

<br />

<h2>👣 Passo 02 - Visualizar a Página index no Navegador</h2>



1. Volte para o **navegador** e observe que a **estrutura da Seção Hero** já estará visível na sua **página HTML**:

<div align="center"><img src="https://i.imgur.com/I1fzfKD.png" title="source: imgur.com" /></div>

Nesse momento, os elementos da **Seção Hero** aparecerão na página **sem nenhum tipo de estilização**, exibindo apenas a estrutura definida em HTML.

<br />

> [!WARNING]
>
> Para que todos os elementos ficassem visíveis na imagem acima, o tamanho da imagem foi ajustado. Ao abrir o arquivo `index.html` no navegador, a imagem da Seção Hero será exibida em seu tamanho original, ocupando toda a largura da página.

<br />

Quando a **Seção Hero** for estilizada com **CSS**, ela passará a ter a aparência ilustrada na imagem abaixo:

<div align="center"><img src="https://i.imgur.com/0gG5lPX.png" title="source: imgur.com" /></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>