<h1>Expressões Regulares</h1>



Em nossa trajetória como pessoas desenvolvedoras, **lidaremos com Expressões Regulares em algum momento**. Seja em tarefas comuns, como **validar e-mails, datas ou números de telefone**, ou em cenários mais avançados, como realizar **parse de textos**, extrair informações ou identificar padrões específicos em grandes volumes de dados.

As **Expressões Regulares** (*Regular Expressions*, ou **regex**) são **padrões formais** utilizados para identificar, validar, extrair ou substituir trechos específicos dentro de uma string. Elas definem **regras de combinação de caracteres**, permitindo verificar se um texto segue determinado formato de maneira precisa e automatizada.

Atualmente, **todas as linguagens de programação de alto nível** — como JavaScript, Python, Java, C#, PHP, Ruby e outras — oferecem suporte nativo a expressões regulares, o que reforça sua importância como uma **ferramenta essencial e transversal** no desenvolvimento de software, independentemente da tecnologia ou da plataforma utilizada.

<br />

<h3>1.1. Principais usos de Regex em JavaScript</h3>



No JavaScript, expressões regulares são muito utilizadas para:

- **Validação de dados:** Verificar se um valor segue um formato esperado, como e-mail, telefone, CEP, CPF ou CNPJ.
- **Busca e substituição de texto:** Localizar padrões dentro de strings e substituí-los de forma controlada.
- **Processamento e filtragem de informações:** Extrair partes relevantes de textos maiores, como logs, formulários ou arquivos.

<br />

<h3>1.2. Criando nossas primeiras regex</h3>



No **JavaScript**, regex são representadas por objetos da classe `RegExp` e podem ser declaradas de duas formas:

1. Usando um constructor:

```js
const regex = new RegExp("abc");
```

2. Criando de forma literal (opção mais comum):

```js
const regex = /abc/;
```

Podemos buscar por somente uma letra:

```js
const regex = /a/;
```

Ou buscar por uma palavra:

```js
const regex = /casa/;
```

Por padrão, as expressões regulares são *case sensitive*, ou seja, diferenciam letras maiúsculas e minúsculas. 

Além disso, sem o uso de **flags**, a regex retorna apenas a primeira ocorrência encontrada. 

<br />

<h3>1.3. Flags</h3>



As **flags** adicionam comportamentos adicionais às expressões regulares, permitindo controlar a forma como a busca é realizada. Entre as principais flags utilizadas no JavaScript, destacam-se:

- **`g` (global)** — Indica que a regex deve encontrar **todas as ocorrências** do padrão na string, e não apenas a primeira.
- **`i` (ignore case)** — Faz com que a busca **ignore a diferença entre letras maiúsculas e minúsculas**.
- **`m` (multiline)** — Ativa o modo multilinha, fazendo com que os caracteres de início (`^`) e fim (`$`) passem a atuar **linha a linha**, e não apenas no início e fim da string inteira.

**Exemplo:**

```js
const regex = /javascript/gi;
```

No exemplo acima, podemos obter todas as **ocorrências** de um padrão utilizando a flag `g` e, ao combiná-la com a flag `i`, realizar buscas **sem distinção entre maiúsculas e minúsculas**.

Esse uso combinado de flags torna as expressões regulares mais flexíveis e adequadas a diferentes cenários de busca e validação.

<br />

<h3>1.4. Operador Pipe</h3>



Em algumas situações, precisamos que uma regex encontre **mais de um termo possível**. Para isso, utilizamos o **operador pipe (`|`)**, que funciona de forma semelhante ao operador lógico **OR (`||`)**.

```js
const regex = /ver|distrai/gi;
```

No exemplo acima a regex valida as palavras **ver** e **distrai**. O operador `|` permite definir **alternativas explícitas**, tornando a regex mais flexível.

<br />

<h3>1.5. Conjuntos de caracteres</h3>



Os **conjuntos** permitem indicar que **uma única posição** da string pode assumir **vários valores possíveis**.

```js
const regex = /[em²]/gi;
```

Nesse caso, a regex encontra qualquer ocorrência de `e`, `m` ou `²`.

<br />

<h3>1.6. Ranges (intervalos) em conjuntos</h3>



Dentro de **conjuntos de caracteres** (`[ ]`), é possível definir **intervalos** (*ranges*) que representam sequências contínuas de caracteres na tabela ASCII/Unicode.

```js
const regex = /[A-Za-z0-9]/gi;
```

Nesse exemplo, a expressão regular encontra **qualquer letra maiúscula (`A–Z`)**, **letra minúscula (`a–z`)** ou **dígito numérico (`0–9`)**. Como consequência, **caracteres especiais não são correspondidos**, pois não fazem parte do conjunto definido.

Podemos também dar match com letras acentuadas, utilizando ranges Unicode:

```js
const regex = /[À-ü]/gi;
```

Também podemos utilizar **Conjuntos negados**, para fazer match com **qualquer caractere que NÃO pertença ao conjunto definido**. Eles são indicados pelo símbolo `^` no início do conjunto.

```js
const regex = /[^aeiouí]/gi;
```

Neste exemplo, a expressão regular corresponde a **qualquer caractere que não seja uma vogal** (`a, e, i, o, u`) **nem a letra “í” acentuada**, considerando letras maiúsculas e minúsculas devido à flag `i`.

Conjuntos negados são úteis quando o objetivo é **excluir padrões específicos**, como remover vogais de um texto ou identificar caracteres indesejados em uma string.

<br />

<h3>1.7. Metacaracteres</h3>



Regex possui dois tipos de caracteres:

- **Caracteres literais** → representam a si mesmos (`abc123`)
- **Metacaracteres** → possuem funções especiais (`^`, `$`, `.`, `*`, etc.)

<br />

### ✅Ponto



O metacaractere `.` funciona como um **coringa**, correspondendo a qualquer caractere (exceto quebra de linha, por padrão).

```js
const regex = /cas./gi;
```

Nesse exemplo, a regex faz match com qualquer palavra que comece com `cas` e possua **um caractere qualquer logo em seguida**, como:
 `casa`, `caso`, `case`, entre outros.

<br />

### ✅Escape



Para usar um metacaractere de forma literal, utilizamos o **escape `\`**.

```js
const regex = /[a\-o]/gi;
```

Nesse caso, o hífen (`-`) foi escapado para que seja interpretado como um **caractere literal**, e não como um operador de intervalo dentro do conjunto. Assim, a regex faz match apenas com as letras `a`, `-` ou `o`.

O uso correto do escape evita ambiguidades e garante que a regex funcione exatamente conforme o esperado.

<br />

<h3>1.8. Quantificadores</h3>



Quantificadores indicam **quantas vezes** um padrão pode ocorrer:

| Quantificador | Significado  |
| ------------- | ------------ |
| `?`           | zero ou uma  |
| `*`           | zero ou mais |
| `+`           | uma ou mais  |
| `{n,m}`       | entre n e m  |

<br />

```js
const regex = /\d{3}\.?\d{3}\.?\d{3}-?\d{2}/;
```

Neste exemplo, temos uma expressão regular utilizada para **validar o formato de um CPF**.
A regra define a seguinte sequência:

- **3 caracteres numéricos** (`\d{3}`)
- **um ponto literal** (`\.`)
- **mais 3 caracteres numéricos** (`\d{3}`)
- **outro ponto literal** (`\.`)
- **mais 3 caracteres numéricos** (`\d{3}`)
- **um hífen literal** (`-`)
- **e, por fim, 2 caracteres numéricos** (`\d{2}`)

Essa estrutura garante que o CPF esteja no formato padrão: `000.000.000-00`.

Para deixar a regex menor, utilizamos neste exemplo as **Shorthands ou atalhos**, que facilitam e simplificam a escrita:

| Atalho | Equivalente             |
| ------ | ----------------------- |
| `\d`   | `[0-9]`                 |
| `\D`   | Não seja `[0-9]`        |
| `\w`   | `[A-Za-z0-9_]`          |
| `\W`   | Não seja `[A-Za-z0-9_]` |
| `\s`   | espaços em branco       |

<br />

<h3>1.9. Âncoras</h3>



As **âncoras** são utilizadas em expressões regulares para indicar **a posição do texto**, e não um caractere específico.
Elas servem para garantir que a verificação ocorra **no início ou no fim da string**.

As duas âncoras mais comuns são:

| Âncora | Significado      |
| ------ | ---------------- |
| `^`    | Início da string |
| `$`    | Final da string  |

<br />

### ✅Âncora de início `^`



A âncora `^` garante que o padrão comece **no primeiro caractere** da string.

```js
const regex = /^Olá/;

regex.test("Olá mundo");   // true
regex.test("Oi, Olá mundo"); // false
```

Nesse exemplo, o texto só será válido se **começar exatamente com "Olá"**.

<br />

### ✅Âncora de fim `$`



A âncora `$` garante que o padrão termine **no último caractere** da string.

```js
const regex = /mundo$/;

regex.test("Olá mundo"); // true
regex.test("mundo Olá"); // false
```

Nesse exemplo, o texto só será válido se **terminar exatamente com "mundo"**.

<br />

Quando usadas juntas, as âncoras garantem que **toda a string siga o padrão definido**.

```js
const regex = /^JavaScript$/;

regex.test("JavaScript");        // true
regex.test("JavaScript é legal"); // false
```

📌 Esse uso é muito comum em **validações de formulário**, como e-mail, CPF ou senha.

<br />

<h3>1.10. Grupos em Expressões Regulares</h3>



Os **grupos** são definidos usando parênteses `()` e servem para **organizar partes da expressão regular**. Eles permitem aplicar regras como repetição ou validação a **conjuntos de caracteres**.

<br />

### ✅Criando um grupo simples



```js
const regex = /(ab)/;

regex.test("ab");     // true
regex.test("abc");    // true
regex.test("a");      // false
```

📌 O grupo `(ab)` trata os dois caracteres como uma única unidade.

<br />

### ✅Grupos com quantificadores



Os grupos ficam ainda mais poderosos quando combinados com quantificadores.

```js
const regex = /(ab)+/;

regex.test("ab");     // true
regex.test("abab");   // true
regex.test("aba");    // false
```

Aqui:

- O `+` indica que o grupo `(ab)` pode se repetir uma ou mais vezes

<br />

Os grupos são úteis para:

✔️ Repetir partes da expressão
✔️ Tornar a regex mais organizada
✔️ Criar validações mais precisas
✔️ Trabalhar com padrões complexos de forma controlada

<br />

<h2>2. Expressões Regulares para Validação (Padrões Brasil)</h2>



Na lista abaixo, vemos alguns exemplos de Expressões Regulares para Validação de dados, amplamente utilizadas em formulários HTML, seguindo os padrões do Brasil.

<br />

> [!WARNING]
>
> Todas as Expressões Regulares abaixo validam **apenas o formato**, não o algoritmo matemático dos documentos ou a veracidade da informação.

<br />

#### ✅CPF



Valida CPF no formato:

- 123.456.789-00

**Expressão Regular**

```js
const cpfregex = /(^\d{3}\.\d{3}\.\d{3}\-\d{2}$)/;
```

<br />

#### ✅CNPJ



Valida CNPJ no formato:

- 12.345.678/0001-00

**Expressão Regular**

```js
const cnpjregex = /(^\d{2}\.\d{3}\.\d{3}\/\d{4}\-\d{2}$)/;
```

<br />

#### ✅CPF ou CNPJ



Útil quando o campo aceita **qualquer um dos dois documentos**.

**Expressão Regular**

```js
const docsregex = /(^\d{3}\.\d{3}\.\d{3}\-\d{2}$)|(^\d{2}\.\d{3}\.\d{3}\/\d{4}\-\d{2}$)/;
```

<br />

#### ✅RG



Valida RG com ou sem separadores, incluindo final com `X` ou `x`.

**Expressão Regular**

```js
const rgregex = /(^\d{1,2}).?(\d{3}).?(\d{3})-?(\d{1}|X|x$)/;
```

<br />

#### ✅Telefone (fixo ou celular)



Valida números com ou sem DDD. O hífen é opcional.

**Expressão Regular**

```js
const telregex = /(^[0-9]{2})?(\s|-)?(9?[0-9]{4})-?([0-9]{4}$)/;
```

**Exemplos válidos:**

- `11987654321`
- `11 98765-4321`
- `9876-4321`

<br />

> [!IMPORTANT]
>
> A regex acima não cobre DDD entre parênteses `(11)`

<br />

#### ✅CEP



Valida CEP com ou sem hífen.

**Expressão Regular**

```js
const cepregex = /(^[0-9]{5})-?([0-9]{3}$)/;
```

<br />

#### ✅E-mail 



Regex amplamente utilizada para validação **prática** de e-mails (não cobre 100% do RFC 5322, por opção de simplicidade).

**Expressão Regular**

```js
const emailregex = /^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{2,3})+$/;
```

<br />

#### ✅Endereços WEB (URL)



Validação simples de URL's com `http`, `https` ou `ftp`.

**Expressão Regular**

```js
const urlregex = /^(((https?|ftp):\/\/)?([\w\-\.])+(\.)([\w]){2,4}([\w\/+=%&_\.~?\-]*))*$/;
```

<br />

#### ✅Senha



Senha com **mínimo de 8 caracteres**, contendo:

- Pelo menos **uma letra maiúscula**
- Pelo menos **uma letra minúscula**
- Pelo menos **um número**
- Pelo menos **um caractere especial**

**Expressão Regular**

```js
const senharegex = /(?=^.{8,}$)((?=.*\w)(?=.*[A-Z])(?=.*[a-z])(?=.*[0-9])(?=.*[|!"$%&\/\(\)\?\^\'\\\+\-\*]))^.*/;
```

Essa estrutura é a mais recomendada atualmente.

<br />

> [!IMPORTANT]
>
> - Expressões Regulares **não substituem validações de regra de negócio**, apenas valida formato.
> - Para documentos como CPF e CNPJ, a validação completa deve incluir o **cálculo dos dígitos verificadores**.
> - Expressões regulares devem ser **claras e legíveis**, priorizando manutenção e segurança.

<br />

<h2>3. Demonstrando o Funcionamento de uma Regex</h2>



Para demonstrar como funciona uma Expressão Regular, vamos utilizar a ferramenta **regex101.com**, que é uma das mais completas para **criar, testar e entender expressões regulares**, sendo amplamente utilizada em ambientes profissionais. 

Ela permite visualizar, em tempo real, como uma regex se comporta sobre um texto, além de oferecer explicações detalhadas e compatíveis com o **JavaScript (ECMAScript)**.

<br />

<div align="left"><img src="https://i.imgur.com/OVkkn06.png" title="source: imgur.com" width="5%"/> <a href="https://regex101.com/" target="_blank"><b>Ferramenta: Regular Expressions 101 - Regex101</b></a></div>

<br />

O **Regex101** é uma ferramenta online que oferece:

- Campo para inserir a **expressão regular**
- Área para digitar o **texto de teste**
- Destaque visual das **correspondências encontradas**
- Explicação detalhada de cada parte da regex
- Visualização de **grupos de captura**
- Suporte a **flags** (`g`, `i`, `m`, `s`)
- Seleção do **engine JavaScript (ECMAScript)**

Essa abordagem torna o Regex101 ideal tanto para aprendizado quanto para depuração de expressões regulares.

<div align="center"><img src="https://i.imgur.com/lB4xPkg.png" title="source: imgur.com"/></div>

<br />

Vamos analisar a expressão regular para validar CPF no formato `123.456.789-00`:

```js
^\d{3}\.\d{3}\.\d{3}-\d{2}$
```

<br />

1. No campo **Flavor**, selecione a linguagem de programação **ECMAScript (JavaScript)**

<div align="center"><img src="https://i.imgur.com/Rh9wKz9.png" title="source: imgur.com"/></div>

2. Cole a expressão regular no campo **Regular Expression**.

<div align="center"><img src="https://i.imgur.com/3zqPqrb.png" title="source: imgur.com"/></div>

3. No campo **Test String**, cole os exemplos abaixo:

```text
123.456.789-00
111.222.333-44
12345678900
```

*O Regex101 destacará apenas as linhas que correspondem exatamente ao padrão definido.*

<div align="center"><img src="https://i.imgur.com/TQ3RUyK.png" title="source: imgur.com"/></div>

4. No painel lateral **Explicação (Explanation)**, o Regex101 descreve cada parte da expressão:

<div align="center"><img src="https://i.imgur.com/5NGX8NQ.png" title="source: imgur.com"/></div>

| Trecho  | Função                 |
| ------- | ---------------------- |
| `^`     | Início da string       |
| `\d{3}` | Três dígitos numéricos |
| `\.`    | Ponto literal          |
| `-`     | Hífen literal          |
| `\d{2}` | Dois dígitos finais    |
| `$`     | Fim da string          |

Além disso, a ferramenta informa:

- Quantas correspondências foram encontradas
- O conteúdo de cada grupo de captura
- O impacto das flags selecionadas

5. No painel lateral **Corresponder Informação (Match Information)**, o Regex101 destaca os testes que foram validados pela regex:

<div align="center"><img src="https://i.imgur.com/HmD6khL.png" title="source: imgur.com"/></div>

*Note que o terceiro exemplo não será validado, pois a regex exige pontuação.*

<br />

Usar a ferramenta **Regex101** no aprendizado e desenvolvimento traz algumas vantagens:

- Explicações técnicas alinhadas ao ECMAScript
- Visualização clara de grupos e correspondências
- Ideal para debugging de regex complexas
- Excelente apoio didático para ensino e documentação

<br />

> [!WARNING]
>
> O Regex101 é uma ferramenta de apoio para **compreensão e testes**, mas não substitui a validação dentro do código JavaScript. Sempre teste suas expressões no contexto real da aplicação.

<br />

<h2>4. Métodos para Trabalhar com regex</h2>



No JavaScript, o uso de expressões regulares ocorre principalmente por meio de **dois conjuntos de métodos**:

1. **Métodos do objeto `RegExp`**
2. **Métodos do objeto `String` que aceitam regex**

Essa separação permite flexibilidade: ora a regex é o elemento principal, ora ela é apenas um critério aplicado sobre um texto.

<br />

<h3>3.1. Método test</h3>



O método `test()` pertence ao objeto `RegExp` e retorna um valor booleano (`true` ou `false`), indicando se o padrão foi encontrado na string.

### Sintaxe

```js
regex.test(string)
```

### <img src="https://i.imgur.com/zCTwZAz.png" title="source: imgur.com" width="5%"/>**Exemplo:**

```js
const regexEmail = /\S+@\S+\.\S+/;
const email = "contato@exemplo.com";

console.log(regexEmail.test(email)); 
```

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="4%"/>**Resultado:**

```js
true
```

`true` indica que o texto foi validado pela regex.

### Quando usar

- Validação de formulários
- Condições em estruturas `if`
- Verificações simples de formato

> Analogia: é como perguntar *“esse texto segue essa regra?”*.

<br />

<h3>3.2. Método exec</h3>



O método `exec()` também pertence ao objeto `RegExp` e retorna um **array com os resultados encontrados**, incluindo grupos de captura. Caso não encontre nada, retorna `null`.

### Sintaxe

```js
regex.exec(string)
```

### <img src="https://i.imgur.com/zCTwZAz.png" title="source: imgur.com" width="5%"/>**Exemplo:**

```js
const regex = /(\d{2})-(\d{4})/;
const texto = "Código: 12-3456";

const resultado = regex.exec(texto);
console.log(resultado);
```

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="4%"/>**Resultado:**

```js
["12-3456", "12", "3456"]
```

Primeiro é exibida a string que correspondeu completamente à expressão regular. Em seguida, é exibido o primeiro grupo de captura, contendo dois caracteres numéricos. Por fim, é exibido o segundo grupo de captura, contendo quatro caracteres numéricos.

### Quando usar

- Extração de partes específicas de uma string
- Leitura de dados estruturados
- Logs e parsing de texto

<br />

<h3>3.3. Método match</h3>



O método `match()` pertence ao objeto `String` e retorna os resultados da busca com base em uma regex.

### Sintaxe

```js
string.match(regex)
```

### <img src="https://i.imgur.com/zCTwZAz.png" title="source: imgur.com" width="5%"/>**Exemplo:**

```js
const texto = "CPF: 123.456.789-00";
const regexCPF = /\d{3}\.\d{3}\.\d{3}-\d{2}/;

console.log(texto.match(regexCPF));
```

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="4%"/>**Resultado:**

```js
[
  '123.456.789-00',
  index: 5,
  input: 'CPF: 123.456.789-00',
  groups: undefined
]
```

Primeiro é exibida a string que correspondeu à expressão regular. Em seguida, é informado o índice onde o padrão começa dentro da string original, neste caso, a partir da posição 5. Na sequência, será exibida a string original. O último item (`groups`) aparece como `undefined` porque a regex não utiliza **grupos de captura nomeados**.

<br />

<h3>3.4. Método search</h3>



O método `search()` retorna o **índice da primeira ocorrência** do padrão ou `-1` se não encontrar.

### Sintaxe

```js
string.search(regex)
```

### <img src="https://i.imgur.com/zCTwZAz.png" title="source: imgur.com" width="5%"/>**Exemplo:**

```js
const texto = "Email: contato@site.com";
console.log(texto.search(/@/)); 
```

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="4%"/>**Resultado:**

```js
14
```

14 é o **índice onde o @ foi encontrado** dentro da string original.

### Quando usar

- Localizar a posição de um padrão
- Análises simples de conteúdo

<br />

<h3>3.5. Método replace</h3>



O método `replace()` permite substituir partes da string que correspondem à regex.

### Sintaxe

```js
string.replace(regex, novoValor)
```

### <img src="https://i.imgur.com/zCTwZAz.png" title="source: imgur.com" width="5%"/>**Exemplo:**

```js
const texto = "CPF: 123.456.789-00";
const resultado = texto.replace(/\D/g, "");

console.log(resultado);
```

### <img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="4%"/>**Resultado:**

```js
12345678900
```

Observe que todos os caracteres não numéricos foram removidos da string.

<br />

<h3>3.6. Método split</h3>



O método `split()` divide uma string em partes com base em um padrão.

### Sintaxe

```js
string.split(regex)
```

<img src="https://i.imgur.com/zCTwZAz.png" title="source: imgur.com" width="5%"/>**Exemplo:**

```js
const texto = "HTML, CSS; JavaScript | React";
const tecnologias = texto.split(/[,;|]/);

console.log(tecnologias);
```

**Resultado:**

```js
["HTML", " CSS", " JavaScript ", " React"]
```

Observe que o texto foi separado sempre que encontrou **uma vírgula, um ponto e vírgula ou uma barra vertical**.

<br />

<div align="left"><img src="https://i.imgur.com/n6JxSHg.png" title="source: imgur.com" width="5%"/> <a href="https://aurelio.net/regex/guia/" target="_blank"><b>Guia das Expressões Regulares</b></a></div>

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="4%"/> <a href="https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Guide/Regular_expressions" target="_blank"><b>Documentação: Expressões Regulares no JavaScript</b></a></div>

<br />

------

## 🔑 Pontos-chave

1. Expressões regulares (regex) são padrões utilizados para validar, localizar ou extrair texto.
2. Em JavaScript, regex podem ser criadas usando delimitadores `/ /`.
3. Regex são amplamente utilizadas para validação de formulários no Frontend.
4. O método `test()` verifica se uma string corresponde a um padrão regex.
5. Quantificadores como `+`, `*` e `?` definem quantas vezes um padrão pode se repetir.
6. Âncoras como `^` e `$` garantem que toda a string siga o padrão definido.
7. Grupos `()` permitem organizar e repetir partes da expressão regular.
8. Caracteres especiais precisam ser escapados com `\`.
9. A validação de e-mail é um dos usos mais comuns de regex em aplicações web.
10. O uso correto de regex melhora a qualidade e segurança das entradas do usuário.

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>