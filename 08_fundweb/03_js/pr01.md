<h1>Projeto 01 - Portfólio - Seção About - Consumo API - GitHub</h1>



Agora que já temos nossas páginas HTML criadas e estilizadas com CSS, e já estudamos os conceitos básicos da linguagem **JavaScript** com foco em páginas web, chegou o momento de utilizar o JavaScript para **criar interatividade** e **adicionar ações** às nossas páginas.

Para começar, por meio do JavaScript, iremos **atualizar dinamicamente os dados do GitHub** exibidos na seção **About** da página *index*. Na prática, realizaremos uma **Requisição HTTP** para a **API do GitHub** e, a partir da resposta recebida, preencheremos automaticamente os elementos dessa seção, como:

- Foto de perfil
- Link do perfil
- Número de repositórios
- Número de seguidores

Dessa forma, os dados serão carregados diretamente da API e exibidos na página em **tempo real**, garantindo que as informações estejam sempre **atualizadas**, sem a necessidade de alterações manuais no código HTML.

<br />

> [!WARNING]
>
> **Ponto de Atenção**
>
> Para que os dados do perfil do usuário — incluindo **foto, link do perfil, número de repositórios e seguidores** — sejam exibidos corretamente na seção **About**, é fundamental que o **perfil do usuário no GitHub esteja completo**, especialmente a **foto de perfil** e demais informações do perfil público. Caso essas informações não estejam preenchidas, os elementos da seção About podem aparecer incompletos ou sem conteúdo.

<br />

<h2>👣 Passo 01 - Criar a pasta js</h2>



Antes de criar o nosso script JavaScript, vamos organizar a estrutura do projeto. Para isso, adicionaremos a pasta **js** dentro da pasta **assets**, que será responsável por armazenar todos os arquivos JavaScript do nosso website.

Essa organização facilita a manutenção do código, melhora a legibilidade do projeto e segue boas práticas adotadas no desenvolvimento web, separando **estrutura (HTML)**, **estilos (CSS)** e **comportamento (JavaScript)**.

1. Na **Barra Explorer**, selecione a pasta **assets** e clique no botão **New Folder** (Nova Pasta), indicado na imagem abaixo:

<div align="center"><img src="https://i.imgur.com/uVBuJQW.png" title="source: imgur.com" /></div>

2. O nome da pasta será **js**, como mostra a imagem abaixo. Após digitar o nome da pasta, pressione a tecla **enter** do seu teclado para concluir.

<div align="center"><img src="https://i.imgur.com/YmB0sck.png" title="source: imgur.com" /></div>

Na sequência, vamos criar o arquivo **script.js**:

<br />

<h2>👣 Passo 02 - Criar o arquivo script.js</h2>



Vamos criar o arquivo **script.js**, dentro da nossa pasta **js**:

1. Na **Barra Explorer**, selecione a pasta **js** e clique no botão **New File** (Novo Arquivo), indicado na imagem abaixo:  

<div align="center"><img src="https://i.imgur.com/RMLMrVL.png" title="source: imgur.com" /></div>

2. O nome do arquivo será **script.js**, como mostra a figura abaixo. Após digitar o nome do arquivo, pressione a tecla **enter** do seu teclado para concluir. 

<div align="center"><img src="https://i.imgur.com/sCRc293.png" title="source: imgur.com" /></div>

<br />

<h2>👣 Passo 03 - Inserir o arquivo script.js na página index</h2>



1. Abra a página **index.html**
2. Depois da Seção `<footer>`, insira o código abaixo:

```html
<!-- Script JavaScript-->
<script src="assets/js/script.js"></script>
```

3. Veja na imagem abaixo:

<div align="center"><img src="https://i.imgur.com/skZiUO0.png" title="source: imgur.com" /></div>

<br />

> [!WARNING]
>
> **O script JavaScript deve ser inserido exatamente no local indicado na imagem acima, ou seja, após todo o conteúdo da página HTML e imediatamente antes do fechamento da tag `</body>`.**
>
> Isso é necessário porque o JavaScript depende dos elementos HTML já estarem carregados no DOM. Caso o script seja executado antes do carregamento completo da página, os elementos ainda não existirão, o que impedirá o funcionamento correto do código.

<br />

<h2>👣 Passo 04 - Criar a Função getAboutGithub()</h2>



1. No arquivo **script.js** adicione o código abaixo:

<div align="center"><img src="https://i.imgur.com/Olyt21K.png" title="source: imgur.com" /></div>

Vamos analisar o código:

**Linha 02:** Seleciona o elemento HTML com o id `about` e armazena a referência na constante `about`. Esse elemento será utilizado para inserir dinamicamente os dados do perfil do GitHub na página.

**Linha 05:** Define a função assíncrona `getAboutGithub()`, responsável por buscar os dados do perfil no GitHub e atualizar o conteúdo da seção *About*.

**Linha 06:** Inicia o bloco `try`, utilizado para tratar possíveis erros durante a execução da requisição e manipulação dos dados.

**Linha 07:** Realiza uma requisição HTTP para a API do GitHub utilizando `fetch`, aguardando a resposta da API com `await`.

<br />

> [!TIP]
>
> A URL utilizada para buscar os dados do usuário no GitHub segue o padrão:
>
> ```js
> https://api.github.com/users/nome_usuario
> ```
>
>  Onde: 
>
> - `nome_usuario` é o seu usuário no GitHub. **Não esqueça de alterar!**
>
> Esse endpoint foi obtido diretamente na documentação oficial da API do GitHub e retorna informações públicas do perfil, como nome, avatar, seguidores e repositórios. 
>
> Nesta mesma documentação você pode consultar os atributos da resposta da API que serão utilizados para preencher dinamicamente os dados do seu perfil na Seção About.
>
> <br />
>
> <div align="left"><img src="https://i.imgur.com/JACNZiR.png" title="source: imgur.com" width="5%"/> <a href="https://docs.github.com/pt/rest/users/users?apiVersion=2022-11-28#get-a-user" target="_blank"><b>Documentação - API do GitHub</b></a></div>
>
> <br />

<br />

**Linha 08:** Converte a resposta da requisição para o formato JSON, permitindo o acesso às informações do perfil, como nome, foto, seguidores e repositórios.

**Linha 10:** Limpa o conteúdo atual da seção `about`, garantindo que o novo conteúdo seja inserido sem duplicações.

**Linha 12:** Insere dinamicamente o conteúdo HTML dentro da seção `about` utilizando *template literals*, permitindo interpolar os dados retornados pela API.

**Linhas 13 a 15:** Exibe a imagem do perfil do GitHub, utilizando a URL da foto (`avatar_url`) e o nome do usuário para melhorar a acessibilidade por meio do atributo `alt`.

**Linhas 18 a 31:** Define a estrutura textual da seção *Sobre mim*, contendo título e parágrafos descritivos. Esses textos devem ser personalizados conforme a necessidade do projeto.

<br />

> [!WARNING]
>
> Não esqueça de **substituir o texto temporário (Lorem Ipsum) pela sua descrição profissional**, semelhante ao resumo apresentado no **LinkedIn** e no **Perfil do GitHub**, destacando quem é você, a sua área de atuação, principais habilidades e tecnologias dominadas.

<br />

**Linhas 34 a 37:** Cria os botões de ação, incluindo um link direto para o perfil do GitHub utilizando `html_url` e um botão para currículo contendo um link para o seu currículo no formato PDF.

<br />

> [!WARNING]
>
> Não esqueça de disponibilizar o seu **Currículo no formato PDF**, através de um **link público no Google Drive**. 

<br />

**Linhas 40 a 47:** Exibe os dados numéricos do perfil, como quantidade de seguidores (`followers`) e repositórios públicos (`public_repos`), obtidos diretamente da API.

**Linha 52:** Inicia o bloco `catch`, responsável por capturar e tratar erros que possam ocorrer durante a requisição ou processamento dos dados.

**Linha 53:** Exibe uma mensagem de erro no console, facilitando a identificação de problemas durante o desenvolvimento.

**Linha 58:** Executa a função `getAboutGithub()` assim que o script é carregado, garantindo que os dados do GitHub sejam buscados e exibidos automaticamente ao carregar a página.

<br />

<h2>👣 Passo 05 - Excluir o conteúdo da Seção About - Página index</h2>



1. Abra a página **index.html**
2. Localize o trecho de código abaixo:

<div align="center"><img src="https://i.imgur.com/IE9corF.png" title="source: imgur.com" /></div>

3. Apague o conteúdo das **linhas 88 a 125**.
3. Após apagar, o trecho ficará igual a imagem abaixo:

<div align="center"><img src="https://i.imgur.com/jwNKE7B.png" title="source: imgur.com" /></div>

A Seção **About** passa a ser preenchida dinamicamente pela função **getAboutGithub()**, criada no arquivo **script.js**. Por esse motivo, não é mais necessário manter conteúdo fixo nessa seção do HTML.

Observe que o código removido da página foi reaproveitado dentro da função **getAboutGithub()**, com uma diferença essencial: **a URL da foto, a URL do perfil do GitHub, o número de repositórios públicos e o número de seguidores** agora são definidos dinamicamente por meio de **variáveis JavaScript**. 

Esses valores são obtidos a partir do **corpo da resposta da requisição HTTP** enviada para a API do GitHub. Dessa forma, na página **index** permanece apenas a marcação da Seção About (`<section>`), enquanto todo o conteúdo interno é inserido dinamicamente pelo JavaScript, garantindo informações sempre atualizadas.

<br />

<h2>👣 Passo 06 - Visualizar a Página</h2>



1. Abra a página index.html no Navegador, através do **Live Server** e clique no item **Sobre** do menu:

<div align="center"><img src="https://i.imgur.com/mTxF4ES.png" title="source: imgur.com" /></div>

Note que a Seção *About* foi criada de forma dinâmica pela função JavaScript **getAboutGithub()**, que realiza uma requisição à API Pública do GitHub, obtém os dados do usuário e insere essas informações diretamente na seção, modificando a DOM original da página `index.html`.

A foto do perfil, o link para o GitHub e o número de repositórios públicos e seguidores são atualizados dinamicamente pela função **getAboutGithub()**, garantindo que a seção *About* sempre exiba os dados mais recentes do usuário obtidos diretamente da API do GitHub.

<br />
<br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>

