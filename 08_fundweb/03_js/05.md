<h1>JavaScript: O Básico para o Frontend - Parte 02</h1>



Neste conteúdo vamos continuar o nosso **overview sobre os principais recursos da linguagem JavaScript**, com foco no Frontend.

<br />

<h2>1. Arrays</h2>



Arrays são **estruturas de dados fundamentais** no JavaScript e estão presentes na maioria dos scripts utilizados no desenvolvimento de aplicações Web, especialmente no Frontend, mesmo sem o uso de frameworks. 

Eles permitem **organizar, agrupar e manipular dados de forma eficiente**, tornando o código mais legível, reutilizável e de fácil manutenção.

No Frontend, arrays são amplamente utilizados para trabalhar com **listas**, **menus**, **tabelas**, **cards**, **dados de formulários** e **informações recebidas de APIs**.

Um **array** é uma estrutura de dados usada para armazenar **uma coleção ordenada de valores**, onde cada elemento pode ser de qualquer tipo de dado, como números, strings, valores booleanos, objetos ou até mesmo outros arrays.

<br />

**Sintaxe**

```js
let numeros = [1, 2, 3, 4, 5];
let nomes = ["Ana", "Carlos", "João"];
let misto = [10, "Texto", true];
```

Cada elemento dentro de um array possui um **índice numérico**, que **sempre começa em 0**.

```js
console.log(nomes[0]); // Ana
console.log(nomes[2]); // João
```

Esse índice é utilizado para acessar, alterar ou remover valores específicos do array.

<br />

<h3>1.1. Principais Métodos de Arrays</h3>



A seguir estão alguns dos **métodos mais utilizados no dia a dia do desenvolvimento Frontend**, especialmente para manipulação e iteração de dados.

<br />

#### ✅push() 



O método `push()` adiciona um ou mais elementos **ao final do array**.

```js
let cores = ["azul", "verde"];
cores.push("vermelho");

console.log(cores);
```

<br />

#### ✅pop() 



O método `pop()` remove o **último elemento do array**.

```js
let cores = ["azul", "verde"];
cores.pop();
console.log(cores);
```

<br />

#### ✅forEach()



O método `forEach()` executa uma função **para cada elemento do array**, sem retornar um novo array.

```js
let numeros = [1, 2, 3, 4];

numeros.forEach(numero => {
    console.log(numero);
});
```

Esse método é muito utilizado para **exibir listas de dados**, aplicar ações repetitivas ou executar efeitos colaterais, como logs ou atualizações na interface.

<br />

#### ✅map() 

O método `map()` percorre o array e **retorna um novo array**, contendo os valores transformados.

```js
let numeros = [1, 2, 3];
let dobrados = numeros.map(numero => numero * 2);

console.log(dobrados);
```

É ideal quando precisamos **modificar os dados sem alterar o array original**, seguindo o princípio de imutabilidade.

<br />

#### ✅filter() 



O método `filter()` cria um novo array contendo apenas os elementos que **atendem a uma condição específica**.

```js
let numeros = [1, 2, 3, 4, 5, 10, 20, 30, 40, 50];

let maioresQueDez = numeros.filter(numero => numero > 10);

console.log(maioresQueDez);
```

Muito utilizado em **filtros de listas**, **buscas**, **validações** e exibição dinâmica de dados no Frontend.

<br />

<h2>2. Funções</h2>



Uma **função** é um **bloco de código reutilizável**, criado para executar uma tarefa específica sempre que for chamado. O uso de funções ajuda a **organizar o código**, **evitar repetições** e **facilitar a manutenção** das aplicações.

Em JavaScript, para declarar uma função, utilizamos a palavra-chave **function**, seguida de um nome e, opcionalmente, uma lista de parâmetros.

**Sintaxe - Função com argumentos:**

```js
function nome(argumento1, argumento2,..., argumentoN){
   // Corpo da função
}
```

Os **argumentos** (ou parâmetros) são valores recebidos pela função e utilizados no seu processamento. Eles permitem que a função seja dinâmica e reutilizada em diferentes contextos.

<br />

**Sintaxe - Função sem argumentos:**

```javascript
function mensagem() {
    // Corpo da função
}
```

Nesse caso, a função executa sempre o mesmo comportamento, pois não depende de dados externos para funcionar.

Embora o JavaScript não exija a definição explícita do **tipo de retorno** de uma função, ela pode retornar um valor ao final da execução. Para isso, utilizamos a palavra reservada **return**, seguida do valor que desejamos retornar.

Caso a função **não possua a instrução `return`**, ela será considerada uma função **sem retorno** (conceitualmente semelhante a uma função `void`), retornando implicitamente `undefined`.

**Sintaxe - Função com retorno de valor:**

```javascript
function mensagem(argumento1, argumento2,..., argumentoN) {
    // Corpo da função
    return valor;
}
```

O valor retornado pode ser utilizado posteriormente, por exemplo, em uma variável, em uma condição ou em outra função.

Esse conceito é fundamental no JavaScript, pois funções com retorno permitem **processar dados**, **tomar decisões**, **reutilizar cálculos** e **encadear lógicas**, sendo amplamente utilizadas em aplicações Frontend e Backend.

<br />

## <img src="https://i.imgur.com/zCTwZAz.png" title="source: imgur.com" width="3%"/>**Exemplo 09- Calculadora com Funções**

```js
/* Criar as variáveis */
let numero_01 = 10, numero_02 = 2;

/* Executa as 4 Operações Matemáticas */
console.log(`Soma: ${somar(numero_01, numero_02)}`);
console.log(`Subtração: ${subtrair(numero_01, numero_02)}`);
console.log(`Multiplicação: ${multiplicar(numero_01, numero_02)}`);

const divisao = dividir(numero_01, numero_02);

/* Valida o resultado da divisão*/
if( divisao === Infinity)
    console.log("Não existe divisão por 0");
else
    console.log(`Divisão: ${divisao}`);

/* Funções de Cálculo */
function somar(numero_01, numero_02){
    return numero_01 + numero_02;
}

function subtrair(numero_01, numero_02){
    return numero_01 - numero_02;
}

function multiplicar(numero_01, numero_02){
    return numero_01 * numero_02;
}

function dividir(numero_01, numero_02){
    return numero_01 / numero_02;
}
```

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="3%"/>**Resultado do Código:**

```bash
Soma: 15
Subtração: 5
Multiplicação: 50
Divisão: 2
```

Caso o valor da variável `numero_02` seja modificado para `0`, a divisão não será realizada e será exibida a mensagem informando que **não existe divisão por zero**, como vemos na saída abaixo:

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="3%"/>**Resultado do Código - Divisão por zero:**

```bash
Soma: 12
Subtração: 8
Multiplicação: 20
Divisão: 5
```

<br />

<h2>3. Arrow Functions</h2>



Uma **Arrow Function** (ou **função seta**) é uma forma **mais curta e moderna** de escrever funções em JavaScript. Elas são amplamente utilizadas no desenvolvimento Frontend e Backend por tornarem o código **mais conciso, legível e expressivo**.

As Arrow Functions geralmente são utilizadas como **funções anônimas**, ou seja, funções **sem nome**, atribuídas a variáveis ou passadas como argumento para outros métodos (como `map`, `filter`, `forEach`, entre outros).

**Sintaxe:**

```js
(lista_de_parâmetros) => expressão
```

Uma Arrow Function é composta por três partes principais:

- **Lista de parâmetros:** valores de entrada da função. Pode estar vazia, conter um ou vários parâmetros.
- **Operador Lambda (`=>`):** separa os parâmetros do corpo da função.
- **Corpo da função:** contém a lógica a ser executada, podendo ser uma única expressão ou um bloco de código.

Os parâmetros ficam à **esquerda** do operador `=>` e o corpo da função à **direita**.

<br />

<h3>3.1. Características</h3>



A seguir, estão as principais características das Arrow Functions no JavaScript.

### 🔹 Parênteses nos parâmetros

- Se houver **apenas um parâmetro**, os parênteses são **opcionais**.
- Se houver **zero ou mais de um parâmetro**, os parênteses são **obrigatórios**.

```typescript
// Arrow Function com um parâmetro
const exibir = n => console.log(n);

// Arrow Function com dois ou mais parâmetros
const somar = (n1, n2) => console.log(n1 + n2);

exibir(5);
somar(5, 10);
```

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="3%"/>**Resultado do Algoritmo:**

```bash
5
15
```

<br />

### 🔹 Uso de chaves no corpo da função

- Quando a função possui **apenas uma instrução**, as **chaves são opcionais**.
- Quando há **mais de uma instrução**, as **chaves são obrigatórias**.

```ts
// Array de números
let numeros = [1, 2, 3, 4, 5, 6, 7, 8, 9];

// Arrow Function com mais de uma ação no Corpo da Função
numeros.forEach(numero => {
    if (numero % 3 == 0)
        console.log(numero)
})
```

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="3%"/>**Resultado do Algoritmo:**

```bash
3
6
9
```

<br />

### 🔹 Retorno implícito de valores

Quando o corpo da Arrow Function possui **apenas uma expressão**, o JavaScript realiza o **retorno automático** do valor, dispensando o uso da palavra-chave `return`.


```js
// Arrow Function com a instrução return
let funcaoComReturn = (n1, n2) => { return n1 + n2 };
console.log(funcaoComReturn(1,2));

// Arrow Function sem a instrução return
let funcaoSemReturn = (n1, n2) => n1 + n2;
console.log(funcaoSemReturn(1,2))
```

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="3%"/>**Resultado do Algoritmo:**

```bash
3
3
```

<br />

## <img src="https://i.imgur.com/zCTwZAz.png" title="source: imgur.com" width="3%"/>**Exemplo 10- Arrow Functions**

```ts
let numeros = [1, 2, 3, 4, 5, 6, 7, 8, 9];

console.log("Exibir os itens\n");

// Exibir os itens da Lista com Arrow Functions
numeros.forEach(numero => console.log(numero));

```

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="3%"/>**Resultado do Algoritmo:**

```bash
Exibir os itens

1
2
3
4
5
6
7
8
9
```

As Arrow Functions são especialmente úteis em **operações com arrays**, **callbacks**, **funções curtas** e **lógicas simples**, contribuindo para um código mais moderno, limpo e fácil de manter.

<br />

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="30px"/> <a href="https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Reference/Functions/Arrow_functions" target="_blank"><b>Documentação: Arrow Functions</b></a></div>

<br />

<h2>4. Tratamento de Exceções</h2>



As **Exceções** (*Exceptions*) representam situações inesperadas que ocorrem durante a execução de um programa. Esses eventos **interrompem o fluxo normal de execução** de uma função, método ou aplicação, podendo causar falhas se não forem tratados adequadamente.

Em JavaScript, o tratamento de exceções é fundamental para tornar o código **mais seguro, robusto e confiável**, evitando que a aplicação seja encerrada de forma abrupta diante de erros.

<br />

<h3>4.1. Estrutura try-catch-finally</h3>



A estrutura **try-catch-finally** é utilizada para **capturar e tratar erros** que podem ocorrer durante a execução de um bloco de código. Ela é composta por até três blocos:

- **try:** contém o código que pode gerar uma exceção. Caso ocorra um erro dentro desse bloco, a execução é interrompida e o controle é transferido para o bloco `catch`.
- **catch:** captura e trata a exceção lançada no bloco `try`. Esse bloco só será executado se ocorrer um erro.
- **finally:** bloco opcional que, quando definido, **sempre será executado**, independentemente de ocorrer erro ou não. É geralmente utilizado para liberar recursos ou finalizar processos.

**Sintaxe:**

```ts
try {
	// Instruções que podem causar uma Exception
} catch (error) {
	// Implementação do Bloco Catch - Caso Ocorra a Exception
} finally {
	// Implementação do Bloco Finally (opcional)
}
```

### ✅Bloco `try`

O bloco `try` contém as instruções que **podem lançar uma exceção**. Caso ocorra um erro, o restante do código dentro desse bloco não será executado.

<br />

### ✅Bloco `catch`

O bloco `catch` é responsável por **capturar e tratar o erro**. Ele recebe um parâmetro (geralmente chamado de `error`) que contém informações sobre a exceção ocorrida, como a mensagem e o tipo do erro.

Para tratar erros específicos, pode-se utilizar estruturas condicionais e o operador `instanceof`.

<br />

### ✅Bloco `finally`

O bloco `finally` é opcional, mas quando presente, quase sempre será executado. Ele é comumente utilizado para **finalizar conexões**, **liberar recursos** ou executar ações que devem ocorrer independentemente do sucesso ou falha do código.

<br />

<h3>4.2. O comando de Saída console.error()</h3>



O método **console.error()** funciona de forma semelhante ao `console.log()`, porém é utilizado especificamente para **exibir mensagens de erro** no console.

Em muitos ambientes, essas mensagens são destacadas visualmente (geralmente em vermelho), facilitando a identificação de falhas durante o desenvolvimento e a depuração do código.

<br />

## <img src="https://i.imgur.com/zCTwZAz.png" title="source: imgur.com" width="3%"/>**Exemplo 11 - Try...Catch**

```js
try {
    
    // Tenta converter um número em letras maiúsculas
    const resultado = converte(123);

    console.log(resultado);

} catch (error) {
    // Captura o erro e informa ao usuário
    console.error("Não é possível converter para letras maiúsculas!");
}

// Função para formatar uma string com todas as letras maiúsculas
function converte(conteudo){
    return conteudo.toUpperCase();
}
```

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="3%"/>**Resultado do Algoritmo:**

```bash
Não é possível converter para letras maiúsculas!
```

<br />

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="4%"/> <a href="https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Reference/Statements/try...catch" target="_blank"><b>Documentação: Try..Catch..Finally</b></a></div>

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="4%"/> <a href="https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Reference/Errors/Not_a_function" target="_blank"><b>Documentação: TypeError: x is not a function.</b></a></div>

<br />

<h3>4.3. Throw</h3>



A cláusula **throw** permite **lançar manualmente uma exceção** em qualquer ponto do código. Quando uma exceção é lançada com `throw`, a execução da função é imediatamente interrompida, e o controle é transferido para o primeiro bloco `catch` disponível na pilha de execução.

Se nenhum bloco `catch` existir, a aplicação será encerrada.

**Sintaxe:**

```ts
throw new Tipo_Erro("Mensagem");
```

<br />

## <img src="https://i.imgur.com/zCTwZAz.png" title="source: imgur.com" width="3%"/>**Exemplo 12 - Throw**

```js
try {
    
    let idade = 15;

    // Tenta validar a idade com a função validarIdade
    validarIdade(idade);

} catch (error) {
    // Caso a iade não seja validada, 
    // exibe a mensagem de erro (message)
    // e finaliza o programa
    console.error(error.message);
}

function validarIdade(idade) {

    if (idade < 18)
        // Se a idade for menor do que 18, a função lança uma Exceção
        throw new Error("A Pessoa não está apta a dirigir!");
    else
        // Caso contráro, exibe a mensagem abaixo
        console.log("A Pessoa está apta a dirigir!");

}
```

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="3%"/>**Resultado do Algoritmo:**

```bash
A Pessoa não está apta a dirigir!
```

<br />

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="4%"/> <a href="https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Reference/Statements/throw" target="_blank"><b>Documentação: throw.</b></a></div>

<br />

<h3>9.4. Por que devemos tratar as Exceções?</h3>



O tratamento de exceções permite que a aplicação **continue funcionando mesmo diante de erros**, oferecendo alternativas ao usuário e evitando falhas críticas.

Além disso, tratar exceções contribui para a criação de um código **mais limpo, organizado, previsível e de fácil manutenção**, sendo uma prática essencial no desenvolvimento profissional de aplicações JavaScript.

<br />

Com este conteúdo, você já possui uma base sólida sobre os principais recursos da linguagem JavaScript. A partir daqui, é possível avançar para conceitos mais avançados, explorando todo o potencial da linguagem no desenvolvimento Web.

<br />

<div align="left"><img src="https://i.imgur.com/JACNZiR.png" title="source: imgur.com" width="5%"/> <a href="https://github.com/rafaelq80/exemplos_fundweb/tree/main/04_js_basico" target="_blank"><b>Código fonte dos exemplos - JS</b></a></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div> 