<h1>Tratamento de Eventos</h1>



A interação do JavaScript com o HTML é tratada por meio de eventos, que ocorrem quando o usuário ou o navegador manipula uma página.

- **Quando uma página é carregada no Navegador, a renderização da página é chamada de evento.** 
- **Quando o usuário clica em um botão, o clique é chamado de evento.** 

- **Quando o usuário digita alguma coisa em um input, a mudança do conteúdo do input é chamada de evento.** 

Eventos são funções executadas em consequência de uma ação. Os eventos fazem parte do DOM e cada elemento HTML contém um conjunto de eventos que podem acionar uma função JavaScript, que executará um processamento.

Através destes eventos, as pessoas desenvolvedoras conseguem interagir com os elementos das páginas HTML, especialmente os campos dos formulários, criando respostas para estes eventos através da Linguagem JavaScript. Esta manipulação dos eventos é o que faz com que um botão feche uma janela, mensagens sejam exibidas na tela, dados sejam validados, dados sejam encaminhados para uma API, entre outras ações.

<br />

<h3>1.1. Eventos - HTML5</h3>



O HTML 5 possui uma extensa lista de eventos. Na tabela abaixo, listamos os eventos mais utilizados:

<br />

### 🖱️ Eventos de Mouse

| Evento          | Descrição                                                    |
| --------------- | ------------------------------------------------------------ |
| `onclick`       | Disparado quando o usuário clica com o botão principal do mouse. |
| `ondblclick`    | Disparado quando ocorre um **duplo clique** sobre o elemento. |
| `onmousedown`   | Disparado quando um botão do mouse é pressionado.            |
| `onmouseup`     | Disparado quando o botão do mouse é solto.                   |
| `onmousemove`   | Disparado sempre que o mouse se move dentro do elemento.     |
| `onmouseover`   | Disparado quando o ponteiro entra na área do elemento.       |
| `onmouseout`    | Disparado quando o ponteiro sai da área do elemento.         |
| `onmouseenter`  | Similar ao `mouseover`, mas **não propaga** para elementos filhos. |
| `onmouseleave`  | Similar ao `mouseout`, mas **não propaga** para elementos filhos. |
| `oncontextmenu` | Disparado ao clicar com o **botão direito** do mouse.        |
| `onwheel`       | Disparado ao usar a **roda do mouse** (scroll).              |

<br />

### ⌨️ Eventos de Teclado

| Evento       | Descrição                                                    |
| ------------ | ------------------------------------------------------------ |
| `onkeydown`  | Disparado quando uma tecla é pressionada.                    |
| `onkeyup`    | Disparado quando uma tecla é liberada.                       |
| `onkeypress` | Disparado enquanto a tecla é pressionada (**obsoleto**, evitar uso). |

📌 **Boa prática:** utilize `keydown` ou `keyup` em vez de `keypress`.

<br />

### 📝 Eventos de Formulário e Inputs

| Evento      | Descrição                                                    |
| ----------- | ------------------------------------------------------------ |
| `onsubmit`  | Disparado quando um formulário é enviado.                    |
| `onreset`   | Disparado quando um formulário é resetado.                   |
| `onchange`  | Disparado quando o valor muda **e o elemento perde o foco**. |
| `oninput`   | Disparado **em tempo real**, a cada alteração no valor do campo. |
| `oninvalid` | Disparado quando um campo não passa na validação HTML5.      |

<br />

### 🎯 Eventos de Foco

| Evento       | Descrição                                 |
| ------------ | ----------------------------------------- |
| `onfocus`    | Disparado quando o elemento recebe foco.  |
| `onblur`     | Disparado quando o elemento perde o foco. |
| `onfocusin`  | Variante do `focus` que **propaga**.      |
| `onfocusout` | Variante do `blur` que **propaga**.       |

<br />

### 📋 Eventos de Clipboard 

| Evento    | Descrição                       |
| --------- | ------------------------------- |
| `oncopy`  | Disparado ao copiar conteúdo.   |
| `oncut`   | Disparado ao recortar conteúdo. |
| `onpaste` | Disparado ao colar conteúdo.    |

<br />

### Observações importantes

- Eventos como `input` são ideais para **validação em tempo real**.
- Eventos de foco são essenciais para **acessibilidade e UX**.
- Eventos de mouse e teclado permitem criar **interfaces interativas e responsivas**.

Essa organização facilita a consulta e o entendimento de **quando e por que** utilizar cada tipo de evento.

<br />

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="30px"/> <a href="https://www.w3schools.com/jsref/dom_obj_event.asp" target="_blank"><b>Documentação: Lista de Eventos - HTML DOM </b></a></div>

<br />

<h2><img src="https://i.imgur.com/ZWD22yi.png" title="source: imgur.com" width="5%"/> Clonar o Projeto Guia</h2>



Antes de prosseguirmos, faça a clonagem do nosso Projeto Guia:

1. Acesse o Repositório do **Projeto Guia** clicando [aqui](https://github.com/rafaelq80/exemplos_fundweb)
2. Clone o Repositório do **Projeto Guia** na sua máquina
3. Abra o Projeto **06_js_eventos** no Visual Studio Code

<br />

<h2>2. Tratamento de Eventos</h2>



Existem 2 maneiras de se trabalhar com eventos no JavaScript:

- Definindo uma função do tipo **Event Handlers**
- Usando a função **addEventListener()**

<br />

<h3>2.1. Definindo Event Handlers</h3>



**Event Handlers** (ou manipuladores de eventos) são **funções definidas para serem executadas automaticamente** quando determinado **evento ocorre** em um elemento da página, como um clique, envio de formulário, digitação, entre outros.

Em JavaScript, podemos declarar uma função normalmente e vinculá-la a um evento por meio de **propriedades específicas**, como `onclick`, `onchange`, `onmouseover`, entre outras. Essa função será chamada sempre que o evento correspondente for disparado.

Vamos ver na prática como criar uma função do tipo **Event Handler**, que será associada ao evento `onchange` do `input` inserido no nosso Projeto Guia **Eventos-JS**:

1. Na pasta **assets/js**, abra o arquivo **scripts.js**.
2. Adicione o trecho de código abaixo o arquivo **scripts.js**:

```js
/* Seleciona os elementos pelo seu id */
const input_change = document.querySelector("#input_change");
const uppercase = document.querySelector("#uppercase");
const botao_click = document.querySelector("#botao_click");
const botao_over = document.querySelector("#botao_over");

/*  Cria uma função para formatar o texto do input 
    'input_change' com letras maiúsculas */
function upperCase() {
  // Converte o texto do input em letras maiúsculas
   uppercase.innerHTML = input_change.value.toUpperCase();
}
```

3. Abra a página **index.html** no VSCode e localize a linha abaixo:

```html
<input type="text" id="input_change" placeholder="Digite com letras minúsculas">
```

4. Substitua a linha acima pelo trecho de código abaixo:

```html
<input type="text" id="input_change" placeholder="Digite com letras minúsculas" onchange="upperCase()">
```

A linha acima associa a **function upperCase()** ao evento `onChange` do elemento **input**.

5. Abra a página **index.html** no navegador, através do **Live Server**

<div align="center"><img src="https://i.imgur.com/kbGPe9x.png" title="source: imgur.com" /></div>

6. Digite um texto no input **com letras minúsculas** e pressione a tecla **Tab** <img src="https://i.imgur.com/hVLFnkf.png" title="source: imgur.com" width="5%"/>do seu teclado:

<div align="center"><img src="https://i.imgur.com/IiTyKxs.png" title="source: imgur.com" /></div>

7. Observe que o texto digitado será exibido no **label uppercase** formatado com letras maiúsculas:

<div align="center"><img src="https://i.imgur.com/rWvKBnf.png" title="source: imgur.com" /></div>

Esta conversão foi realizada enquanto você digitava (evento onchange) e exibida no **label uppercase** após o **input input_change** perder o foco (ao pressionar a tecla tab).

<br />

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="30px"/> <a href="https://www.w3schools.com/js/js_events.asp" target="_blank"><b>Documentação: Eventos JavaScript </b></a></div>

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="30px"/> <a href="https://www.w3schools.com/jsref/dom_obj_event.asp" target="_blank"><b>Documentação: Lista de Eventos - HTML DOM </b></a></div>

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="30px"/> <a href="https://www.w3schools.com/jsref/event_onchange.asp" target="_blank"><b>Documentação: Evento onchange</b></a></div>

<br />

<h3>2.2. Tratando eventos com a função addEventListener()</h3>



A função `addEventListener()` é uma **função nativa do JavaScript** que permite **associar dinamicamente um evento a um elemento** do DOM. Com ela, é possível definir qual função será executada quando o evento ocorrer, sem a necessidade de manipular atributos diretamente no HTML.

Essa abordagem, além de ser a mais recomendada, favorece a **separação entre estrutura (HTML) e comportamento (JavaScript)**, além de permitir **registrar múltiplos manipuladores** para o mesmo evento em um único elemento. Em outras palavras, mantém o **HTML focado apenas na marcação da interface** (estrutura e conteúdo), enquanto o **JavaScript se concentra no comportamento** (respostas aos eventos e lógica de interação).

**Sintaxe:**

```js
elemento.addEventListener("evento", () => {
  // Ações que serão executadas
});
```

Onde:

- **elemento** é o elemento HTML, que será adicionado o evento;
- **evento** é o evento que será adicionando ao elemento HTML.

Vamos ver na prática como adicionar um evento em cada botão adicionado em nosso Projeto Guia **Eventos-JS**,  através da função `addEventListener()`:

1. Na pasta **assets/js**, abra o arquivo **scripts.js**.
2. Adicione o trecho de código abaixo o arquivo **scripts.js**:

```js
/* Atribui uma função para o evento Click do elemento 'botao_clique' */
botao_click.addEventListener("click", () => {
  alert("O botão foi clicado!");
});

/* Atribui uma função para o evento Mouse Over do elemento 'botao_over' */ 
botao_over.addEventListener("mouseover", () => {
  alert("O mouse passou sobre o botão!");
});
```

3. Abra a página **index.html** no navegador, através do **Live Server**

<div align="center"><img src="https://i.imgur.com/kbGPe9x.png" title="source: imgur.com" /></div>

4. Clique no primeiro botão:

<div align="center"><img src="https://i.imgur.com/wjSepng.png" title="source: imgur.com" /></div>

5. Observe que será exibido um **alert** informando que você clicou no botão:

<div align="center"><img src="https://i.imgur.com/tBh6IgF.png" title="source: imgur.com" /></div>

6. Passe o mouse sobre o segundo botão (não precisa clicar):

<div align="center"><img src="https://i.imgur.com/tP0wraL.png" title="source: imgur.com" /></div>

7. Observe que será exibido um **alert** informando que você passou o mouse sobre o botão (mouse over):

<div align="center"><img src="https://i.imgur.com/BFLm9xZ.png" title="source: imgur.com" /></div>

Observe que utilizando a função `addEventListener()` não efetuamos nenhuma alteração no código do nosso arquivo HTML.

<br />

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="30px"/> <a href="https://www.w3schools.com/jsref/met_element_addeventlistener.asp" target="_blank"><b>Documentação: Método addEventListener()</b></a></div>

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="30px"/> <a href="https://www.w3schools.com/jsref/event_onclick.asp" target="_blank"><b>Documentação: Evento onclick</b></a></div>

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="30px"/> <a href="https://www.w3schools.com/jsref/event_onmouseover.asp" target="_blank"><b>Documentação: Evento onmouseover</b></a></div>

<br />

<h2>3. O Objeto event</h2>



Quando um evento ocorre em um elemento da página — como um clique, o envio de um formulário ou a movimentação do mouse — o navegador cria automaticamente um **objeto especial**, chamado de **objeto `event`**.

Esse objeto contém **todas as informações relacionadas ao evento ocorrido**, como o tipo de evento, o elemento que o disparou e o contexto da interação do usuário.

Ao utilizar o método `addEventListener()`, esse objeto pode ser recebido como **parâmetro da função** associada ao evento.

### Sintaxe

```js
elemento.addEventListener("click", function(event) {
  // código executado quando o evento ocorre
});
```

O parâmetro `event` pode receber qualquer nome, porém, por convenção, utiliza-se **`event`** ou **`e`**.

<br />

<h3>3.1. Principais propriedades do objeto event</h3>



#### 🔹 `event.target`



Indica **qual elemento disparou o evento**, ou seja, o elemento que recebeu a interação do usuário.

<img src="https://i.imgur.com/zCTwZAz.png" title="source: imgur.com" width="5%"/>**Exemplo:**

```js
document.querySelector("button").addEventListener("click", function(event) {
  console.log(event.target);
});
```

👉 Nesse caso, `event.target` será o botão que foi clicado.

A partir da propriedade **target** é possível obter os atributos do elemento que disparou o evento. Na **tabela abaixo, temos uma lista com os principais atributos de `event.target`**:

| **Atributo**  | **Descrição**                                                | **Exemplo**                                                  |
| ------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `id`          | Retorna o `id` do elemento que disparou o evento             | `<button id="btn">Clique</button>``btn.addEventListener("click", e => console.log(e.target.id));` |
| `className`   | Retorna todas as classes do elemento como string             | `<div class="caixa ativo"></div>``div.addEventListener("click", e => console.log(e.target.className));` |
| `classList`   | Retorna as classes como objeto `DOMTokenList` e permite manipulação | `e.target.classList.add("novo");``e.target.classList.toggle("ativo");` |
| `value`       | Retorna o valor atual de inputs, selects ou textareas        | `<input type="text" id="nome" />``input.addEventListener("input", e => console.log(e.target.value));` |
| `textContent` | Retorna ou altera o texto dentro do elemento                 | `<p id="par">Olá</p>``par.addEventListener("click", e => e.target.textContent = "Clicado!");` |
| `innerHTML`   | Retorna ou altera o HTML interno do elemento                 | `div.addEventListener("click", e => e.target.innerHTML = "<strong>Negrito</strong>");` |
| `tagName`     | Retorna o nome da tag em maiúsculas                          | `console.log(e.target.tagName); // "BUTTON"`                 |
| `disabled`    | Indica se o elemento está desabilitado (boolean)             | `e.target.disabled = true;`                                  |
| `href`        | Para links, retorna ou altera o endereço                     | `<a href="https://ex.com" id="link">Link</a>``link.addEventListener("click", e => console.log(e.target.href));` |
| `src`         | Para imagens ou elementos com src, retorna ou altera a fonte | `<img src="img.png" id="img">``img.addEventListener("click", e => console.log(e.target.src));` |

<br />

> [!NOTE]
>
> **ALERTA DE BSM**
>
> *Orientação aos Detalhes*
>
> A propriedade **`target`** do objeto **event** será utilizada com frequência, principalmente no **React**, para acessar informações sobre o elemento que disparou um evento, como campos de formulário, botões ou links.

<br />

#### 🔹 `event.type`



Indica **o tipo de evento que foi disparado**, como `click`, `mouseover`, `submit`, entre outros.

<img src="https://i.imgur.com/zCTwZAz.png" title="source: imgur.com" width="5%"/>**Exemplo:**

```js
elemento.addEventListener("mouseover", function(event) {
  console.log(event.type);
});
```

Saída:

```
mouseover
```

<br />

 Esse tipo de informação é muito útil para:

- Depuração (*debug*)
- Identificar qual elemento disparou o evento
- Criar comportamentos dinâmicos reutilizáveis
- Trabalhar com múltiplos elementos usando um único manipulador de eventos

<br />

### Boas práticas

✔️ Utilize o objeto `event` sempre que precisar de informações sobre a ação do usuário
 ✔️ Evite depender exclusivamente de variáveis globais
 ✔️ Prefira funções mais genéricas e reutilizáveis
 ✔️ Explore propriedades como `event.target`, `event.type` e `event.preventDefault()` quando necessário

Esse conceito é fundamental para o desenvolvimento Frontend moderno, pois permite criar **interfaces interativas**, **responsivas** e **orientadas à ação do usuário**.

<br />

------

## 🔑 Pontos-chave

1. Eventos representam ações do usuário ou do navegador sobre a página.
2. O JavaScript reage a eventos para tornar a página interativa.
3. Cada elemento HTML possui eventos específicos que podem ser monitorados.
4. Eventos podem ser tratados por **Event Handlers inline** ou por **addEventListener()**.
5. O uso de `addEventListener()` é a prática recomendada em aplicações modernas.
6. Eventos como `click`, `change` e `mouseover` são amplamente usados em interfaces Frontend.
7. A função associada ao evento é executada automaticamente quando o evento ocorre.
8. O objeto **event** contém **todas as informações relacionadas ao evento ocorrido**, como o tipo de evento, o elemento que o disparou e o contexto da interação do usuário.
9. O uso correto de eventos permite validar dados, exibir mensagens e reagir às ações do usuário.
10. A compreensão de eventos é essencial para o desenvolvimento de aplicações Frontend interativas.

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div> 
