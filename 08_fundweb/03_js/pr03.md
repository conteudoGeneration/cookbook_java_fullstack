<h1>Projeto 01 - Portfólio - Seção Projects - Carrossel com Swipper</h1>



Após a criação dinâmica dos **cards de projetos**, através do consumo da API do GitHub, o próximo passo é implementar o **carrossel da seção Projects** utilizando a biblioteca **Swiper**.

Nesta etapa, o JavaScript será responsável apenas por **inicializar e configurar o Swiper**, transformando os cards já existentes em **slides navegáveis** dentro do carrossel. Para isso, os elementos gerados dinamicamente passam a ser inseridos dentro da `swiper-wrapper`, utilizando a classe `swiper-slide`, conforme a estrutura exigida pela biblioteca e criada na função `getProjectsGithub()`.

O Swiper permitirá adicionar recursos como **navegação por setas**, **paginação**, **rolagem horizontal suave** e **comportamento responsivo**, melhorando a experiência do usuário ao visualizar os projetos. A inicialização do carrossel ocorrerá **após o carregamento dos cards**, garantindo que todos os projetos sejam corretamente reconhecidos como slides.

Dessa forma, a seção **Projects** passa a exibir os projetos de forma interativa e organizada, mantendo o código de consumo da API e de geração dos cards intacto, enquanto o Swiper atua exclusivamente no controle visual e na navegação do carrossel.

<br />

<h2>1. A Biblioteca Swiper</h2>



A **Swiper** é uma biblioteca JavaScript moderna e amplamente utilizada para a criação de **carrosséis, sliders e apresentações interativas** em páginas web. Ela é conhecida por sua **alta performance**, **facilidade de configuração** e excelente suporte a **layouts responsivos**, funcionando de forma fluida em dispositivos desktop, tablets e smartphones.

Com a Swiper, é possível implementar recursos como **navegação por setas**, **paginação**, **autoplay**, **gestos de toque**, **loop de slides** e diferentes efeitos de transição, tudo de maneira simples e modular. Por essas características, a biblioteca é uma escolha popular para exibição de conteúdos dinâmicos, como **cards de projetos, banners e galerias**, proporcionando uma experiência de navegação intuitiva e profissional.

<br />

<div align="left"><img src="https://i.imgur.com/ZUDElcI.png" title="source: imgur.com" width="4%"/> <a href="https://swiperjs.com" target="_blank"><b>Site Oficial Swiper JS</b></a></div>

<div align="left"><img src="https://i.imgur.com/ZUDElcI.png" title="source: imgur.com" width="4%"/> <a href="https://swiperjs.com/get-started" target="_blank"><b>Documentação do Swiper JS</b></a></div>

<br />

Para utilizar a **biblioteca Swiper** em nosso projeto, vamos adicioná-la **via CDN (Content Delivery Network)**. Essa abordagem permite incluir rapidamente os arquivos necessários da biblioteca, sem a necessidade de download ou configuração local, tornando o processo mais simples e eficiente.

> Uma **CDN (Content Delivery Network)** é uma rede de servidores distribuídos geograficamente, projetada para **entregar conteúdos de forma mais rápida e eficiente** aos usuários. Em vez de carregar arquivos a partir de um único servidor, a CDN fornece os recursos a partir do servidor mais próximo do usuário, reduzindo o tempo de resposta e melhorando a performance da aplicação.

No contexto do desenvolvimento Frontend, CDN's são amplamente utilizadas para disponibilizar **bibliotecas JavaScript, arquivos CSS, fontes e imagens**, como é o caso da biblioteca **Swiper**. Além de melhorar o desempenho, o uso de CDN simplifica a configuração do projeto, elimina a necessidade de hospedar esses arquivos localmente e garante maior disponibilidade e confiabilidade no carregamento dos recursos.

<br />

<div align="left"><img src="https://i.imgur.com/ZUDElcI.png" title="source: imgur.com" width="4%"/> <a href="https://swiperjs.com/get-started#use-swiper-from-cdn" target="_blank"><b>Documentação do Swiper JS - CDN</b></a></div>

<br />

<h2>👣 Passo 01 - Inserir o script JS padrão do Swiper na página index</h2>




1. Abra a página **index.html**
2. Depois da Seção `<footer>`, antes do script padrão da página, insira o código abaixo:

```html
<!-- SWIPER JS -->
<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
```

3. Veja na imagem abaixo:

<div align="center"><img src="https://i.imgur.com/qyMXjuq.png" title="source: imgur.com" /></div>

<br />

> [!WARNING]
>
> **O script JavaScript do Swiper deve ser inserido antes do script JavaScript padrão da página HTML.**
>
> Essa ordem é essencial porque o **script principal da aplicação depende da biblioteca Swiper já estar carregada**. O Swiper fornece objetos, métodos e configurações que são utilizados na inicialização e no controle do carrossel.
>
> Se o script da página for executado **antes** do script do Swiper, o navegador não reconhecerá a biblioteca, resultando em erros como *“Swiper is not defined”* e impedindo o funcionamento do carrossel.
>
> Portanto, ao carregar primeiro o **JavaScript do Swiper**, garantimos que todos os recursos da biblioteca estejam disponíveis no momento em que o script da página for executado, assegurando a **inicialização correta do carrossel** e o comportamento esperado da seção **Projects**.

<br />

<h2>👣 Passo 02 - Criar a Função iniciarSwiper()</h2>



1. No final do arquivo **script.js** adicione o trecho de código abaixo:

<div align="center"><img src="https://i.imgur.com/w1uW4VH.png" title="source: imgur.com" /></div>

Vamos analisar o código:

**Linha 151:** Define a função `iniciarSwiper()`, responsável por inicializar o carrossel de projetos utilizando a biblioteca Swiper.

**Linha 152:** Cria uma nova instância do Swiper, associando o carrossel ao elemento HTML identificado pela classe `.projects-swiper`.

**Linha 153:** Define que apenas **1 slide** será exibido por vez no layout padrão.

**Linha 154:** Determina que o avanço do carrossel ocorrerá de **1 slide por grupo** a cada navegação.

**Linha 155:** Define o espaçamento padrão de **24px** entre os slides.

**Linha 156:** Indica que os slides não serão centralizados no container.

**Linha 157:** Ativa o modo de **loop**, fazendo com que o carrossel volte ao início após o último slide.

**Linha 158:** Habilita a verificação de overflow, evitando comportamentos incorretos quando a quantidade de slides for menor que o necessário.

**Linha 160:** Define o objeto `breakpoints`, responsável por controlar o comportamento do carrossel de acordo com a largura da tela.

**Linha 161 a 166:** Para telas pequenas (a partir de 0px), exibe 1 slide por vez, com maior espaçamento entre eles, garantindo melhor leitura em dispositivos móveis.

**Linha 167 a 172:** Para telas a partir de 769px (tablets), exibe 2 slides por vez, agrupando a navegação em pares e mantendo um espaçamento adequado.

**Linha 173 a 178:** Para telas a partir de 1025px (desktops), exibe 3 slides por vez, com maior espaçamento, aproveitando melhor a largura da tela.

**Linha 181:** Define o sistema de navegação por setas.

**Linha 182:** Associa o botão de próxima navegação ao elemento `.swiper-button-next`.

**Linha 183:** Associa o botão de navegação anterior ao elemento `.swiper-button-prev`.

**Linha 186:** Define a configuração da paginação do carrossel.

**Linha 187:** Associa a paginação ao elemento `.swiper-pagination`.

**Linha 188:** Permite que os indicadores de paginação sejam clicáveis.

**Linha 189:** Ativa o uso de bullets dinâmicos, ajustando a quantidade visível conforme a navegação.

**Linha 192:** Configura o autoplay do carrossel.

**Linha 193:** Define um intervalo de **5 segundos** entre as transições automáticas (5000 ms).

**Linha 194:** Pausa o autoplay quando o usuário posiciona o mouse sobre o carrossel.

**Linha 195:** Garante que o autoplay continue ativo mesmo após interação manual.

**Linha 198:** Ativa o cursor em formato de “mão”, indicando que o carrossel pode ser arrastado.

**Linha 199:** Define que não haverá deslocamento inicial antes do primeiro slide.

**Linha 200:** Define que não haverá deslocamento adicional após o último slide.

<br />

> [!WARNING]
>
> A função `getProjectsGithub()` será responsável por executar a função `iniciarSwiper()`.

<br />

<div align="left"><img src="https://i.imgur.com/ZUDElcI.png" title="source: imgur.com" width="4%"/> <a href="https://swiperjs.com/swiper-api" target="_blank"><b>Documentação do Swiper JS</b></a></div>

<br />

<h2>👣 Passo 03 - Visualizar a Página</h2>



1. Abra a página index.html no Navegador, através do **Live Server** e clique no item **Projetos** do menu:

<div align="center"><img src="https://i.imgur.com/Bq9K2PW.png" title="source: imgur.com" /></div>

Note que a Seção **Projects** é construída de forma dinâmica pela função JavaScript responsável por **listar os repositórios públicos do GitHub**. Essa função realiza uma **requisição à API Pública do GitHub**, obtém os dados dos projetos e **insere os cards diretamente no carrossel**, modificando a DOM original da página `index.html`.

Os **cards dos projetos** — contendo informações como nome do repositório, descrição, links e demais detalhes — são gerados automaticamente e adicionados à estrutura do **Swiper** via JavaScript. Dessa forma, a Seção **Projects** passa a exibir **sempre os projetos mais recentes**, garantindo que o carrossel esteja constantemente **atualizado com base nos dados retornados pela API do GitHub**, sem a necessidade de alterações manuais no HTML.

2. Acesse o Modo Responsivo do seu Navegador favorito
3. Selecione o Dispositivo **iPad Pro (Tablet)**

<div align="center"><img src="https://i.imgur.com/DgdqIg4.png" title="source: imgur.com" /></div>

4. Selecione o Dispositivo **iPhone SE (Smartphone)**

<div align="center"><img src="https://i.imgur.com/M2lWUuC.png" title="source: imgur.com" /></div>

Note que o Carrossel se adapta a largura da tela, reduzindo o número de cards visíveis na tela.

<br />

> [!WARNING]
>
> Em alguns casos, pode ser necessário **atualizar a página** para que o carrossel do **Swiper** se ajuste corretamente ao **tamanho da tela**.
>
> Isso ocorre porque o Swiper calcula as dimensões dos slides no momento da inicialização e, dependendo do carregamento dinâmico do conteúdo ou da resolução do dispositivo, pode ser preciso recarregar a página para que o layout seja recalculado e exibido corretamente.

<br />
<br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>