<h1>Projeto 01 - Portfólio - Validação de Dados do Formulário</h1>



A **validação de formulários HTML** é uma ferramenta essencial para garantir que os dados inseridos pelos usuários em formulários web sejam corretos e consistentes. Entre os principais **benefícios**, destacam-se:

1. **Integridade dos Dados**: A validação assegura que os dados enviados estejam no formato esperado, evitando erros, inconsistências e aumentando a qualidade das informações coletadas.
2. **Experiência do Usuário Aprimorada**: O feedback imediato, através de mensagens de erro, orienta o usuário sobre campos preenchidos incorretamente, melhorando a usabilidade e tornando a interação mais intuitiva e agradável.
3. **Redução de Requisições Desnecessárias**: Validar os dados no frontend evita o envio de informações inválidas ao servidor, economizando recursos e melhorando a eficiência do sistema.
4. **Segurança**: A validação ajuda a prevenir ataques, evitando que dados maliciosos, como *SQL Injection* ou *Cross-Site Scripting (XSS)*, sejam processados pelo servidor.

<br />

<h2>1. Tipos de Validação</h2>



Existem diversos tipos de validação que podem ser aplicados em formulários HTML, incluindo:

- **Presença de Dados**: Garante que campos obrigatórios sejam preenchidos.
- **Tipo de Dado**: Verifica se o valor corresponde ao tipo esperado (texto, número, e-mail, etc.).
- **Formato (Expressões Regulares)**: Valida campos que exigem formatos específicos, como e-mails, telefones, CEP, CPF, entre outros.
- **Unicidade**: Confirma se o valor informado já existe na base de dados.
- **Consistência entre Dados Relacionados**: Garante coerência entre campos interdependentes, como datas de início e término.
- **Complexidade de Dados**: Verifica se senhas ou outros campos atendem a requisitos mínimos de segurança (mínimo de 8 caracteres, pelo menos uma letra maiúscula, um número e um caractere especial).

<br />

<h2>2. Como realizar a validação de um formulário utilizando JavaScript?</h2>



Ao implementar a validação de formulários, algumas perguntas são fundamentais:

1. **O que é um dado válido?**
    Define o formato, tipo, tamanho e obrigatoriedade de cada campo.
2. **O que acontece se o usuário informar dados inválidos?**
    Determina o comportamento da validação, como exibição de mensagens de erro, nível de detalhamento, possibilidade de envio do formulário, ou registro de tentativas de envio inválidas.

Após definir esses pontos, os passos práticos são:

- Selecionar os campos do formulário com os métodos de seleção do JavaScript (`querySelector`, `getElementById`, etc.).
- Criar funções de validação específicas para cada campo ou tipo de dado.
- Associar essas funções aos campos através de eventos (`oninput`, `onblur`, `onsubmit`).
- Exibir mensagens de erro diretamente no formulário, utilizando elementos `<span>` ou `<div>` designados, manipulados pelo JavaScript.

**Próximo passo:** implementar o código JavaScript que realizará a validação, aplicando os conceitos apresentados e garantindo que os dados enviados sejam válidos e consistentes.

<br />

<h2>👣 Passo 01 - Atualizar o arquivo script.js</h2>



1. Localize o trecho de código abaixo no inicio do arquivo **script .js**:

<div align="center"><img src="https://i.imgur.com/YUNDG1j.png" title="source: imgur.com" /></div>

2. Adicione o código abaixo depois do trecho acima:

```js
// Seletor do Formulário
const formulario = document.querySelector('#formulario');

// Regex de validação do e-mail
const emailRegex = /^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{2,3})+$/;
```

3. Na imagem abaixo você confere o resultado:

<div align="center"><img src="https://i.imgur.com/2mC3qT6.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 07:** Seleciona o elemento HTML com o id `formulario` e armazena sua referência na constante `formulario`. Esse elemento será utilizado para aplicar eventos e realizar a validação dos dados informados pelo usuário.

**Linha 11:** Define a expressão regular `emailRegex`, responsável por validar o formato de endereços de e-mail.
Essa REGEX permite letras, números e alguns caracteres especiais antes do `@`, valida o domínio e garante a presença de uma extensão válida, como `.com`, `.net` ou `.org`, assegurando que o e-mail informado siga um padrão aceitável.

4. No final do arquivo **script.js** adicione o trecho de código abaixo:

<div align="center"><img src="https://i.imgur.com/1bAu3Z2.png" title="source: imgur.com" /></div>

Vamos entender o código:

**Linha 211:** Adiciona um *event listener* ao formulário para o evento `submit`, permitindo interceptar o envio dos dados para realizar a validação antes do envio.

**Linha 212:** Executa o método `preventDefault()`, impedindo o envio automático do formulário até que todas as validações sejam concluídas.

**Linhas 214 a 215:** Seleciona todos os elementos `<span>` do formulário e limpa as mensagens de erro exibidas anteriormente, garantindo que apenas os erros atuais sejam mostrados.

**Linha 217:** Declara a variável `isValid`, utilizada para controlar se o formulário está válido ou não ao final das validações.

**Linha 219:** Seleciona o campo `nome` do formulário.

**Linha 220:** Seleciona o elemento responsável por exibir a mensagem de erro do campo `nome`.

**Linha 222:** Verifica se o valor do campo `nome`, após remover os espaços em branco, possui menos de 3 caracteres.

> ### Função trim()
>
> A função **`trim()`** é utilizada para **remover os espaços em branco no início e no final de uma string**. Esses espaços geralmente são inseridos pelo usuário de forma involuntária, especialmente em campos de formulário, e podem causar problemas em validações e comparações de dados.
>
> Ao aplicar o método `trim()` em uma string, o JavaScript retorna um **novo valor sem os espaços extras**, sem alterar o conteúdo original da string. Esse método é muito útil para garantir que informações digitadas pelo usuário sejam processadas corretamente, evitando erros comuns como campos aparentemente preenchidos, mas que contêm apenas espaços em branco.

**Linha 223:** Exibe uma mensagem de erro caso a validação falhe.

**Linha 224:** Define o foco no campo `nome` apenas se este for o primeiro campo inválido encontrado.

> ### Função focus()
>
> A função **focus()** é usada para dar foco a um elemento HTML, como um campo de entrada, botão ou link. 
>
> Quando chamada em um elemento, ela o torna o elemento ativo no momento, geralmente destacando-o ou preparando-o para interação do usuário.

**Linha 225:** Atualiza a variável `isValid` para `false`, indicando que o formulário contém erros.

**Linha 228:** Seleciona o campo `email` do formulário.

**Linha 229:** Seleciona o elemento responsável por exibir a mensagem de erro do campo `email`.

**Linha 231:** Valida o formato do e-mail utilizando a expressão regular `emailRegex`.

**Linha 232:** Exibe uma mensagem de erro caso o e-mail seja inválido.

**Linha 233:** Define o foco no campo `email` se ainda não houver erro anterior.

**Linha 234:** Marca o formulário como inválido.

**Linha 237:** Seleciona o campo `assunto` do formulário.

**Linha 238:** Seleciona o elemento responsável por exibir a mensagem de erro do campo `assunto`.

**Linha 240:** Verifica se o campo `assunto` possui menos de 5 caracteres.

**Linha 241:** Exibe a mensagem de erro correspondente.

**Linha 242:** Define o foco no campo `assunto`, se for o primeiro erro encontrado.

**Linha 243:** Atualiza o estado de validação para inválido.

**Linha 246:** Seleciona o campo `mensagem` do formulário.

**Linha 247:** Seleciona o elemento responsável por exibir a mensagem de erro do campo `mensagem`.

**Linha 249:** Verifica se o campo `mensagem` está vazio após remover os espaços em branco.

**Linha 250:** Exibe a mensagem de erro correspondente.

**Linha 251:** Define o foco no campo `mensagem`, se for o primeiro erro identificado.

**Linha 252:** Marca o formulário como inválido.

**Linha 255:** Verifica se todas as validações foram concluídas com sucesso.

**Linha 256:** Seleciona o botão de envio do formulário.

**Linha 257:** Desabilita o botão de envio, evitando múltiplos envios.

**Linha 258:** Altera o texto do botão para informar o usuário que o formulário está sendo enviado.

**Linha 260:** Envia o formulário programaticamente utilizando o método `submit()`.

**Linha 262:** Finaliza o bloco do *event listener*.

<br />

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="4%"/> <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/trim" target="_blank"><b>Documentação: Função trim()</b></a></div>

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="4%"/> <a href="https://developer.mozilla.org/en-US/docs/Web/API/Event/preventDefault" target="_blank"><b>Documentação: Função PreventDefault()</b></a></div>

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="4%"/> <a href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement/focus" target="_blank"><b>Documentação: Função Focus()</b></a></div>

<div align="left"><img src="https://i.imgur.com/r9lrbPG.png" title="source: imgur.com" width="4%"/> <a href="https://www.w3schools.com/jsref/met_form_submit.asp" target="_blank"><b>Documentação: Função Submit()</b></a></div>

<br />

<h2>👣 Passo 02 - Testar o Formulário</h2>



1. Abra a página index.html no Navegador, através do **Live Server** e clique no item **Contato** do menu:

<div align="center"><img src="https://i.imgur.com/vSGGOM0.png" title="source: imgur.com" /></div>

2. Clique no botão **Enviar Mensagem** e observe que **todas as mensagens de erro de validação** serão exibidas

<div align="center"><img src="https://i.imgur.com/QBgBIfw.png" title="source: imgur.com" /></div>

3. Preencha o Formulário com dados inválidos e clique no botão **Enviar Mensagem**

<div align="center"><img src="https://i.imgur.com/1mQd9lW.png" title="source: imgur.com" /></div>

4. Observe que **todas as mensagens de erro de validação** continuarão sendo exibidas
5. Corrija o campo **nome** e clique no botão **Enviar Mensagem**

<div align="center"><img src="https://i.imgur.com/rCu9FIn.png" title="source: imgur.com" /></div>

6. Observe que **a mensagem de erro de validação do campo nome sumiu**, mas as demais continuam sendo exibidas
7. Corrija o campo **e-mail** e clique no botão **Enviar Mensagem**

<div align="center"><img src="https://i.imgur.com/3dYV2vF.png" title="source: imgur.com" /></div>

8. Observe que **a mensagem de erro de validação do campo e-mail sumiu**, mas as demais continuam sendo exibidas
9. O formulário será enviado somente depois de todos os campos passarem pela validação, como no exemplo abaixo

<div align="center"><img src="https://i.imgur.com/nTcJ8ls.png" title="source: imgur.com" /></div>

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div> 
