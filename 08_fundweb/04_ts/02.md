<h1>Primeiro Projeto TypeScript</h1>



Vamos criar o nosso **primeiro script TypeScript**, entendendo a **estrutura básica de um arquivo**, bem como o processo pelo qual o código é **verificado, transpilado e executado**.

Neste capítulo, utilizaremos o **Node.js** em conjunto com o **TypeScript** apenas como ferramenta de execução. Na prática, o TypeScript será responsável por **analisar e validar o código**, enquanto o Node.js executará o **JavaScript gerado após o processo de transpilação**.

Diferentemente do JavaScript, o **TypeScript não é executado diretamente no navegador** dentro de uma página HTML. Por esse motivo, inicialmente, iremos executar nossos scripts **via terminal**, o que nos permitirá conhecer a linguagem, sua sintaxe e os benefícios da **tipagem estática** de forma mais clara e controlada.

Mais adiante, essa combinação será utilizada para rodar **ferramentas de desenvolvimento** e **servidores de desenvolvimento Frontend**, ampliando significativamente as possibilidades de uso da linguagem e proporcionando **mais segurança, organização e manutenibilidade ao código**.

<br />

<h2>👣 Passo 01 - Criar o ambiente do primeiro Projeto TypeScript</h2>



Para utilizar o TypeScript, precisamos ter o Node instalado na máquina. Através do Gerenciador de Pacotes do Node (NPM) faremos a instalação do **Transpilador do TypeScript**:

1. Crie uma pasta na **Área de Trabalho**, chamada **typescript**.
2. Abra esta pasta **typescript** no Visual Studio Code, clicando no menu **File (Arquivo)**, na opção **Open Folder (Abrir Pasta)**.
3. Selecione a pasta **typescript**, que foi criada na Área de Trabalho.
4. Abra o Terminal do Visual Studio Code e digite o comando abaixo para configurar o **Node e o NPM**:

```bash
npm init -y
```

5. Observe que será criado o arquivo **package.json**
6. Na sequência, digite o comando abaixo para instalar o **TypeScript**:

```bash
npm install -g typescript
```

7. Em seguida, digite o comando abaixo para checar se o TypeScript foi instalado corretamente:

```bash
tsc -v
```

8. Será exibido na tela a versão do TypeScript:

```bash
Version 5.9.3
```

<br />

> [!WARNING]
>
> No momento em que este material foi escrito, **a versão mais atual do TypeScript era a versão 5.9.3**. Ao utilizar este material no futuro a versão mais atual será outra.

<br />

> Caso apareça a mensagem: 
>
> ```bash
> "tsc is not recognized as an internal or external command". 
> ```
>
> 1. Na Barra de Pesquisar do Windows, localize o app **Editar as Variáveis de Ambiente do Sistema**.
> 2. Será aberta a janela abaixo. Clique no botão **Variáveis de Ambiente**
> 3. No item **Variáveis de Ambiente do Usuário**, dê um duplo clique no item **path**
> 4. Verifique se existe o caminho para a pasta do NPM (**c:\users\seu_usuario\AppData\Roaming\npm**), como mostra a imagem abaixo:
> 5. Se não existir, adicione este caminho através do botão **Novo**.

9. TypeScript instalado, vamos instalar a Biblioteca **ts-node**, para simplificar o uso do **TypeScript**, através do comando:

```bash
npm install -g ts-node
```

10. Na sequência, digite o comando abaixo para checar se o ts-node foi instalado corretamente:

```bash
ts-node -v
```

11. Será exibido na tela a versão do ts-node:

```bash
v10.9.1
```

<br />

> [!WARNING]
>
> No momento em que este material foi escrito, **a versão mais atual do TS-Node era a versão 10.9.2**. Ao utilizar este material no futuro a versão mais atual será outra.

<br />

12. Para finalizar, vamos inicializar o Projeto TypeScript através do comando abaixo:

```bash
tsc --init
```

13. Observe que será criado o arquivo **tsconfig.json**

<br />

<div align="left"><img src="https://i.imgur.com/izFuHID.png" title="source: imgur.com" width="30px"/> <a href="https://www.typescriptlang.org/pt/download" target="_blank"><b>Documentação - TypeScript: Instalação</b></a></div>

<br />

<h2>👣 Passo 02 - Criar o Projeto Hello World</h2>



1. Crie um novo arquivo, na pasta **typescript**, chamado **HelloWorld.ts**.
2. Insira o código abaixo:

```typescript
console.log("Hello World!");
```

<br />

> [!TIP]
>
> Assim como no JavaScript, o ponto e vírgula no final da linha é opcional.

<br />

4. No Terminal do Visual Studio Code, execute o comando abaixo:

```bash
tsc HelloWorld.ts
```

5. Observe que será criado na mesma pasta um arquivo chamado **HelloWorld.js**

<div align="center"><img src="https://i.imgur.com/vVURe45.png" title="source: imgur.com" /></div>

> O arquivo **HelloWorld.ts** foi **transpilado**, e durante esse processo foi gerado o arquivo **HelloWorld.js**. Isso significa que o código escrito em **TypeScript** foi convertido para **JavaScript**, que é o formato efetivamente compreendido e executado pelo **Node.js** ou pelo navegador.
>
> Esse processo de transpilação é fundamental, pois o TypeScript adiciona recursos como **tipagem estática e verificações em tempo de desenvolvimento**, mas o código final executado em produção sempre será JavaScript.

6. Abra o arquivo **HelloWorld.js** e veja o código:

<div align="center"><img src="https://i.imgur.com/TJjdUBs.png" title="source: imgur.com" /></div>

> Neste exemplo, por se tratar de um código simples, **os arquivos `HelloWorld.ts` e `HelloWorld.js` apresentam praticamente o mesmo conteúdo**. Isso ocorre porque não há recursos específicos do TypeScript, como **tipos, interfaces ou enums**, que exijam transformações mais evidentes durante a transpilação.
>
> À medida que o código passa a utilizar **tipagem estática e outras funcionalidades do TypeScript**, as diferenças entre o arquivo `.ts` e o `.js` gerado tornam-se mais perceptíveis, reforçando o papel do TypeScript como uma camada de segurança e organização durante o desenvolvimento.

7. Vamos executar o arquivo **HelloWorld.js** através do comando abaixo:

```bash
node HelloWorld.js
```

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="3%"/>**Resultado do Código:**

```bash
Hello World!
```

<br />

<h2>👣 Passo 03 - Executar o Projeto Hello World com o ts-node</h2>



Através do **pacote `ts-node`**, é possível **automatizar todo o processo de transpilação e execução** do código TypeScript em um **único comando**, eliminando a necessidade de gerar manualmente o arquivo JavaScript intermediário.

Na prática, o `ts-node` interpreta o código TypeScript, realiza a transpilação em memória e executa o JavaScript resultante imediatamente, tornando o fluxo de desenvolvimento mais simples e produtivo.

1. Apague o arquivo **HelloWorld.js**
2. No Terminal do Visual Studio Code, execute o comando abaixo:

```bash
ts-node HelloWorld.ts
```

<img src="https://i.imgur.com/V2ReOnx.png" title="source: imgur.com" width="3%"/>**Resultado do Código:**

```bash
Hello World!
```

Observe que o **resultado é exatamente o mesmo** da execução tradicional. A diferença é que, ao utilizar o `ts-node`, **nenhum arquivo `.js` é gerado**, pois a transpilação ocorre apenas em tempo de execução. Essa abordagem é muito utilizada em ambientes de desenvolvimento e aprendizado, por sua praticidade e agilidade.

<br /><br />

<div align="left"><a href="README.md"><img src="https://i.imgur.com/XMgF3gl.png" title="source: imgur.com" width="3%"/>Voltar</a></div>